# 项目开发路线图

> 回答「接下来做什么、优先级、前置依赖、什么时候才能做」。
> **不是历史日志**——已完成的开发流水在 `docs/DEVELOP_LOG.md`，这里只留一行完成结论 + 指针。
> 更新触发：任务完成 / 新增 / 优先级或依赖变化 / 暂停 / 解锁 / 验收条件变化。
> 不要同时保留多个互相冲突的「下一步」。

核对时间：2026-09-01。

---

## 当前阶段目标

在**约一周内无法进行 MuMu / 游戏内真机测试**的约束下，只推进「纯静态可验证、且不改变现有真实生产行为」的工作：

1. 建立可长期维护的行为观测能力（BehaviorTrace）——**已完成 v1**。
2. 清理误导性 / 死代码，澄清跨后端契约——**进行中**。
3. 为后续需要真机的重构（状态机、列表稳定判定、Wait/Retry 策略）打地基（组件设计 + 单元可测的部分）。

真机窗口打开后再落地 Level C 改动（见下方「需要真机验证的任务」）。

---

## 已完成

| 编号 | 内容 | 结论 | 详见 |
|---|---|---|---|
| T0-1 | `wait_until_appear_then_click` timeout 参数绑定 Bug | 已修：改为 `wait_until_appear(target, wait_time=wait_time)` 关键字传参；5 用例，正反向验证 | `docs/DEVELOP_LOG.md` 2026-09-01；`docs/AI_CONTEXT.md` §4.12 |
| T1-1 | BehaviorTrace v1 | 已实现：`module/behavior_trace.py`，默认关闭，只记 `ACTION` / `TASK`，接入 `Control.{click,long_click,swipe}` + `Script.run`；14 用例 | `docs/AI_CONTEXT.md` §4.13；`docs/DECISIONS.md` D004 |
| T1-2 | BehaviorTrace `set_task()` 生命周期复核 | 已复核，**无需修改**：`Script.run` 的 `finally` 在所有出口 `set_task('')`，顺序先 record TASK 后清空 | `docs/AI_CONTEXT.md` §4.14；`docs/DECISIONS.md` D005 |
| T3-1 | `Control.swipe` `duration` 契约澄清 | 已完成：只加 docstring 说明后端消费边界；曾误删 RyouToppa duration 后**已收口**，`flush_area_cache` 与 HEAD 逐字一致；7 用例 | `docs/AI_CONTEXT.md` §4.14；`docs/DECISIONS.md` D003 |
| T3-2 | `RuleSwipe.trace()` 死代码清理 | 已完成：删 `trace()` + `is_default_mode` / `is_vector_mode` + 4 个 import；`RuleSwipe` 只剩 `__init__` / `coord()`；4 用例 | `docs/AI_CONTEXT.md` §4.15；`docs/DECISIONS.md` D006 |
| — | 疲劳 / 发呆 / 休息系统 P0-A / P0-B / scheduler_idle / idle 事件率模型 | 已实现并补测试，默认关闭，仅 `RyouToppa` 接入 | `docs/AI_CONTEXT.md` §4.9；`docs/DEVELOP_LOG.md` 2026-08-30 多条 |
| — | KekkaiUtilize 结界蹭卡选卡缺陷 + 收益阈值 + 可靠性修复 | 已实现，默认阈值等于六星满值（默认行为零变化） | `docs/AI_CONTEXT.md` §4.11 |
| — | Minitouch 自定义轨迹能力专项审查 | 已完成（2026-09-03，纯只读，零代码改动，`minitouch.py` 对 HEAD 零 diff）：确认 **协议层 = YES**（`Command` / `CommandBuilder` 通用；`m {contact} {x} {y} {pressure}` 任意 x/y、无「x 不变」约束；`.wait(ms)` 逐段独立；`down → (move.commit.wait(dt))* → wait(short) → up` 今天就能表达）；**当前执行层 = PARTIAL**（`swipe_minitouch` / `drag_minitouch` / `_press_and_drag_minitouch` 已逐点发 MOVE + 每 MOVE 独立 `random_int(6,15)` + 贝塞尔曲线路径，但路径 / dt 写死在 `insert_swipe` + 内联 for 循环，没有方法接受调用方点列表）；**当前公开 swipe API = PARTIAL（对自定义轨迹 = NO）**（只 `(p1,p2,duration)`，minitouch 忽略 duration）。**推荐 Plan B**（第一阶段见下一行，已实施）。专题 `docs/Minitouch自定义轨迹能力审查.md` | `docs/AI_CONTEXT.md` §8.2；`docs/Minitouch自定义轨迹能力审查.md` |
| — | TouchSwipeModel 第一阶段实现（Plan B 基础设施） | 已完成（2026-09-04，`docs/DECISIONS.md` **D018**）：落地「模型出纯轨迹数据 → minitouch 薄执行层 → 显式入口」三层，**未接任何任务、未接 `BaseTask`、未真机**。① 新增 `module/device/touch_swipe_model.py` 的 `TouchSwipeModel(params, rng).generate(start,end) -> [(x,y,dt_ms)]`——minimum-jerk `s(t)=10t³-15t⁴+6t⁵`（前慢中快后慢）+ 整体曲率 `curve_amp·sin(πt)`（可正可负、幅度有上限、无逐点抖动 → 左弯/右弯/近直线不锯齿）+ 逐段 dt（两端放慢 + 有界抖动，全 `>0`）+ 距离决定点数；首点 dt=0 且执行层忽略、起终点强制精确；`TouchSwipeParams`（frozen）+ `_DefaultRng`（委托公共 `random_int`）+ `rng=` 可注入；非法输入 `ValueError`；可选尾部微修正默认关。② `Minitouch.swipe_minitouch_trajectory`（`_ensure_trajectory` 在 `@retry` 外校验 + `@retry _swipe_minitouch_trajectory_run` 发命令，不套 `insert_swipe`、不叠加 `random_int(6,15)`、不改 `Command`/`CommandBuilder`/`minitouch_send`/握手）。③ `Control.swipe_trajectory(trajectory, control_name)`（显式 opt-in，非 minitouch 后端 `NotImplementedError`、不静默退化端点滑动、BehaviorTrace 端点级一行、**不改 `Control.swipe`**）。`BaseTask.swipe` / `RuleSwipe` / `insert_swipe` / KekkaiUtilize / FrameWait / 结界卡 ROI 均未动。+53 测试（29+15+9），回归 **783→836** | `docs/AI_CONTEXT.md` §4.42；`docs/DECISIONS.md` D018 |
| — | TouchSwipeModel 第二阶段（平滑时间扰动 + 尾段慢拖） | 已完成（2026-09-04，`docs/DECISIONS.md` **D018 第二阶段追加**）：正式生产模型优化，**只改 `module/device/touch_swipe_model.py` + `tests/test_touch_swipe_model.py`**，未接消费者、未真机。① 逐段 dt 随机从「每 MOVE 独立 `randint(-3,3)`」改为**一阶低通 / EMA**（`noise_i = α·noise_{i-1} + (1-α)·raw_i`，凸组合 → 恒有界 / 零均值 / 不漂移 / 无无界跳变），消除 speed 曲线中段尖峰；时间模型固定拆成 `base_dt(s)` 趋势 + smooth_noise，趋势 ≫ 噪声。② **尾段慢拖**（按已走距离占比 s，约最后 15% 距离）：参数 t 网格主段等距 + 尾段按 `tail_density_gain` 倍加密（`_t_for_s` 二分求解）、`slow` 因子叠加 `tail_slow_gain·smoothstep(k)`（平滑接续不突跳）；**终点仍精确、不额外滑一段、不做固定 pre-UP 停顿、不过冲回拉**。③ `TouchSwipeParams` +4 字段（`dt_smooth_alpha=0.72` / `tail_start_ratio=0.85` / `tail_density_gain` / `tail_slow_gain`，均有范围校验），改默认 `max_dt_ms 28→34` / `max_points 64→80`；**`max_curve_px` / `curve_px_ratio` 空间曲率参数本轮明令不动**。**尾段力度「收一档」（2026-09-04，仅两个默认值）：`tail_density_gain 1.55→1.35`、`tail_slow_gain 0.55→0.35`**（第二阶段 Level C 后末段速度偏低有「黏住终点」感 → 收一档，`tail_start_ratio` 与算法不动）。静态数据（非真机，speed 已按 executor 时间对齐；收一档后）：120→~117ms / 260→~202~220ms / 440→~366~398ms，`120<260<440` 严格成立、中段 dt≈10~11ms 不变、每个 case `tail_avg_dt > mid_avg_dt` 且 `tail_avg_speed < mid_avg_speed`、末端段速度不再贴 0。`test_touch_swipe_model.py` 29→50（对齐修正后 →56；收一档零新增测试），回归 **867→888→897**。executor / 协议 / `dt` 语义 / `Control.swipe_trajectory` / KekkaiUtilize / FrameWait 未改 | `docs/AI_CONTEXT.md` §4.42「第二阶段」；`docs/DECISIONS.md` D018 第二阶段追加 |
| — | TouchSwipeModel Level C 工具：segment speed 与 dt 对齐修正 | 已完成（2026-09-04，**FIXED 统计一位偏移**，生产零改动）：从源码核实 executor 命令流 `DOWN(p0) | MOVE(p_i) COMMIT WAIT(dt_i) ... | UP` —— `dt_i` 是「到达 `p_i` 后、移动到 `p_{i+1}` 前的停留」（D018 dt 契约**正确、未改**）。原 `trajectory_stats` / PNG C.speed / `test_touch_swipe_model` 统计 helper 把段 `p_{i-1}→p_i` 配给 `dt_i`（应为 `dt_{i-1}`）—— 相邻错位一格。修：`dev_tools/test_touch_swipe_mumu.py` 新增 `trajectory_segments()`（段 `p_i→p_{i+1}` 用 `dt_i`，`i=1..n-2`；排除 `p0→p1`——DOWN 无显式 dt、`DEFAULT_DELAY` 不并入；排除 `dt_{n-1}`——pre-UP dwell），`segment_speeds`/`avg_speed`/`max_speed` 全走它 + `segment_count`；PNG C 标题 `speed per SEGMENT (p_i->p_i+1 = dist / dt_i)`、B 标题 `WAIT after MOVE`。**未动** `total_dt_ms` / `actual_path_length_px` / `min·max_dt_ms` / JSON schema / A1·A2 / 生产模型 / executor / `Control`。手算样本 `[(0,0,0),(20,0,5),(25,0,20),(35,0,10)]` → 2 段 `[1.0, 0.5]` px/ms。`test_touch_swipe_mumu_tool` 31→37、`test_touch_swipe_model` 50→56，回归 **888→897** | `docs/AI_CONTEXT.md` §4.42「segment speed 与 dt 对齐修正」；`docs/DECISIONS.md` D018 第二阶段追加 |
| — | KekkaiUtilize K3：标准 PASS minitouch 路径的 swipe settle 固定 `time.sleep(2)` → FrameWait changed+stable | 已完成（2026-09-07，仅 Kekkai 业务层，`TouchSwipeModel` / `frame_wait` / `FrameStateDetector` 未改）：`_perform_search_swipe()` minitouch 分支不再固定等 2 秒猜停稳——swipe **前**明确重截 `baseline`（不复用可能被候选卡详情页污染的 `self.device.image`）→ `swipe_trajectory` + `click_record_clear` → `wait_for_changed_and_stable(baseline, self.device.screenshot, roi=SWIPE_WAIT_ROI, changed_threshold=…, stable_threshold=…, stable_frames=…, timeout=…)` → 返回 `wait.success`（签名 `-> None` 改 `-> bool`）。**`module/base/frame_wait.py` 由此得到首个且唯一生产消费者**（此前 D012/§4.18 记「零生产消费者」）。**失败即 ABORT**：`changed` 迟迟不出现 / `changed` 后不 `stable` / `timeout` → `False` → `_run_search_pass` `return PassResult.ABORT`（外层 bounded recovery）；**`no change ≠ BOTTOM`**（D017 唯一 BOTTOM marker 仍是 `I_U_EMPTY_CARD`），不新增「滑不动 → PASS_MISS」隐式到底；方法内一次 swipe → 一次 FrameWait，无内部 retry。**ROI = card-column**（`SWIPE_WAIT_ROI=(525,155,620,610)`，取 `I_U_*_6` / `I_U_EMPTY_CARD` 的 `roi_back` 包络，排除详情栏 / tab / 底部 UI / OCR 数字）；**4 个判定阈值 + `poll_interval` 均 provisional 类常量**（`changed 0.10` / `stable 0.02` / `stable_frames 3` / `timeout 3.0s`；`SWIPE_WAIT_POLL_INTERVAL=0.15` **显式传**、覆盖 FrameWait 全局默认 0.0——检测采样 pacing，让连续 `stable_frames` 帧跨越更长真实时间窗、避免惯性微滚被误判 stable，叠在 `device.screenshot` 的 `_screenshot_interval` 之上，**非恢复固定 `sleep(2)`**；全部注释标「需 Level C 调整」，用 `swipe wait` 日志 `elapsed·diff` 分布回填）。**不变**：K1（`TouchSwipeModel().generate` / `swipe_trajectory` / `control_name`）、K2（随机 212~265px）、`click_record_clear`、BehaviorTrace（一次 swipe = 一个 ACTION，FrameWait 轮询不额外产生 ACTION）、D017 契约、FrameWait 全局默认、**怠惰 + 非 minitouch 回退（`perform_swipe_action`：固定 416 + `time.sleep(2)`，不接 FrameWait）**。新增 `PerformSearchSwipeK3Test` 14 用例（含显式 `poll_interval` 锁传参）+ `RunSearchPassTest` +1 失败→ABORT 集成用例 + `UtilizeStructureTest` 的「零 FrameWait 消费者」用例改成正向锁「唯一消费者 = `_perform_search_swipe`」 + 改 K1/K2 若干用例（device 加 `screenshot` mock、FrameWait patch 成功、settle 断言 `sleep`→`wait`）。`test_kekkai_utilize_state` 69→84，回归 **984→999**。**K3 真机验收 = Level C**：`SWIPE_WAIT_ROI` 是否恰框滚动主体 / 4 个判定阈值 + `poll_interval` 是否合适（看 `changed·stable·elapsed·diff` 日志分布）/ `timeout=3.0` 是否够覆盖惯性拖尾 / 正常滚动稳定判 `changed&stable`、真卡死稳定进 ABORT / 是否比旧固定 2 秒更快 | `docs/AI_CONTEXT.md` §4.41「K3」；`docs/DECISIONS.md` D017 K3 补记 + D018 K3 补记 + D012 K3 补记 |
| — | KekkaiUtilize K2：标准 PASS minitouch 路径 —— commanded 位移 416px → 随机较短位移（`SWIPE_DISTANCE_RANGE`，Level C 分档）+ 轻微整体斜度（`SWIPE_LATERAL_OFFSET_RANGE`） | **静态实现完成，首轮 Level C 发现距离过大、已回调，需二次 Level C**。`_perform_search_swipe()` minitouch 分支 `distance = random_int(*self.SWIPE_DISTANCE_RANGE)`，`end = (start_x, start_y - distance)`，每次调用独立采样。`KEKKAI_ROW_PITCH_PX=106`（y≈220/326/432/538，一屏约 4 格；旧 416px≈3.9 格接近整屏 + 惯性 → 严重跨项漏卡）。**分档**：首档 `(212, 265)`（≈2.0~2.5 row_pitch）—— 2026-09-07 首次 Level C **实测「一次内容仍滚 >4 格」**，确证 commanded finger distance ≠ actual content scroll（列表惯性放大），判定过大；**当前 `(140, 180)`**（≈1.3~1.7 row_pitch 手指位移，整体降约 1/3，预计实际仍滚 2~3 格，目标相邻两屏保留 1~2 格重叠）。**轻微整体斜度（2026-09-07）**：`lateral_offset = random_int(*SWIPE_LATERAL_OFFSET_RANGE)`（`(-12,12)`，provisional），`end_x = clamp(start_x + lateral_offset, *SWIPE_START_X_RANGE)`，`end = (end_x, start_y - distance)`——只作用于最终 `end_x`、不给 MOVE 点加噪声；旧「`end_x==start_x` 接近竖直」变成有的轻微左/右斜、有的竖直，整体仍明确向上；`end_x` 夹回起点安全区（**不扩大 `start_x` 范围**），贴边界时偏移被裁掉退化竖直。**只改「滑多远 / 往哪斜」**：起点安全区 `(340,600)×(500,565)`（`start_x` 范围不变）/ `TouchSwipeModel().generate(start,end)` 零参、只收 start/end、仍只调一次 / 曲率 / 时间模型 / tail / `swipe_trajectory` 一次 / `control_name='KEKKAI_UTILIZE_SWIPE'` / `click_record_clear` 全不变；settle（K3 FrameWait，含 `SWIPE_WAIT_*` / `poll_interval`）全不动；**怠惰 + 非 minitouch 回退（`perform_swipe_action`→`swipe_adb`）仍固定 `SWIPE_DISTANCE=416` 纯竖直**；D017 契约不变。**契约**：K2 只减 commanded 位移建重叠，不保证 actual scroll 格数与 commanded row 估计一致；`finger≠content` 的测量是 K4-1（`actual_scroll_dy`，2026-09-07 已实施 Level A/B，见「已完成」表 K4 行）；`scroll_gain` 从未实现、也非必需。`PerformSearchSwipeK2Test` 距离用例 + **新增 2 个斜度用例**（`SWIPE_LATERAL_OFFSET_RANGE==(-12,12)` 有正有负 / `end_x=clamp(...)` `end_y` 不受影响 / 左右斜·竖直都能出现 / 贴边界退化竖直 / 大样本 `end_x` 恒合法且三向都出现 / 源码断言）+ K1/K3 用例 `random_int` side_effect 补第 4 值，`test_kekkai_utilize_state` `84 → 86`，回归 **1025 → 1027**。**二次 Level C 验收（`(140,180)` + 斜度）**：① 实际滚几格 / ② 是否仍 >4 格 / ③ 能否稳定保留 1~2 格重叠 / ④ 是否滑动太小列表几乎不动 / ⑤⑥ K3 changed·stable 仍正常 / ⑦ OASX trajectory 里 commanded distance 落在 140~180、`end_x` 呈现轻微左右斜且不越安全区。若距离仍实际 >3 格 → 下一档 `(110, 150)`（单变量分阶段，本轮不直接跳） | `docs/AI_CONTEXT.md` §4.41「K2」；`docs/DECISIONS.md` D018 K2 补记 + D017 |
| — | KekkaiUtilize K4-1（Selected Anchor → actual_scroll_dy）+ K4-2（Frame-to-Frame Projection Dedup） | **K4-1 与 K4-2 均已实施 Level A/B（2026-09-07，`docs/DECISIONS.md` D017 K4 补记）；K4 overall Level C ⏳ pending**（好友卡样本不足，不伪造真机）。`new-region-only optimization`（按 dy 只裁新进入视觉区域跑识别）⏸ NOT IMPLEMENTED / optional。用 `I_IS_SELECTED`（用户标注，`utilize_is_selected.png` 21×59 = 选中态发光竖线；`roi_back=(602,168,30,442)` 动态搜索条；threshold 0.8）做动态锚点：`actual_scroll_dy = anchor_before.center_y − anchor_after.center_y`（`center_y=y+h/2`，before = swipe 前重截帧、after = K3 稳定后帧）——**不用** commanded 位移（`SWIPE_DISTANCE_RANGE`）；把上一稳定屏 `find_everyone` 候选按 `y−dy` 投影到当前屏，与当前屏 `find_everyone` 结果做「同 `image.name` + bbox IoU>0」max-IoU 贪心一对一去重，只把 `new_detections` 交给 D017 处理。**测量不可靠（首屏无选中 / NMS 后 >1 / `dy<=0` / `dy>=roi_back 高度` / anchor 被边界裁切）→ 完整扫描（不漏卡）**；`unavailable ≠ ABORT ≠ BOTTOM ≠ PASS_MISS`；**K3 失败仍 ABORT 且不进 K4**；**BOTTOM 仍只认 `I_U_EMPTY_CARD`**；`clicked_any`/`FINAL_USE_LAST` 不变；**无持久历史 / 全局 database**；类型不一致不去重、不重新 OCR、不加 ±px magic tolerance。K1/K2/K3 一行不改；`select_realm_on_1~4` 死资产不删。两个 task-local 纯组件 `selected_anchor.py`（`detect_selected_anchor` / `SelectedAnchorResult`）+ `frame_projection.py`（`actual_scroll_dy_px` / `project_bbox` / `bbox_iou` / `dedup_by_projection`），都不 swipe / 不调 FrameWait / 不改 PASS。`K4_ENABLED` 是 kill switch。集成在 `_run_search_pass` 循环（loop 顶测 after + 算 dedup_dy、`find_everyone` 后过滤成 `scan_cards`、loop 底存 `prev_detections` + 重截测 before）。新增 `tests/test_kekkai_k4_selected_anchor.py`（11）+ `tests/test_kekkai_k4_projection.py`（21）+ `K4IntegrationTest`（8），`test_kekkai_utilize_state` `86→94`，回归 **1027→1067**。**K4-2 是业务层去重**（跑完整 `find_everyone` 后过滤 `new_detections`）；`new-region-only optimization`（按 dy 只裁新进入视觉区域跑识别——性能优化）⏸ **NOT IMPLEMENTED / optional**，当前无必要，不创建高优先级任务。**Level C 观察**：selected glow 连续稳定 / 0.8 无假阳性 / dy 正确 / 投影 overlap / 新卡不误去重 / unavailable 完整扫描 / 重叠 1~2 格不重复点旧卡 / 呼吸动画影响 / **center-based visible filtering（投影中心出界即忽略）是否只造成重复、不漏卡** / `K4_LIST_VISIBLE_Y` + `threshold=0.8` + multi-match 收敛（当前「>1 → unavailable」）是否需实测调整 | `docs/AI_CONTEXT.md` §4.47；`docs/DECISIONS.md` D017 K4 补记；上一轮静态审查报告 |
| — | 全仓 Swipe Consumer 审查 + TouchSwipeModel 全局迁移（普通滑动 / 列表滚动） | 已完成静态（2026-09-07，`docs/DECISIONS.md` **D018 全仓迁移补记**，非新 ADR）：把全仓「普通页面滑动 / 列表滚动」的输入后端从 legacy 端点滑动统一迁到 `TouchSwipeModel` 轨迹——**只换「怎么滑」**。① 新增薄公共 helper **`BaseTask.swipe_trajectory(start, end, *, control_name='SWIPE', fallback=True)`**（`tasks/base_task.py`；minitouch 且位移 ≥10px → `TouchSwipeModel().generate` → `Control.swipe_trajectory`；非 minitouch / <10px → `fallback=True` 回退 `Control.swipe` 端点滑动，`fallback=False` 抛 `NotImplementedError`；**不** screenshot / FrameWait / sleep / retry / 加随机延迟——不含 K3）。② **`BaseTask.swipe(RuleSwipe)` 改为委托 helper** —— **42 个 `self.swipe(S_*)` 生产 consumer 透明迁移**（AreaBoss / SwitchSoul / SwitchAccount / QuickLoadout / MemoryScrolls / WeeklyPurchase / WantedQuests / Hyakkiyakou / Exploration base+script / KekkaiUtilize `S_GUILD_LOTTERY`·`S_U_END` / GeneralBattle `random_click_swipt` / Summon / Dokan / KittyShop / Secret / CollectiveMissions / DailyTrifles / GeneralBuff `S_BUFF_UP`），起终点仍 `swipe.coord()`，方向 / 距离 / `interval` / `control_name`（→ `click_record` 计数）不变。③ **GeneralBuff `exp_50`/`exp_100`** 直连 `device.swipe` → helper。**未迁**：`list_find` 翻页（绑 T5-2）/ KekkaiActivation `swipe_adb`（距离敏感，需独立 K 式）/ RyouToppa（依赖 `duration=`，D003 契约）/ KekkaiUtilize `perform_swipe_action`（K-series 保留回退）。**保留 legacy**：`Control.drag` + 各后端 drag / Chess `press_and_drag` / AbyssShadows 摇杆。数字：审查到生产 swipe/drag consumer ≈51、ordinary swipe ≈47、本轮迁 44、原已 trajectory 1、**保留 ordinary legacy = 4**（逐项有原因）、drag 1 + press-and-drag 1 + 摇杆 1。新增 `tests/test_base_task_swipe_trajectory.py`（16），既有测试零改动（task-state 测试 config 是 MagicMock，`!= 'minitouch'` 自然走端点回退），回归 **1067→1083**。**迁移 consumer 真机验收（除 KekkaiUtilize 外全部 Level C PENDING）** 见 Level C 区 | `docs/AI_CONTEXT.md` §4.48；`docs/DECISIONS.md` D018 全仓迁移补记；`docs/DEVELOP_LOG.md` 2026-09-07 同名条 |
| — | 普通滑动端点空间分布 v2（`SwipeEndpointSampler`，D022） | 已完成 Level A/B（2026-09-08，`docs/AI_CONTEXT.md` §4.61 / `docs/DECISIONS.md` **D022**）：OASX 行为统计显示同类 swipe 起点 / 终点过度固定、轨迹高度重合——根因是 swipe 资产 `roi_front` / `roi_back` 太小（~21×21 甚至 4×4 / 2×4）+ `random_center_point_in_roi` 的 Bates 均值有效 σ 只有 3~6px。新增端点采样层 `module/atom/swipe_endpoint.py`（与 `ClickSampler` 同构）：`sample_swipe_endpoints(roi_front, roi_back)` —— 起终点**各自独立**采样（不共享平移量）、参照点 = 各 ROI 整数范围几何中点、游走尺度按业务基准距离比例推出并夹 `[6,40]px`、主成分 0.85 窄高斯 + 0.15 宽尾、夹到 `[preferred±hard_half]∩屏幕安全边界`、越界有限次 rejection → 回退轴中心；**轴向 ROI `>=48px` 的轴 + 两端两轴都大 → 逐字保持旧 `_center_biased_int`**（`S_BATTLE_RANDOM_*` / Summon `S_RANDOM_SWIPE_*` 分布字节级不变）；**联合校验**夹角 `<=~25°` + 距离 `[0.55,1.45]×基准` + `>=10px` + 主轴符号，`joint_max_attempts=6` 用尽 → 确定性回退 `(preferred_start, preferred_end)`（有界、无 `while True`）。`RuleSwipe.sample_endpoints()` 委托它；`BaseTask.swipe` 的 `coord()` → `sample_endpoints()`（唯一改动点，~50 consumer 透明迁移）。**`RuleSwipe.coord()` / `TouchSwipeModel` 中段轨迹 / `Control` / BehaviorTrace 未改**（端点分布与中间轨迹正交、不同时改）。未迁：`list_find` 翻页 / K1~K4 自建端点 / `KekkaiActivation` `swipe_adb` / RyouToppa `duration=` / drag / press-and-drag / 摇杆。新增 `tests/test_swipe_endpoint_sampling.py`（36），`tests/test_base_task_swipe_trajectory.py` 1 用例改 patch 目标（计数不变），回归 **1321 → 1357**。**Level C（2026-09-08）：用户多轮 MuMu 真机 + OASX trajectory 统计（最新约 42 swipe）观察——起点 / 终点形成「明显但仍集中」的独立小范围分布、多条同类 trajectory 不再完全重合、主方向稳定、无反向 / 无极端 / 无飞散、无业务回退——判定 Level C PASS，`SwipeEndpointParams` 当前默认值在无新反例前冻结（reopen 条件见 D022 Level C 补记）。本轮为 status / documentation closeout，未改生产代码 / 测试** | `docs/AI_CONTEXT.md` §4.61；`docs/DECISIONS.md` D022 + Level C 补记；`docs/DEVELOP_LOG.md` 2026-09-08 D022 条 + Level C 收口条 |
| — | Exploration Boss 直退 / Exit Contract / Chapter FrameWait / solo Fatigue（§4.58 / D021） | 已完成 Level A/B（2026-09-08，`docs/AI_CONTEXT.md` §4.58 / `docs/DECISIONS.md` **D021**）：`run_on_battle` 消费 `run_general_battle` bool，`fire_monster_type == 'boss' and battle_won` → `_complete_boss_business_cycle`：连续 fresh `page_exp_main` 确认 map ready → task-local `EXIT_READY / EXIT_CONFIRM / TRANSITION_UNKNOWN / EXIT_SUCCESS`（复用 `quit_exp_main` / `run_on_exp_exit`，unknown 只等、旧 marker 消失不算成功，15s bounded timeout raise `GameStuckError`）→ solo `try_fatigue_break(deadline=start_time+limit_time)` → fresh revalidate。Boss path + 退出后首次 entrance dispatch bypass 地图 treasure（`_skip_treasure_once_at_entrance`）。`open_expect_level` 实际 swipe 分支 `sleep(1) → wait_for_changed_and_stable`（ROI `(1065,203,1189,555)` + 6 个 provisional 参数，FrameWait `success/stable` ≠ chapter found，`swipeCount>=25` 不变）。`fire(button)` / GeneralBattle / Navigation 未改。`tests/test_exploration_state.py` `33 → 48`，回归 **1298 → 1313** | `docs/AI_CONTEXT.md` §4.58；`docs/DECISIONS.md` D021；`docs/DEVELOP_LOG.md` 2026-09-08 同名条 |
| — | Exploration Boss Reward Flow 收敛：撤销「Boss 后不领地图宝箱直接退出」错误需求，恢复原生链 | 已完成（2026-09-08，`docs/AI_CONTEXT.md` §4.60 / `docs/DECISIONS.md` **D021 标 Superseded + D021 corrected 补记**）：用户重新确认真实需求 = 原项目一直以来的「检查地图宝箱 → 有则正常领取 → 无则继续 → **不领取小纸人奖励** → 沿用原 Exploration 原生退出 flow → 回外层页面 → 下一轮」。§4.58 + §4.59 基于错误理解（「Boss 后完全不领地图宝箱直接退出」）新增的整套 Boss 专用退出结构本轮**撤销**：删 `ExplorationExitState` / `BOSS_EXIT_*` / `_resolved_page` / `_wait_for_stable_page` / `_set_exit_state` / `_is_boss_exit_success_state` / `_wait_for_boss_exit_success_state` / `_boss_exit_probe` / `_run_boss_exit_transaction` / `_complete_boss_business_cycle` / `_skip_treasure_once_at_entrance`（`run_on_exp_entrance` / `run_on_exp` 两处 `collect_treasure_box()` 恢复无条件），`run_on_battle` 恢复 HEAD 原生 `run_general_battle(exit_matcher=page_exp_main)` + `_match_end.refresh()`。**原生 reward / exit 链**：`page_exp_main → run_on_exp_main → collect_reward()` = `collect_treasure_box()`（有小 / 大宝箱 → `ui_click` 领取；无 → `return False`）`or collect_paper_man_reward()`（`fire_monster_type=='boss' and not collect_paper_reward` → `"Not collect paper doll reward"` + 原生 `quit_exp_main()`——**原项目本就有**）→ `page_exp_exit` → `I_E_EXIT_CONFIRM` → page-dispatch 接管 `page_exp_entrance` / `page_exploration`。**自动退出场景**（无宝箱时游戏自动结束探索）：page-dispatch 天然接管；`quit_exp_main` / `run_on_exp_exit` 的 `appear_then_click(confirm_delay=)` 二次确认目标 marker、消失则不点，天然避免 stale click——**未加新 current-page guard、未造 Auto Exit FSM**。**保留（独立成立，未动）**：§4.59 Rotation Contract（`switch_rotate()` `AutoRotate.no` = observe only）、§4.58 Chapter FrameWait（6 provisional 参数 PARTIAL / Level C）、solo Fatigue 能力——Safe Point 重挂到新 `_maybe_boss_cycle_fatigue()`（`run_on_exp_entrance` / `run_on_exp` 顶部，仅 ALONE 且刚完成 Boss 循环、已在外层稳定页时；`deadline=start_time+limit_time`；消费 `fire_monster_type` + early return → 下一轮 fresh dispatch；**不在 `page_exp_main` 触发**）。`fire(button)` / GeneralBattle / RealmRaid / GeneralInvite / RyouToppa / Orochi / EvoZone / Kekkai / Navigation 未改。**未用 `git checkout` / `restore` / `reset` 回滚——语义级手工收敛。** `tests/test_exploration_state.py` `66 → 56`（−10 全为删掉的废弃 contract 用例——`BossDirectExitContractTest` 12 + `BossExitSuccessStatePredicateTest` 6 + `RunOnExpTreasureSkipTest` 3 + `ExplorationFatigueSafePointTest` 依赖 `_complete_boss_business_cycle` 的 3；新增 `BossRewardFlowNativeTest` 8 + `BossCycleFatigueTest` 7；`GeneralBattleHandoffTest::test_run_on_battle_*` 改锁原生 handoff）。回归 **1331 → 1321**，0 regression。**Level C re-test 待窗口** 见 P0「Exploration 主流程收口」节 | `docs/AI_CONTEXT.md` §4.60；`docs/DECISIONS.md` D021 corrected 补记；`docs/DEVELOP_LOG.md` 2026-09-08 同名条 |
| — | Exploration Level C hotfix：Boss 直退 `EXIT_SUCCESS` 多态 positive + 轮换模式归用户所有 | 已完成（2026-09-08，`docs/AI_CONTEXT.md` §4.59 / `docs/DECISIONS.md` **D021 补记**，非新 ADR；**Exit timeout 部分已被 §4.60 撤销，Rotation Contract 部分保留**）：§4.58 首次真机 Level C 两个 correctness bug。**(B) Exit timeout**——`_run_boss_exit_transaction` 用 `detect_page_in(page_exp_main, page_exp_exit, page_exp_entrance, include_global=False)` +「连续两帧 `page_exp_entrance`」为唯一 `EXIT_SUCCESS`；真机点 `I_E_EXIT_CONFIRM` 后停在**探索模式列表 `page_exploration`**（`I_CHECK_EXPLORATION`，`category="global"`，被 `include_global=False` 排除），`_detect_pages` 每帧 `None`（日志 11 秒零 `[UI]` 行），事务全盲 → 15s `GameStuckError`。**修**：新增 task-local 只读 predicate `_is_boss_exit_success_state()` = `match_page_once(_resolved_page(page_exp_entrance)) or match_page_once(_resolved_page(page_exploration))`（两者都是 `exec_exp_page` 有 handler / `check_exit` 认可的合法外层页，**正向 marker 命中、非 disappearance**），事务成功分支改用它（连续 `BOSS_EXIT_STABLE_FRAMES=2` 帧）；`detect_page_in` 缩到只判 `page_exp_main` / `page_exp_exit` 驱动 READY / CONFIRM；新增 `_boss_exit_probe()`（首次进入 unknown + 超时前各打一条 `page_exp_entrance=.. page_exploration=.. page_exp_main=.. page_exp_exit=.. rotate_on/off` marker 快照）；`_complete_boss_business_cycle` 的 fatigue 后 revalidate 改用 `_wait_for_boss_exit_success_state()`；`run_on_exp()` 的 `ALONE` 分支也读 / 清 `_skip_treasure_once_at_entrance`（Boss 直退落探索列表也 0 treasure）。**timeout `BOSS_EXIT_TRANSACTION_TIMEOUT=15.0` / `BOSS_EXIT_READY_TIMEOUT=10.0` / `BOSS_EXIT_STABLE_FRAMES=2` 一字未动**（是 detector 错、不是等待不够）。**(A) 轮换模式被脚本取消**——`BaseExploration.switch_rotate()` 的 `case AutoRotate.no:`（config 默认）`appear_then_click(I_E_AUTO_ROTATE_ON)` 把用户手工开着的「自动轮换」点关（为让地图布局回旧样子）。**修**：`AutoRotate.no` 分支改为 `pass`（observe only，脚本绝不点 `I_E_AUTO_ROTATE_ON` / `I_E_AUTO_ROTATE_OFF` 取消 / 重置用户轮换）；`page_exp_main` 的 `any_of(I_E_SETTINGS_BUTTON, I_E_AUTO_ROTATE_ON, I_E_AUTO_ROTATE_OFF)` 本就覆盖轮换开 / 关两布局。`AutoRotate.yes`（用户 opt-in「脚本管理候补」）不变——仍 `click(C_CLICK_SETTINGS) → fill_shikigami + 开轮换`，只做「开」方向。`base.py` 移除已 dead 的 `REACTION_FAST` import。GeneralBattle / `_exit_matcher` / `fire(button)` / Chapter FrameWait / Fatigue 顺序契约 / RealmRaid / GeneralInvite / RyouToppa / Orochi / EvoZone / Kekkai / Navigation 均未改。`tests/test_exploration_state.py` `48 → 66`（`BossDirectExitContractTest` 多态成功 + probe + timeout-not-widened；`ExplorationFatigueSafePointTest` `_wait_for_boss_exit_success_state` + exit-timeout-0-fatigue；新增 `RotationOwnershipTest` 5 / `BossExitSuccessStatePredicateTest` 6 / `RunOnExpTreasureSkipTest` 3）+ `tests/test_reaction_timing_batch1.py` 同步（`switch_rotate` 不再有轮换 toggle）。回归 **1313 → 1331**。**Level C re-test 待窗口** 见 P0「Exploration 主流程收口」节 | `docs/AI_CONTEXT.md` §4.59；`docs/DECISIONS.md` D021 补记；`docs/DEVELOP_LOG.md` 2026-09-08 同名条 |
| — | RealmRaid Level C hotfix：`_fire_again()` 失败结果页误判为已进入新战斗 + OASX 个人突破过时文案同步 | 已完成（2026-09-08，`docs/AI_CONTEXT.md` §4.56 Level C hotfix / `docs/DECISIONS.md` **D001 补记（FIRE 分节 增补：Battle Lifecycle Detector ≠ New Battle Entry Detector）**，非新 ADR）：**真机 Level C defect** —— 退四首战主动退出、`Battle result: Lose` 之后 `_fire_again()` 没有 `Fire again: attempt` / reaction / `I_FIRE_AGAIN` 点击日志就直接 `Fire again: entered battle`，caller 又 `run_general_battle()` → 立即重新识别同一个 `page_battle_result` → 假退四循环。**根因**：`_fire_again()`（3 处）+ `_wait_again_entered_battle()`（1 处）用 `GeneralBattle.is_in_battle(False)` 作 positive battle-entry 判据；`is_in_battle()` 是宽生命周期 detector（`I_BATTLE_INFO | I_PREPARE_HIGHLIGHT | I_FRIENDS | I_WIN | I_DE_WIN | I_FALSE | I_REWARD | I_REWARD_GOLD`），失败结算页 `I_FALSE` 命中即恒 True。**修复**（只改 RealmRaid consumer，`GeneralBattle.is_in_battle()` / `is_in_prepare()` / `is_in_real_battle()` 本体及其它 consumer 全不动）：新增 RealmRaid-local 只读 helper `_is_active_battle_entry() -> bool` = `self.is_in_prepare(False) or self.is_in_real_battle(False)`（复用 GeneralBattle 已有的两个窄 detector，都不含 result / reward marker），`_fire_again` / `_wait_again_entered_battle` 的 4 处 `is_in_battle(False)` 全部换成它；三态语义不变，只是 `battle` 判据收窄。**长期规则**（D001 补记）：challenge / `fire_again` / retry 的「进入新战斗」positive 确认不得用含 result / reward marker 的宽 detector。`fire()` R-R1 的 `is_in_battle()` 用法本轮不动（目标详情页点第一次 FIRE、其前无结果页，安全-by-context；未来一致性收窄见 Level C）。`RR_AGAIN_*` 数值 / `RR_EXIT_FOUR_SURRENDERS=4` / 退四触发 / `RR_TARGET_PACING` / try/finally unlock / `failed_orders` / `when_attack_fail` / Fatigue safe point 均未改（bug 与 timeout 参数无关，不借机调参）。**OASX 同步**：`d:\oas_xy\OASX` 个人突破配置文案 SoT = 前端硬编码 Dart map `lib/translation/cn_parts/cn_realm_raid_config.dart`（非 backend schema 自动生成）；`exit_four` 旧「当进攻到左上角第一个的时候先退四次再进攻」→「打九退四（只剩最后一个可攻打目标时）」+ help 说明新触发；`order_attack` 旧「挑战顺序」/「使用过滤器，保持默认即可」→「攻打的勋章档位」+ help 说明「数字 5~0 = 勋章数量档位，先后顺序不再决定攻击顺序，目标一律按九宫格固定 1→9 选择」。**`order_attack` backend config field 保留不删**（`tasks/RealmRaid/config.py`，仍被 `order_medal` cached_property 消费——决定 `ImageGrid` 放哪些 `I_MEDAL_*` = 哪些勋章档位算「可攻打对手」；`>` 排序 vestigial），按「仍有其它语义 → 改名不删」。backend web i18n 副本 `module/config/i18n/zh-CN.json`（`.gitignore` 忽略）同步 4 key；PyQt `module/config/i18n/zh_CN.xml` / `zh_CN.qm` 未改（需 Qt `lrelease` 重编译，本环境无工具链）—— backend PyQt i18n 待同步项。`tests/test_realm_raid_state.py` `93 → 103`（`FireAgainThreeStateTest._mk` 改 mock `_is_active_battle_entry` + 新增 `ActiveBattleEntryContractTest` 10），回归 **1287 → 1297**。**Level C re-test 待窗口** 见 P0「RealmRaid 目标选择 / 退四策略改造」节 | `docs/AI_CONTEXT.md` §4.56；`docs/DECISIONS.md` D001 补记；`docs/DEVELOP_LOG.md` 2026-09-08 同名条 |
| — | RealmRaid 主循环 correctness follow-up（CONTINUE 跳过 / Fatigue 后移 / Last Target fresh revalidate / Unlock try/finally） | 已完成（2026-09-08，`docs/AI_CONTEXT.md` §4.56 correctness follow-up / `docs/DECISIONS.md` **D020 补充**，非新 ADR）：§4.56 落地后复核出 4 个 correctness 边界，本轮只修这 4 个。**A CONTINUE 跳过刚失败目标**：新增纯局部 `failed_orders: set`（不涂帧 / 不改 asset / 不当 broken），普通失败 + `when_attack_fail == CONTINUE` → `failed_orders.add(index)`，选目标 `available = targets - failed_orders`，`available` 空才 refresh；**每处 `check_refresh()` 成功都 `failed_orders.clear()`**（唯一清空点）。**B Fatigue 后移**：删普通分支 battle 后的 `_realm_raid_cycle_safe_break()`，改在共享尾部每个结果流的 recovery 完成之后（REFRESH = `check_refresh()` 成功后 / CONTINUE = `failed_orders.add` 后 / EXIT = 不触发直接 break / 退四中断·最终失败 = `check_refresh()` 成功后 / 胜利 = 尾部末尾）；`_realm_raid_cycle_safe_break()` 前先 `wait_until_appear(I_BACK_RED, wait_time=RR_CYCLE_STABLE_TIMEOUT=10)` 确认回稳定九宫格。**C 退四前 fresh 重确认**：新只读 helper `_is_only_remaining_target(order)`；退四块入口 `screenshot` + `_is_only_remaining_target(index)`（在 `ensure_lock(False)` 前）不满足 → `continue`；`_enter_target(order, require_only_remaining=True)` 退四专用、pacing 前后收紧成「仍是唯一可攻打目标」；`only_last` 仍用 raw `len(targets)`（不用 skip 过滤后的 `available`）。**D 退四解锁 try/finally**：`ensure_lock(False)` → `try:` (battles) `finally: ensure_lock(lock_default)`；`finally` 只恢复锁、无 `except`、不吞原异常；`lock_default` 来自 config 不写死。`fire()` / `_fire_again()` / `RR_FIRE_*` / `RR_AGAIN_*` / `RR_TARGET_PACING` / `RR_EXIT_FOUR_SURRENDERS` / `order_medal` / GeneralBattle / GeneralInvite / RyouToppa / Orochi / EvoZone 均未改。`tests/test_realm_raid_state.py` `63 → 93`（`CorrectnessFollowupSourceTest` 11 + `RunLoopBehaviorTest` 19 `_RunHarness` 驱动 `run()` + 改 3 处既有），回归 **1257 → 1287**。**Level C 验收待窗口** 见 P0「RealmRaid 目标选择 / 退四策略改造」节 | `docs/AI_CONTEXT.md` §4.56；`docs/DECISIONS.md` D020 补充；`docs/DEVELOP_LOG.md` 2026-09-08 同名条 |
| — | RealmRaid 九宫格固定 1→9 / 退四主流程改造 + Fatigue 安全节点 | 已完成（2026-09-08，正式业务策略变更 Level A/B，`docs/AI_CONTEXT.md` §4.56 / `docs/DECISIONS.md` **D020** + **D001 补记 FIRE 分节 增补（`_fire_again`）**）：`docs/ROADMAP.md`「RealmRaid 目标选择 / 退四策略改造」8 条全部落地，唯一生产文件 `tasks/RealmRaid/script_task.py`。三个 AskUserQuestion 收口——退四触发「完全替换为『只剩最后 1 个可攻打目标』」/ 普通失败「保留配置，仍按 `when_attack_fail` 走」/ Fatigue「本轮就接入」。**A 固定 1→9**：新增 `_grid_targets()`（`order_medal.find_everyone()` 当过滤器，`run()` 取 `index = min(targets)`），**不再按勋章数 / `order_attack` 排序**（勋章值只留给呱太判定）；`find_one()` 重写成薄包装。**B 失败格**：`_broken_orders()` 复用 `false_roi` / `false_image`、**所有模式都扫**；`_grid_targets` 在 `image.copy()` 上涂黑（修 R-R9 原地污染）。**C 目标 pacing `RR_TARGET_PACING = (1.0, 2.5)`**（module 常量 PROVISIONAL，与 `REACTION_FIRE` 是不同 timing owner，旧「1~3 秒」作废）：`_enter_target()` 里唯一一次 `random_delay(*RR_TARGET_PACING)` → `sleep` → fresh 二次确认 → `click(C_PARTITION[order-1])`。**D 普通失败**：落 `run()` 尾部 `when_attack_fail`（默认 REFRESH）；`_fire_again()` 只在退四内部。**E 退四触发**：`only_last = len(targets) == 1`（替换旧 `index == 1`）。**F 退四前解锁**：`ensure_lock(False)` → … → `ensure_lock(lock_default)` 恢复。**G `fire_again()` → `_fire_again()` 三态 bounded 重写**（`RR_AGAIN_MAX_TRIES=4` / `RR_AGAIN_TIMEOUT=10` / `RR_AGAIN_POST_CLICK_TIMEOUT=3`，每 attempt 独立 `REACTION_FIRE` + fresh 二次确认 + `_wait_again_entered_battle()` 三态 + 可达 `return False`）。**退四次数**：`RR_EXIT_FOUR_SURRENDERS = 4`（旧 `run()` 硬编码，源码可证）。**H 退四最终失败**：`check_refresh()`（不再点「再次挑战」）。**Fatigue**：`begin_fatigue_task('RealmRaid')` + `_realm_raid_cycle_safe_break()`（`deadline=None`）。`fire()` R-R1 / `RR_FIRE_*` / `ensure_lock` / `check_refresh` / `check_ticket` / `C_PARTITION_n` 坐标·ROI / `order_medal`·`I_MEDAL_*` 资产 / `REACTION_FIRE` 数值 / GeneralBattle / GeneralInvite / RyouToppa / Orochi / EvoZone 均未改。`tests/test_realm_raid_state.py` 重写（`FireAgainThreeStateTest` 11 / `FindOneFixedOrderTest` 7 / `MainFlowRefactorTest` 12 等，文件共 63）+ `tests/test_reaction_timing_batch1.py` 一行改，回归 **1235 → 1257**。**Level C 验收待窗口** 见 P0「RealmRaid 目标选择 / 退四策略改造」节 | `docs/AI_CONTEXT.md` §4.56；`docs/DECISIONS.md` D020 + D001 补记；`docs/DEVELOP_LOG.md` 2026-09-08 同名条 |
| — | `GeneralInvite.click_fire()` Battle Entry post-click 状态机 / bounded FSM 收口 | 已完成（2026-09-08，`docs/AI_CONTEXT.md` §4.55 / `docs/DECISIONS.md` **D001 补记 FIRE 分节 增补（GeneralInvite battle-entry FSM）**，非新 ADR）：**§4.54 只补 reaction，本轮补 post-click state contract（两件事分开记）**。修 `click_fire()` 的两个结构性问题——① `while 1: if not is_in_room(False): break` 使**「旧 room 状态消失」隐含等价 success**（也可能房间解散 / 队长离开 / loading / overlay / marker 临时失败）；② `click_fire()` 自身 `while 1` **无界**（`run_invite` 的 `timer_wait` 不能打断已进入的同步 while）。**placement 选方案 A**：READ ONLY inventory 证实 `click_fire()` 的 11 个 consumer MRO 全带 `GeneralBattle`（Exploration 经 `BaseExploration`），`general_battle.py` 不 import `general_invite.py`（无循环依赖），`MysteryShop`（唯一 `GeneralInvite`-without-`GeneralBattle`）不调 `click_fire`。新增只读 helper `_battle_entry_positive()`（优先 `getattr(self,'is_in_battle',None)` → `is_in_battle(False)`，否则兜底 `appear(I_BATTLE_INFO) or appear(I_PREPARE_HIGHLIGHT)` —— `is_in_battle` 严格子集，静态复核后收尾轮已从 fallback 移除 `I_EXIT`）/ `_room_entry_failed()`（`I_MATCHING` / `GameUiAssets.I_CHECK_MAIN` / `I_CHECK_EXPLORATION`，复用既有 marker）/ `_classify_room_entry_state()`（四态：BATTLE > ROOM_FAILED > RETRYABLE_ROOM > TRANSITION_UNKNOWN）/ `_wait_room_entry_state()`（有界 `Timer` 轮询、不点坐标，'battle'/'room_failed'/'retryable'/'timeout'）。**`click_fire() -> str`**（`'battle'` / `'room_failed'` / `'timeout'`）：`Timer(GI_FIRE_TIMEOUT=15)` + `for attempt in range(1, GI_FIRE_MAX_TRIES=4 + 1)`，**无 `while 1`**；「旧 room 状态消失」单独发生 = UNKNOWN、有界 polling、不点。常量 `GI_FIRE_MAX_TRIES=4` / `GI_FIRE_TIMEOUT=15` / `GI_FIRE_POST_CLICK_TIMEOUT=4` 均 **PROVISIONAL / Level C**。**caller guard**：`run_invite` 只在 `click_fire() == 'battle'` 才 `return True`（其它 → `return False`，各 caller 已有「邀请失败退出」/ `raise InviteFailedException` 承接，不误交接 `run_general_battle`）；4 个直接 caller（ExperienceYoukai / GoldYoukai / Hunt / Tako）加 `if self.click_fire() == 'battle':` guard。`REACTION_FIRE`=(0.4,0.8) / 每 attempt 独立采样 / fresh reconfirm / no stale click / 无 timing stack / 不接 Fatigue / `run_invite` invite cadence / member·passive path / Orochi·EvoZone `_fire_*_alone` / RealmRaid / RyouToppa / GeneralBattle FSM 均未改。`tests/test_general_invite_challenge_reaction.py` `23 → 41`（加 `_FakeTimer` patch `gi.Timer`），回归 **1212 → 1230**。**静态复核后最小收尾（同 2026-09-08，非新 ADR）**：READ ONLY 复核确认 fallback 的 `I_EXIT` 不属 `is_in_battle()` marker 集 + `GI_FIRE_TIMEOUT` 实为 soft admission budget → ① `_battle_entry_positive()` fallback 移除 `I_EXIT`（收窄为 `I_BATTLE_INFO or I_PREPARE_HIGHLIGHT`，`is_in_battle` 严格子集）+ docstring 改正，`GeneralBattleAssets.I_EXIT` 本体及其它 consumer（`exit_battle` / `check_then_accept` / `BondlingFairyland.wait_battle` / `AbyssShadows` / `Duel`）全部保留，11 consumer 走 `is_in_battle` → **0 behavior change**；② `GI_FIRE_TIMEOUT` = SOFT_ATTEMPT_BUDGET 的文档定性（**不改代码**，保留 soft，与 RealmRaid / Orochi / EvoZone 同结构）。`tests/test_general_invite_challenge_reaction.py` `41 → 46`，回归 **1230 → 1235** | `docs/AI_CONTEXT.md` §4.55；`docs/DECISIONS.md` D001 补记 + 静态复核后最小收尾补记；`docs/DEVELOP_LOG.md` 2026-09-08 同名条 |
| — | 通用组队挑战按钮 Reaction Timing 补齐（`GeneralInvite.click_fire`） | 已完成（2026-09-08，`docs/AI_CONTEXT.md` §4.54 / `docs/DECISIONS.md` **D001 补记 FIRE 分节 增补（组队 challenge owner + Action Owner vs Passive Waiter）**，非新 ADR）：Orochi / EvoZone 上一轮只迁 `run_alone`，但组队 / 房主真正点「挑战 / 开始战斗」按钮统一走 **`GeneralInvite.click_fire()`**（`run_invite` 经 `room_check_can_fire` 门槛调用，或 ExperienceYoukai / GoldYoukai / Hunt / Tako 直接调用）。**只改 `click_fire()` 一处**：识别 `I_FIRE`（优先）/ `I_FIRE_SEA` → 每 attempt 独立 `random_delay(*REACTION_FIRE)` → `sleep` → fresh `screenshot` → 二次确认（`not is_in_room(False)` 已离开房间 → 不点、`break`；按钮消失但仍在房间 → 回循环顶；仍在 → click）。`is_in_room` 循环边界 + `run_invite` 的 `Timer` + `interval=1` throttle + minitouch dwell 不变；**不叠 `confirm_delay` / 第二套 reaction**；**不接 Fatigue**；正向战斗确认仍在下游 `run_general_battle`（未改邀请 / 房间 / 战斗状态链）。**Action Owner vs Passive Waiter** 原则：本机真实点挑战 → `REACTION_FIRE`；只等别人开战（`wait_battle` / `check_then_accept` / member）→ 无 reaction。**自动继承**（无 task 覆写）：经 `run_invite` = BondlingFairyland / EternitySea / EvoZone / Exploration / FallenSun / Orochi / OtherWorldTwilight；直接调 = ExperienceYoukai / GoldYoukai / Hunt / Tako（共 11）。RealmRaid / RyouToppa 不纳入。`REACTION_FIRE` / Orochi·EvoZone `_fire_*_alone` / GeneralBattle / Fatigue 布局 / T7 / FrameWait 均未改。新增 `tests/test_general_invite_challenge_reaction.py`（23），回归 **1189 → 1212** | `docs/AI_CONTEXT.md` §4.54；`docs/DECISIONS.md` D001 补记；`docs/DEVELOP_LOG.md` 2026-09-08 同名条 |
| — | 常驻刷本 FIRE FSM 第二批（Orochi / EvoZone 单人）+ Fatigue 安全节点第一批铺开 | 已完成（2026-09-08，`docs/AI_CONTEXT.md` §4.53 / `docs/DECISIONS.md` **D001 补记 FIRE 分节 增补 + Fatigue Safe Point 铺开补记**，非新 ADR）：把 §4.52 的三态 FIRE Contract 铺到 **Orochi `run_alone`（`I_OROCHI_FIRE`）+ EvoZone `run_alone`（`I_EVOZONE_FIRE`）**——task-local `_fire_{orochi,evozone}_alone()`（正向 `is_in_battle()` + 每 attempt 独立 `REACTION_FIRE` + fresh reconfirm + `{OROCHI,EVOZONE}_FIRE_MAX_TRIES=4` / `*_FIRE_TIMEOUT=10` / `*_FIRE_POST_CLICK_TIMEOUT=3` + 可达 `return False`）/ `_wait_*_fire_state() -> 'battle'|'retryable'|'timeout'`（不点坐标）/ `_is_*_challenge_retryable()`（纯只读 = `appear(I_*_FIRE)`）；caller `if self._fire_*_alone(): run_general_battle(...)`，FIRE False 不误交接。**Fatigue 安全节点第一批**：`FatigueManager` consumer 从「仅 RyouToppa」扩到 **Orochi / EvoZone 的 `run_alone`**——`begin_fatigue_task` 在循环前、`try_fatigue_break(safe=True, repeat_completed=True, deadline=)` 在一场战斗完整结束 / `run_general_battle` 回到稳定挑战页之后、休息后回循环顶 fresh revalidate；`run_leader` / `run_member` / `run_wild`（邀请 / 房间 / 队友同步）**不接**。**PARTIAL / 未迁**：契灵（BondlingFairyland）`I_BALL_FIRE`（连点 + `BondlingNumberMax` 资源耗尽耦合，结构特殊）、Orochi `run_wild` 的 `I_OROCHI_WILD_FIRE`（全仓无 `RuleImage` 定义、既有断链）——均入下方 P0。`REACTION_FIRE=(0.4,0.8)` / RealmRaid `fire()` / RyouToppa `attack_area` / GeneralBattle / T7 / FrameWait / TouchSwipeModel / `FatigueManager` 本体 / `begin_fatigue_task` / `try_fatigue_break` 签名均未改。新增 `tests/test_second_batch_fire_fsm.py`（27）+ 改 `test_fire_reaction_fsm.py::OtherFireNotMigratedTest`（净 0），回归 **1162 → 1189** | `docs/AI_CONTEXT.md` §4.53；`docs/DECISIONS.md` D001 补记；`docs/DEVELOP_LOG.md` 2026-09-08 同名条 |
| — | RealmRaid FIRE FSM transition / unknown 状态边界收口（三态） | 已完成（2026-09-08，同 §4.52 追加，`docs/DECISIONS.md` **D001 补记 FIRE 分节增补 + D015 补记增补**，非新 ADR）：修 R-R1 后 `_wait_fire_entered_battle()` 仍有的**过早失败**——「点 FIRE → 旧 marker 消失 → 过渡 / 加载 blank 帧（`I_FIRE`=F、`I_RR_PERSON`=F、`is_in_battle`=F）→ 稍后才出战斗页」被当成 FAILED/RETRY、且在未知态下乱点 `C_PARTITION_n`。`_wait_fire_entered_battle()` `-> bool` 改 `-> str` 三态：**`'battle'`**（`is_in_battle()`，唯一 positive success）/ **`'retryable'`**（新纯只读 helper `_is_realm_raid_retryable_state()` = `appear(I_RR_PERSON) or appear(I_FIRE) or appear(I_BACK_RED)`，明确仍在个人突破可操作页面）/ **`'timeout'`**（整个 `RR_FIRE_POST_CLICK_TIMEOUT` 内一直 transition/unknown —— **既不当 success 也不当 immediate failure**，timer 内持续等）。`fire()` 循环里 `I_FIRE` 未就绪也走同一三态轮询；**`C_PARTITION_n` 点击必须有明确 retryable-state 守卫**（`not appear(I_FIRE)` 不再是充分条件），transition/unknown 期间不点任何坐标。`REACTION_FIRE=(0.4,0.8)` / `RR_FIRE_MAX_TRIES=4` / `RR_FIRE_TIMEOUT=10` / `RR_FIRE_POST_CLICK_TIMEOUT=3` 数值未改、bounded 未破坏、未新增随机延迟、`run()` caller 未改。`tests/test_fire_reaction_fsm.py` `29→34` + `test_realm_raid_state.py` `FireCharacterizationTest` `7→8`。回归 **1156 → 1162** | `docs/AI_CONTEXT.md` §4.52；`docs/DECISIONS.md` D001 补记 + D015 补记；`docs/DEVELOP_LOG.md` 2026-09-08 同名条 |
| — | RyouToppa / RealmRaid FIRE Reaction 统一 + RealmRaid `fire()` 状态机收口（R-R1） | 已完成（2026-09-08，生产行为变化 Level A/B，`docs/AI_CONTEXT.md` §4.52 / `docs/DECISIONS.md` **D001 补记 FIRE 分节 + D015 补记**，非新 ADR）：新增公共 **`REACTION_FIRE = (0.4, 0.8)`**（`module/reaction_profile.py`，PROVISIONAL）。**RyouToppa `attack_area`**：`fire_delay = random_delay(0.2, 0.6)` → `random_delay(*REACTION_FIRE)`（仍在 `for attempt` 内、每次独立采样），成熟状态机（`RYOU_TOPPA_ACTION_RETRIES` / `_wait_for_attack_state` / `is_in_battle` / fresh screenshot / 二次 appear / 区域 `random_delay(1.0,3.0)` pacing / fatigue safe node）**全部保持**，未加 `confirm_delay`。**RealmRaid `fire()` R-R1**：`while True` + 「旧标识 `I_RR_PERSON` 消失即成功」+ 恒返回 True → 改为 **`for attempt in range(1, RR_FIRE_MAX_TRIES+1)` + `Timer(RR_FIRE_TIMEOUT)`** + **正向战斗确认 `is_in_battle(False)`**（复用 `GeneralBattle.is_in_battle`）+ 每 attempt 独立 `REACTION_FIRE` reaction → fresh screenshot → 二次 `appear(I_FIRE)`（没了不点旧坐标）→ `appear_then_click(I_FIRE, interval=0, threshold=0.8)` → `_wait_fire_entered_battle()`（有界）→ **可达 `return False`**。常量 `RR_FIRE_MAX_TRIES=4` / `RR_FIRE_TIMEOUT=10`（参考 Exploration.fire）/ `RR_FIRE_POST_CLICK_TIMEOUT=3`（参考 RyouToppa），engineering baseline 非 Level C。`I_RR_PERSON` 降级为辅助信号。**caller `run()` 未改**：两处 `if not self.fire(index): continue`（原本因 `fire()` 恒 True 而不可达）现在真正生效，不误交接 `run_general_battle`。`C_PARTITION_n` 空间行为 / T7 / `fire_again` / GeneralBattle / Orochi·EvoZone·Exploration FIRE / Fatigue **均未改**。新增 `tests/test_fire_reaction_fsm.py`（29）+ 改 `test_realm_raid_state.py`（`FireCharacterizationTest` / `LoopAndWaitBoundsTest` 锁新形态）+ `test_general_battle_timing.py`（RealmRaid fire timing 用例）。回归 **1126 → 1156** | `docs/AI_CONTEXT.md` §4.52；`docs/DECISIONS.md` D001 补记 + D015 补记；`docs/DEVELOP_LOG.md` 2026-09-08 同名条 |
| — | 第一批业务 Reaction Timing / `confirm_delay` 迁移（Batch A #1–#23，5 任务 27 调用点） | 已完成（2026-09-08，生产行为变化 Level A/B，`docs/AI_CONTEXT.md` §4.51 / `docs/DECISIONS.md` **D001 补记**，非新 ADR）：把常用业务里「识别稳定 Point Target → 立即点」的一批调用改成经 `appear_then_click(..., confirm_delay=REACTION_*)`（primitive 未改，`confirm_delay=None` 默认不变）。新增 **`module/reaction_profile.py`**（具名 `(min,max)` 秒区间常量：`FAST=(0.18,0.35)` / `NORMAL=(0.45,0.85)` / `NORMAL_HIGH=(0.60,1.00)` / `CONFIRM=(0.55,1.20)` / `NAVIGATION=(0.55,1.10)` / `DELIBERATE=(0.90,1.60)`，**全部 PROVISIONAL**）。首批 **27 consumer**（此前 = 0）：RealmRaid `ensure_lock`×4 FAST + `check_refresh` `I_FRESH` NORMAL / `I_FRESH_ENSURE` CONFIRM；Orochi `check_lock`×3 FAST + `I_FORM_TEAM`×2 NORMAL；EvoZone `check_lock`×2 FAST + `evozone_enter` kirin DELIBERATE + `I_FORM_TEAM` NORMAL；RyouToppa `_ensure_team_lock_state` + `start_ryou_toppa` 2 管理按钮 = 3 FAST；Exploration auto-rotate×2 FAST / exit dialog CONFIRM+NAV / `I_UI_BACK_YELLOW` NAV / `open_expect_level` 章节确认×4 NORMAL。按 profile：FAST 14 / NORMAL 8 / CONFIRM 2 / NAV 2 / DELIBERATE 1 / **NORMAL_HIGH 0**（允许）。`GeneralBattle.check_lock` 加 `confirm_delay=None` 可选参数并透传（非 batch 调用方逐字不变）。**排除**（0 新 reaction）：所有 `*_FIRE`（R2，须先 R-R1）/ 动态 / OCR / polling / Region / RyouToppa `I_FIRE`（已手搓 `random_delay(0.2,0.6)`）/ GeneralBattle `I_PREPARE_HIGHLIGHT`（`prepare_click_timer`）/ Settlement V3（D016）/ 瞬态战斗按钮；上一轮 #24/#25/#26 不实施。新增 `tests/test_reaction_timing_batch1.py`（34），回归 **1092 → 1126** | `docs/AI_CONTEXT.md` §4.51；`docs/DECISIONS.md` D001 补记；`docs/DEVELOP_LOG.md` 2026-09-08 同名条 |
| — | 全仓 FrameWait + confirm_delay 消费者审查；`list_find` 翻页 settle 迁 FrameWait | 已完成（2026-09-08，`docs/AI_CONTEXT.md` §4.49，非新 ADR）：对整个 Backend 时序层（`sleep` / `wait_*` / `Timer` / `random_delay` / `confirm_delay`）完整审查，按 W1~W7 分类。**FrameWait 迁移 = 1**：`BaseTask.list_find` 翻页 `sleep(random.uniform(0.8, 1.3))` → `wait_for_changed_and_stable(settle_baseline=swipe 前 `self.device.image`, self.device.screenshot, roi=`RuleList.roi_back` 换算, 模块级 `_LIST_FIND_SETTLE_*` provisional 阈值)`。**结果丢弃、不参与控制流**——settle / timeout 都回循环顶重扫，`max_swipe` 仍是唯一收敛边界，不新增 BOTTOM / ABORT（W1 结构性 settle，与 K3「失败→ABORT」不同）。`list_find` 成为 FrameWait 第 2 个生产消费者。**FrameWait Batch B（eligible、本轮不迁、Level C pending）**：SwitchSoul / QuickLoadout / WeeklyPurchase 商店列表 / GeneralBuff / WantedQuests / SwitchAccount 的 swipe→sleep→rescan（各自嵌定制循环 + marker + `interval` timer，无专属 characterization，盲迁有 FSM 风险）。**`confirm_delay` 生产 opt-in = 0（不变）**：全仓 486 `appear_then_click` + ~264 `self.click`/`device.click` 无一传 `confirm_delay`；真实语义 = delay 后重新 screenshot + 二次 appear + 重定位（不是点旧 bbox）；与 2026-09-02 专项审查（`docs/Action点击前反应时序静态审查.md`）结论一致——D001「逐点 opt-in」+ D008「不引入全局自动等待」+ 无实测区间（§15 禁止拍脑袋），首个 opt-in 等 Level C 随 RealmRaid `fire()` 改造。`tests/test_list_find.py` `22 → 27`，回归 **1083 → 1088** | `docs/AI_CONTEXT.md` §4.18 / §4.19 / §4.49；`docs/DECISIONS.md` D013 补记 + D012 补记；`docs/DEVELOP_LOG.md` 2026-09-08 同名条 |
| — | KekkaiUtilize TouchSwipe 接入 K1（标准 PASS 搜索的列表下划 ADB → minitouch trajectory） | 已完成（2026-09-04，`TouchSwipeModel` **首个生产消费者**，只换输入后端）：`_run_search_pass` 的下划入口 `perform_swipe_action()` → 新 `_perform_search_swipe()`：`control_method=='minitouch'` → `TouchSwipeModel().generate((rand_x, rand_y), (rand_x, rand_y-416))`（同一起点安全区 `(340,600)×(500,565)` + 同一 416px 主方向位移，模型第二阶段正式默认参数不覆盖）→ `self.device.swipe_trajectory(trajectory, control_name='KEKKAI_UTILIZE_SWIPE')`（经 `Control.swipe_trajectory` → minitouch executor → BehaviorTrace `ACTION`/`swipe`）→ `click_record_clear()` → `sleep(2)`；非 minitouch → 显式回退 `perform_swipe_action()`（旧 `swipe_adb` 逐字不变，不抛 `NotImplementedError`）。**怠惰模式 `_select_lazy_resource_card` 仍走 `perform_swipe_action`；全模块 `swipe_adb` 调用点仍恰 1 处。** D017 业务契约（PASS 顺序 / threshold / bottom / final fallback / lower once / OCR / group switch / lazy）逐项不变；未改滚动几何 / 结界卡 ROI / config（**FrameWait 由 K3 2026-09-07 接入，见「已完成」表 K3 行**；K1 当时 settle 仍是固定 `sleep(2)`）。`test_kekkai_utilize_state` 46→53，回归 **897→904**。**K1 真机验收 = 下午 Level C**。**前置阻断已解除（2026-09-04）**：`oas2` 从结界页 OCR「式神育成」导航进页此前直接 `TypeError: ROI必须由整数组成`（`RuleOcr` FULL 模式浮点 OCR bbox）、K1 根本到不了；已在 `RuleOcr.coord()` 边界做 floor/ceil 整数化修复（回归 904→922，见 `docs/AI_CONTEXT.md` §4.43） | `docs/AI_CONTEXT.md` §4.41「K1」/ §4.42 / §4.43；`docs/DECISIONS.md` D018（首个 consumer 事实）+ D017 + D014 注记 |
| — | KekkaiUtilize 单向分区搜索改造（取消蛇形 / PASS 序列 / 阶段化阈值） | 已完成（2026-09-03，正式业务策略变更，`docs/DECISIONS.md` **D017**）：非怠惰路径的好友结界卡搜索从「扫全列表 + global-best + `_reselect_best_card` 回选」改成「按 PASS 序列，每个 PASS 都 TOP→BOTTOM 单向扫描、命中阈值立即寄养、最后一个 PASS 兼任兜底」。**优先跨区** = 跨6★HIGH→同6★HIGH→降一档→跨5/6★LOWER→同5/6★LOWER(final)；**优先同区** = 同6★HIGH→跨6★HIGH→降一档→同5/6★LOWER(final)。第一阶段只 6★高阈值；全失败后 `lower_reward_tier` 按离散档位**降一档一次**，第二阶段 5+6★。取消蛇形 / 反向 swipe / TOP marker / `miss-as-bottom`；`I_U_EMPTY_CARD` = 真正到底，`Timer(120)`/`SEARCH_MAX_SWIPES=20`/切区超时 = SAFETY_ABORT（不当到底）。新增 `PassResult`/`SearchPass`/`lower_reward_tier` 等；删 `_select_optimal_resource_card`/`_current_select_best`/`_reselect_best_card`/`_card_rank`/`order_cards`/`order_targets`(KU)/`_reward_threshold`/`_reaches_reward_threshold` + `utilize_best_*`/`utilize_last_*`/`ap_max_num`/`jade_max_num`（KA 自带 override 不受影响）。怠惰模式、`check_utilize_add`、寄养 verify、failure recovery、swipe 后端/几何、结界卡 ROI、config **均未动**。`I_K_SEARCH`（用户 WIP asset）未接线。测试 23→46 + 23→26，回归 759→783 | `docs/AI_CONTEXT.md` §4.41；`docs/DECISIONS.md` D017 |
| — | KekkaiUtilize 蹭卡 / 好友寄养流程重新审查 | 已完成（2026-09-03，纯只读，`tasks/KekkaiUtilize/` diff = 0）：完整状态图 A（真实）/ B（用户目标）+ 16 项 Gap Matrix + `run()` 调用链 + 逐函数解析（`_current_select_best` / `_select_lazy_resource_card` / `_reselect_best_card` / `check_card_num` / `switch_friend_list` / `perform_swipe_action` / `check_utilize_add` / `run_utilize`）+ 收益判断链路 + 寄养两层 verify + Failure/Recovery + Loop/Timer/Sleep Inventory + BehaviorTrace 覆盖，专题 `docs/KekkaiUtilize蹭卡流程重新审查.md`。**结论 PARTIAL**，三大差异：① 跨区/同区顺序由 `config.select_friend_list` 决定、默认同区优先（与「跨区优先」相反）；② 同屏扫描顺序 = 屏幕 y 位置、不是星级（`find_everyone` 按 y 升序，`order_cards` 只用于 `_card_rank`）；③ 默认阈值 = 六星满值 → 实际行为 = 扫全列表 + `_reselect_best_card` 回选最优，不是「找到满足即寄养」。**GeneralBattle V3 与 KekkaiUtilize 无调用/继承关系**。**未新增测试 / 未接 FrameWait / 未改任何代码** | `docs/AI_CONTEXT.md` §4.40；`docs/KekkaiUtilize蹭卡流程重新审查.md` |
| — | 随机源一致性修复（protect / RyouToppa / Chess 拖拽） | 已收敛到公共 `module/base/utils/random.py` | `docs/AI_CONTEXT.md` §4.10；`docs/DECISIONS.md` D007 |
| — | AI 交接文档体系（`ROADMAP` / `ARCHITECTURE` / `DECISIONS` / `TESTING` + `CLAUDE.md` / `AGENTS.md`） | 已建立并收口：文档同步规则改为按「项目事实是否变化」判断（非「是否改代码」）；`CLAUDE.md` / `AGENTS.md` 经 `.gitignore` `!` 例外纳入版本控制，跨 clone 可用 | `docs/DEVELOP_LOG.md` 2026-09-01 两条；`docs/AI_CONTEXT.md` §0 / §10.1 |
| T4-1 | List Changed / Stable 独立视觉状态组件 | 组件本体 + 单测已完成：`module/atom/frame_state.py`（纯 numpy，零生产消费者），25 用例，回归 191→216。接入任务仍是 Level C（待真机） | `docs/AI_CONTEXT.md` §4.16；`docs/DECISIONS.md` D010 |
| — | BehaviorTrace 点击位置统计后端 v1 | 已完成后端：`Control.click/long_click` 的 ACTION 带 `extra.x/y`；`module/server/behavior_stats.py` + `GET /stats/{script_name}/behavior/clicks`（按需读 JSONL、无后台聚合）；20 用例，回归 216→236。**OASX「统计 → 点击分布」前端页面待下一轮实现** | `docs/AI_CONTEXT.md` §4.17；`docs/DECISIONS.md` D011 |
| — | BehaviorTrace Swipe Trajectory Backend（swipe 轨迹后台记录 + 统计查询） | **已完成后端（2026-09-07，additive extension，`docs/DECISIONS.md` D004 补记 + D011 补记）**：一次完整 swipe 轨迹的后台记录与 `/stats` 查询。**只做 Backend，OASX 前端零改动、下一轮单独做**。① 事件模型不变（仍 `TASK`/`ACTION`）——**一次 swipe = 一个 ACTION**，轨迹点整体嵌 `extra.trajectory`（`[[x,y,dt],...]`，≤~80），无 `MOVE`/`SWIPE_POINT`/`FRAME` 事件。② `Control.swipe_trajectory` 的 `extra` = `{start_x,start_y,end_x,end_y,point_count,trajectory}`——直接来自传入的这份 commanded 点列、不重 `generate`、`dt_ms` 语义不变（D018）；`Control.swipe`（legacy 端点）只带端点、**无 `trajectory` 键**（不伪造曲线）；`swipe_adb` 直连路径仍不覆盖（已知缺口）。③ `BehaviorTrace.is_recording()`（`enabled and not _broken`）读访问器，`swipe_trajectory` 仅在其为真时组装大 `extra`（关闭态不白拷贝）。④ `read_behavior_clicks(config,date,*,task,interaction_type,target)` + 同一 `/behavior/clicks` 端点扩 `swipes[]` + 四层等值 AND 过滤（`date AND task AND interaction_type∈{all,click,swipe} AND target`，`target` 对 click/swipe 同语义）+ `available_tasks`/`available_targets` 下拉 metadata；`summary` 新增 `swipe_count`（一条 swipe 只 +1）/`filtered_count`/`malformed_trajectories`，旧计数（`total`=click 点数 等）语义不变。⑤ 向后兼容：响应旧键全保留（旧 OASX 忽略新键即可）；旧 JSONL / legacy swipe 无 trajectory / 单条坏 trajectory 只降级该条、**不 500**。⑥ K4 完全未实现（无 `actual_scroll_dy`/`scroll_gain`/dedup——记的是 commanded input）；K1/K2/K3 `script_task.py` 一字未改。改 5 文件（`behavior_trace.py`/`control.py`/`behavior_stats.py`/`stats_router.py` + 3 测试文件），`test_behavior_click_stats` 24→45 / `test_control_swipe_trajectory` 9→13 / `test_behavior_trace` 14→15，回归 **999→1025**。**前端下一轮消费的 schema 见 `docs/AI_CONTEXT.md` §7「BehaviorTrace Swipe schema（前端待接）」** | `docs/AI_CONTEXT.md` §4.46 / §7；`docs/DECISIONS.md` D004 补记 + D011 补记；`docs/ARCHITECTURE.md`「点击 / 滑动统计后端」/「自定义轨迹滑动」链 |
| T4-2 | FrameState Wait Layer | 已完成基础设施 + 单测：`module/base/frame_wait.py` 的 `wait_for_changed_and_stable(baseline, frame_provider, *, 阈值, timeout, clock, ...)`——在 `FrameStateDetector` 上加「取帧 + 轮询 + 有限 timeout」，设备无关、`frame_provider` / `clock` 注入、超时返回结果不抛、成功严格 `changed and stable`，区分「从未变化 / 变化未稳定」。20 用例，回归 240→260。**2026-09-07（KekkaiUtilize K3，见「已完成」表）起首个且唯一生产消费者 = `KekkaiUtilize._perform_search_swipe`（标准 PASS + minitouch 的 swipe settle）**——阈值真机标定仍是 Level C | `docs/AI_CONTEXT.md` §4.18；`docs/DECISIONS.md` D012（+ K3 补记） |
| — | `BaseTask.list_find` 迁移前静态收口（T5-2 前半，会话内称 T4-3） | 已完成：`list_find` 真实控制流逐行审查 + 全部 8 处生产调用方 + 2 个包装入口清单 + 语义/视觉等待分类 + `tests/test_list_find.py`（22 characterization 用例，回归 260→282）。当时结论「本轮不抽 seam」（理由见 D013）。**2026-09-08：T5-2 后半 FrameWait 已直接落地（不抽独立 seam，`list_find` 循环体内替换那一行），见「全仓 FrameWait + confirm_delay 消费者审查」条 / §4.49 / D013 补记**——`list_find` 生产行为从「固定 `sleep(0.8~1.3)`」变成「FrameWait settle（结果不参与控制流）」，`tests/test_list_find.py` `22 → 27` | `docs/AI_CONTEXT.md` §4.19 / §4.49；`docs/DECISIONS.md` D013 补记 |
| — | 三案例 Verify / Wait / Retry / Recovery 架构归纳（决策节点） | 已完成：基于 `list_find` / Kekkai / RealmRaid 三个真实案例横向归纳四类职责的边界与 owner，专题文档 `docs/状态验证与重试模式归纳.md`（17 节 + 十四问速答），长期契约固化到 `docs/DECISIONS.md` D015。**结论：PARTIAL 且不是现在**——structural wait（`frame_wait`）与 semantic verify（`wait_until_*` 组合）已够、不新增抽象；bounded retry 唯一共同内核约 15 行、值得未来抽但当前零消费者不抽（等第一个真实 Level C 迁移）；recovery 永远留 Task。**纯设计归纳，无 Python 改动**，完整回归维持 710/710 | `docs/AI_CONTEXT.md` §4.35；`docs/DECISIONS.md` D015 |
| — | `appear_then_click` / `confirm_delay` / Reaction Timing 全仓专项审查 | 已完成：还原 `appear_then_click` 两条真实路径（默认「命中即点当帧」无 delay；`confirm_delay` 路径「识别 → `sleep(random_delay)` → `screenshot` fresh → 二次 `appear`（没了不点）→ 新帧 `roi_front` 上 `coord()`」）+ Fresh Frame / Relocation 检查（confirm 路径 = 模式 A，T7 采样在 fresh 之后无 stale-coord）+ 全仓盘点（486 处 `appear_then_click` / **0 处传 `confirm_delay`**）+ Action Taxonomy + Timing Ownership 分层（Micro `confirm_delay` / Throttle `interval` / State Wait `wait_until_*`·`frame_wait` / Macro `FatigueManager`）+ Fatigue/Retry/T7/GeneralBattle interaction + Point Action Candidate Tiers。专题文档 `docs/Action点击前反应时序静态审查.md`（31 节）。**结论：PARTIAL**——`confirm_delay` 已是正确的 Point Action reaction-timing 机制，但零 opt-in、不应「统一」、**不新增 `reaction_delay`**（D001 扩写）。**零生产改动、零新增测试**（`tests/test_base_task_confirm_click.py` 5 用例已充分），回归 741/741 未变 | `docs/AI_CONTEXT.md` §4.38；`docs/DECISIONS.md` D001 |
| — | GeneralBattle 新通用结算策略迁移（SETTLEMENT CONTRACT V3） | 已完成（正式生产迁移，2026-09-03，`docs/DECISIONS.md` **D016**）：废弃旧 `C_RANDOM_RD` / `C_RANDOM_RD2` + HABIT profile（不恢复、不做 alias），改为 ① 三个 Large Safe Region `C_RANDOM_DEFAULT` / `C_RANDOM_SAVE_RIGHT` / `C_RANDOM_SAVE_BOTTOM` 整 ROI 均匀（`LEGACY_UNIFORM`，不走 `appear_then_click`/`confirm_delay`）；② **Generic Result 强制两次点击序列** `_advance_generic_result`（用户实测：结果页 → #1 进奖励动画 → #2 推进动画 → 才进奖励结算；不是 retry / 不是「最多两次」；两次坐标独立采样；中间一次阻塞 sleep = 结算随机间隔；positive guard `I_WIN`/`I_DE_WIN`/`I_FALSE`，`I_BATTLE_STATE_INFO` 保守排除，不用 Task 黑名单）；③ **Reward layout-aware policy**：命中 `I_OVER_GHOST`/`I_GB_SKIN_CONFIRM` 即本帧 `CONTINUE`；否则 `I_GET_BATTLE_REWARD`/`_2`（Reward Layout Discriminator，不进 recognizer）命中 → DEFAULT，否则 `random_int(1,100)<=80` → RIGHT / 否则 BOTTOM。`is_win` / `_exit_matcher` / 2.5s missing fallback / FSM / `random_click()`（与 Settlement 分离）**均未改**；Fatigue / FrameWait / retry primitive / `confirm_delay` 不参与。private override（BondlingFairyland result / SixRealms）靠不 `super()` 天然不被穿透。**仅改 `general_battle.py`**（唯一生产文件），回归 741→759（settlement 37→54、trace-check 16→17）。**仍需 Level C MuMu 真机验收**（见下方 P1） | `docs/AI_CONTEXT.md` §4.3.1 / §4.39；`docs/DECISIONS.md` D016 |
| — | Exploration 状态机迁移前静态收口（第四案例） | 已完成：完整控制流 + State Matrix（E1~E15）+ 目标选择 / `fire()` 逐条 + 「战斗进入判定」四案例横向 + GeneralBattle 交接 + Wait/Loop/Stale Inventory + Target Relocation + Issue Register（E-1~E-9）+ 四案例对比 + D015 逐条再验证，专题文档 `docs/Exploration状态机静态收口.md`。`tests/test_exploration_state.py`（33 characterization 用例），回归 708→741。**核心发现**：Exploration 是四案例里唯一「本身已是 page-dispatch FSM」的任务；`fire()` 用**正向战斗页确认**（`get_current_page() in (page_battle_prepare, page_battle)`）+ **双重上界**（`max_tries=4` 且 `Timer(10)`）+ **会返回 `False`** + Task 层 recovery（滑地图）——正是 RealmRaid `fire()`（R-R1）未来改造的**参照实现**；只覆写 `_exit_matcher()`，结算 100% 走基类 Contract v2；输入全走 `Control`（BehaviorTrace 全覆盖）；无界裸 `while` = 0；唯一 `wait_until_appear` 带 `wait_time=3`。**D015 无需修改**（四案例逐条正向印证）。**Exploration 生产行为零改动**（`tasks/Exploration/` 对 HEAD 零 diff） | `docs/AI_CONTEXT.md` §4.37；`docs/DECISIONS.md` D015 |
| — | Level A cleanup 批次 1（只删可证明零引用的死代码） | 已完成：**FIXED 4 个 Cleanup Queue issue（§16 行）/ 5 条子项 / 6 个具名删除 / 3 个生产文件，全部 behavior-neutral**——RealmRaid `medal_fire()` / `is_ticket()` / `medal_grid` 死代码（零调用方，`medal_grid=None` 使 `medal_fire` 调用即 `AttributeError`）+ `RealmRaid`·`AttackNumber` dead import + 随之变死的 `import time`；`KekkaiActivation` dead import `parse_rule`；`KekkaiUtilize` `last_best_index = 99` 死属性。**DEFERRED**：RealmRaid `fire`/`fire_again`/`check_refresh` 不可达 `return False` + `run()` 死分支（Level C 耦合，不可达语句是未来 timeout 失败路径接入点，刻意保留）；`ocr_time` 的 `None + datetime` crash（正确恢复语义不唯一）。**KEPT**：`KekkaiActivation.check_card_num` stdlib `random.randint`（D007「已存在未迁移用法保留」+ `RyouToppa.flush_area_cache` 同款先例）。**NOT_REPRODUCED**：lazy_roll `random_delay(0.0,1.0)`（`random_delay` 是纯 `_rng.uniform` 无 sleep，行为已正确）。回归 710→708（`test_realm_raid_state` 41→39），**无生产运行语义变化** | `docs/AI_CONTEXT.md` §4.36；`docs/状态验证与重试模式归纳.md` §16 |
| — | RealmRaid 状态机迁移前静态收口 | 已完成：完整状态图 + State Matrix（S1~S15）+ 目标选择 / 进攻按钮逐条回答 + 「战斗进入判定」专项 + GeneralBattle 交接 + Wait/Loop/Stale Inventory + Action→Verify Gap（P1~P4）+ Existing Issue Register（13 条）+ RealmRaid vs Kekkai 对比，专题文档 `docs/RealmRaid状态机静态收口.md`。`tests/test_realm_raid_state.py`（41 characterization 用例），回归 669→710。**核心发现**：`fire()` 判定进入战斗的唯一依据是「旧页面标识 `I_RR_PERSON` 消失」，无战斗页正向标识 / 无超时 / 无 retry 上限，且 `fire()` 恒返回 `True`（尾部 `return False` 不可达）使 `run()` 里 `if not self.fire(index)` 成为死分支。**RealmRaid 生产行为零改动**（`tasks/RealmRaid/` 对 HEAD 零 diff），FrameWait 在 RealmRaid 仍零消费者。真正迁移待 Level C | `docs/AI_CONTEXT.md` §4.34；`docs/DECISIONS.md` D013 |
| T4-4/T5-1 前半 | Kekkai 状态机迁移前静态收口（`KekkaiActivation` / `KekkaiUtilize`） | 已完成：两个任务完整文本状态图 + State Matrix（KA 11 / KU 10）+ `harvest_card` 逐步拆解 + pilot 评估 + 三处列表 swipe 对照（`list_find` / KA `check_card_num` / KU `perform_swipe_action`）+ Semantic vs Visual Wait 迁移矩阵 + Loop/Retry 三分类 + Existing Issue Register（12 条）+ 迁移候选 A/B/C/D，专题文档 `docs/Kekkai状态机静态收口.md`。`tests/test_kekkai_activation_state.py`（18）+ `tests/test_kekkai_utilize_state.py`（23）characterization，回归 628→669。**Kekkai 生产行为零改动**，`wait_for_changed_and_stable` 在 Kekkai 仍零消费者。真正迁移（`harvest_card` 状态化 / swipe 后 frame-wait / `swipe_adb` trace）仍是 Level C | `docs/AI_CONTEXT.md` §4.33；`docs/DECISIONS.md` D013 |
| — | 点击空间随机 / 偏移机制全仓审查 | 已完成静态审查：确认点击落点 = ROI 内单层均匀随机、全程无状态、无 preferred center、无 double jitter；中心偏置只用于 swipe；固定坐标 / 任务私有偏移清单；BehaviorTrace 学习中心的数据缺口；5 个 latent bug（仅记录）。未改任何代码 | `docs/AI_CONTEXT.md` §4.20 / §8.2；`docs/DECISIONS.md` D014 |
| — | 人工点击采样器 ManualClickRecorder v1 | 已完成：`dev_tools/manual_click_recorder.py` 被动监听目标 MuMu 游戏画面内真实人工左键 → 换算 1280×720 逻辑坐标 → 写 `log/manual_click/<config>_<日期>.jsonl`；只观察不发输入，与 BehaviorTrace 数据分离；纯比例坐标转换（窗口移动 / resize 均支持）；`--scene/--target/--roi` 人工标签、ROI 模式输出 `u/v`；31 用例，回归 282→313 | `docs/AI_CONTEXT.md` §4.21；`docs/DECISIONS.md` D014 |
| — | T7-1 第 1 阶段：ClickSampler v1 + LEGACY_UNIFORM 等价接入 | 已完成：`module/click_sampler.py` 是最终点击 x/y 空间随机的唯一公共入口，`LEGACY_UNIFORM` 逐字等价 `random_point_in_roi`；`RuleClick`/`RuleImage`/`RuleOcr`/`RuleGif` 的 `coord()`(+`coord_more()`) 已接入；`HABIT/STRICT/UNIFORM` 占位未实现。18 用例，回归 363→381，**生产点击分布零变化**，`Control`/后端/`BehaviorTrace` 未改 | `docs/AI_CONTEXT.md` §4.24；`docs/DECISIONS.md` D014 |
| — | T7-1 第 2 阶段：ClickProfile + HABIT/STRICT/UNIFORM + Safe ROI | 已完成：`module/click_profile.py` 的 `ClickProfile`(frozen，ROI 相对，fail-fast) + `DEFAULT_PROFILES`(个人先验 + provisional tiny/small) + `ClickProfileManager`/`resolve_profile`；`module/base/utils/random.py` 加 `random_normal`；`ClickSampler` 实现 HABIT(三成分 mixture + rejection + 非 clamp fallback)/STRICT(强制窄 profile 无 tail)/UNIFORM(Safe ROI 内均匀)。**新策略零生产消费者**，`Rule*.coord()` 仍默认 `LEGACY_UNIFORM`，`Control`/后端/`BehaviorTrace` 未改。63 用例(44+19)，回归 381→425，生产分布零变化 | `docs/AI_CONTEXT.md` §4.25；`docs/DECISIONS.md` D014 |
| — | 点击 ROI 尺寸静态普查 | 已完成：只读 AST 工具 `dev_tools/click_roi_inventory.py` 普查全仓 2678 个可点击 ROI 定义的真实尺寸分布，输出 `log/click_roi_analysis/`。得数据推导的候选尺寸边界（Tiny<24 / Small 24–40 / Normal 40–96 / Large≥96，中间分界≈40 非 48），**未采纳、未落地任何默认行为**；窄条按钮需按语义单独指定。27 用例，回归 425→452，零生产代码改动 | `docs/AI_CONTEXT.md` §4.26 |
| — | 大尺寸点击 ROI 人工审查目录 | 已完成（工具与目录）：只读 `dev_tools/large_click_roi_review.py` 从 inventory 筛出 `short_side>=96` 且**确认可点**的 198 条，逐条给语义初判 / Point·Region / P0-P2 / 真实消费者（AST，含 `random.choice` 间接点击、按 Assets 类名消歧同名符号）/ 模板图路径 / 人工检查重点，输出 `log/click_roi_analysis/large_click_roi_review.{md,json}`，全部 `review_status=pending`。52 用例，回归 452→504，零生产代码改动。**用户逐项人工确认仍待做**（见 P1「点击 ROI 人工审查」） | `docs/AI_CONTEXT.md` §4.27 |
| — | T7-1 Phase A：GeneralBattle 通用结算迁到 C_RANDOM_RD | **已被 SETTLEMENT CONTRACT V3（上一行，2026-09-03，D016）取代**——RD/RD2 + HABIT profile 已从生产移除。以下为 V2 历史：已完成（GENERALBATTLE SETTLEMENT CONTRACT V2，2026-09-02）：`_handle_result` / `_handle_reward` → `_settlement_click(context)`——**timer 驱动的重复 RD 点击**，恢复 Phase A 前「点击→下一帧重新识别→仍是 settlement 就 timer 到点再点」节奏，只把 `random_click(LEFT/RIGHT)` 换成 `C_RANDOM_RD`（HABIT + `_SETTLEMENT_PRIMARY_PROFILE`，每次重采坐标）、把固定 0.8s 换成每次随机 `0.7~1.0s`。同一语义 Page 连续多帧点 RD = 正常结算内部推进（不判失败）；page 变化立即换 handler。**Contract v1 的 one-primary / 2s 观察 / same-page 超时 / sticky RD2 / Reward marker 决定区域 全部 Superseded**（`_advance_settlement` / `REWARD_MARKERS` / `settlement_stage`·`primary_ts`·`fallback` 均删）。`C_RANDOM_RD2` / `_SETTLEMENT_FALLBACK_PROFILE` 保留为**零消费者预留 fallback**。69 用例（37 settlement + 17 timing + 15→16 trace-check），回归 504→559。**仅改 `general_battle.py`**，`random_click()` 及带 `ltrb` 的调用方、所有其它 `RuleClick`、profile 参数、RD/RD2 Asset ROI 均不变。**仍需 Level C MuMu 真机验收** | `docs/AI_CONTEXT.md` §4.28；`docs/DECISIONS.md` D014 |
| — | T7-2：Point Target 连续尺寸适配 | 已完成：`module/click_profile.py` 加 `point_size_factor(short_side)`（smoothstep，锚点 `POINT_SIZE_MIN_PX=24` / `POINT_SIZE_FULL_PX=96`）+ `adapt_point_profile(base, roi)`（`short_side=min(w,h)` → factor → `effective_uv = 0.5 + (base_uv - 0.5) * factor`，返回新 frozen `ClickProfile`）。**只调 `preferred`**，sigma/weights/margin/metadata 全不变；取代离散 `if tiny/elif small/…`。纯计算、不调 RNG/设备。**零生产消费者**（无 `Rule*.coord()` 调它）。+21 用例，回归 559→581，零生产行为变化 | `docs/AI_CONTEXT.md` §4.29；`docs/DECISIONS.md` D014 |
| — | T7-3 / T7-3.1：首批 opt-in 候选审计 + Point Semantic Profile 数据基线收口 | 已完成：T7-3 只读审计选出低风险候选 + 确认「小 ROI 只拉回 `preferred`、`sigma` 不变」需在生产 opt-in 前解决（→ T7-4）。T7-3.1 用现有 manual click 原始数据（`yys1_2026-09-01` cluster_A，107 独立落点）对当前 `RyouToppa.C_AREA_1` WIP ROI 重算，`wide_card` preferred `(0.68,0.53)`→`(0.58,0.59)`（旧值坐标基准是手工估的大卡片区，非真实 RuleClick ROI）；`normal_button`（无 runtime ROI）/ `large_area`（Region 混合来源）不重估、补 `provisional`；`DEFAULT_PROFILES` 现 8 个条目全 `provisional=True`。sigma/weights/margin 全未改，`provisional` 是纯文档标记不改采样。+3 用例（65→68），回归 581→584，零生产行为变化 | `docs/AI_CONTEXT.md` §4.30；`docs/DECISIONS.md` D014 |
| — | T7-3.2：首个普通 Point Target 生产 opt-in（`RyouToppa.C_AREA_1`） | 已完成（静态，**待 Level C**）：`C_AREA_1`（`area_map[0]`）从 `LEGACY_UNIFORM` 显式改为 `wide_card` → `adapt_point_profile` → `ClickSampler` HABIT。新增薄组合入口 `ClickSampler.sample_point(roi, base_profile)` + `RyouToppa` 私有 `_click_toppa_area(index)`（`if rule is self.C_AREA_1` 逐调用点身份守卫；`C_AREA_2..8` 与其它一切保持 `self.click` / `LEGACY_UNIFORM`）。`C_AREA_1.roi_front (514,141,223,116)` short_side 116 ≥ 96 → factor 1.0 → EffectiveProfile 逐字段 == `wide_card` base（不收缩，预期；T7-4 落地后仍成立）。`control_name` 仍 `rule.name`（BehaviorTrace `target="area_1"`）。**未塞进 `BaseTask`**（god class 约束，D014 扩展）。+25 用例，回归 584→609。**Point Target 生产消费者 0 → 1** | `docs/AI_CONTEXT.md` §4.31；`docs/DECISIONS.md` D014 |
| — | T7-5：全局 Preferred Hotspot 点击策略（生产默认策略变更）—— Stage 1 + Stage 2 均 IMPLEMENTED | **Stage 2（2026-09-07，IMPLEMENTED）**：`ClickSampler.sample_region(roi, name)` + `click_profile.default_region` + `click_preference` 3 条 Region 兜底条目；`GeneralBattle._sample_settlement_click` 从 `sample()` 改 `sample_region()`（region 选择 / 两次点击 / policy 一字不变）；`GeneralInvite._random_point_in_area` → `sample_target`（删私有 helper）；**`Secret.find_battle` 关卡卡片 `click_rule.center` ×2 → `for click_index in 1..2: click_rule.coord()`（两次独立 `sample_target`；`click_roi` 几何 / 状态文字避让 / 循环 / 间隔 / `control_name` 未改）**；`RuleLongClick` 经 `RuleClick.coord` 继承已在模型上。**生产 bare `ClickSampler.sample()` 消费者 = 0；whole-ROI Uniform = 0；double-sampling = 0。** direct `device.click`（44）+ 全部 `.center`/`.front_center()`/就地 `RuleClick` 消费点重新核对：ALREADY_SAMPLED 18 / **PREFERRED_POINT 迁 2**（`GeneralInvite` + `Secret`）/ PREFERRED_REGION 迁 1 / **PENDING_MIGRATION（有可证明安全 ROI 但未迁）= 0** / EXACT_COORDINATE_BLOCKED 23（无静态可证安全 ROI → 后续任务 B）。改 `test_general_battle_settlement` 4 用例 + `test_t7_5_preferred_hotspot` registry 断言 + 新增 `tests/test_t7_5_stage2.py` 24 用例（含 `SecretLayerCardMigrationTest` 4），回归 **953→977**。`normal_button`/`I_FIRE`/Region empirical + 23 处 EXACT 属**后续独立持续任务 A（Empirical Hotspot Calibration）/ B（Exact Coordinate ROI Discovery）**，Level C，**非 Stage 2 blocker**。<br>—— 以下为 Stage 1（2026-09-04，`docs/DECISIONS.md` D014 **T7-5 段**，两条旧约束 `Superseded`）：普通生产 Point 点击的**默认**从整 ROI `LEGACY_UNIFORM` 改成「该 target 的 preferred 热点 + 现有偏移模型 + Safe ROI」。新增 `module/click_preference.py`（`Provenance` 四级 / `TargetPreference` frozen / 静态 `TARGET_PREFERENCES` 按 `rule.name` 索引 / `resolve_target_preference`）+ `click_profile.default_point` + `ClickSampler.sample_target(roi, name)`；`module/atom/{click,image,gif,ocr}.py` 的 `coord()` 全改走它（`ocr.py` 保留 `_normalize_ocr_click_area`）。没有 empirical 热点 → `CENTER_FALLBACK=(0.5,0.5)`，**不退回 Uniform**（Tiny 仍中心附近小幅随机）。registry 现仅 `area_1`（EMPIRICAL，`(0.58,0.59)`+`wide_card`，C_AREA_1 落点不变）。**未改** `ClickSampler` 采样算法 / `adapt_point_profile` / `random_point_in_roi` / `Control` / 后端 / `BehaviorTrace` / `BaseTask.click` / `random_click()` / GeneralBattle V3 `_sample_settlement_click`（region policy + 区内 `LEGACY_UNIFORM` 一字不变）/ `RyouToppa._click_toppa_area`。改写 10 个锁旧默认的用例 + 新增 `tests/test_t7_5_preferred_hotspot.py` 22 用例，回归 **922→944**。inventory：`docs/T7_TARGET_PREFERENCE_MAP.md`。**Stage 2（Level C 后）**：`device.click` 旁路逐类迁移 / GeneralBattle V3 区内采样 / `normal_button`·`I_FIRE` 用 `runtime_roi_probe`+manual click 重标定升 EMPIRICAL。**Stage 1 Level C**：Tiny/Small/Normal/Large/Dynamic/Region 各抽真实消费者看 BehaviorTrace+OASX 散点 | `docs/AI_CONTEXT.md` §4.44；`docs/DECISIONS.md` D014 T7-5 段；`docs/T7_TARGET_PREFERENCE_MAP.md` |
| — | T7 点击热点统一规则：人工热点优先 + 未采样目标按 ROI 尺寸计算热点 | 已完成（2026-09-08，D019）：热点来源优先级统一为 `EMPIRICAL > RULE_FALLBACK`；保留 `area_1` / `wide_card` 的 EMPIRICAL `(0.58,0.59)`，未采样 target 使用 `RULE_BASE_PREFERRED=(0.58,0.59)` 但 provenance 明确为 RULE_FALLBACK。新增 preferred-only `adapt_preferred_by_size`（short side 24/96 + smoothstep）；Point 继续由 `adapt_point_profile` 同时适配 preferred / sigma / tail，Region 只复用 preferred 适配、绝不继承 Point shape 收缩。GeneralBattle Settlement 三个 Region 改 effective hotspot，region 选择 / 两次点击 / FSM 不变。相关 216 项离线测试已通过；全量基线见 `docs/AI_CONTEXT.md` §7。Level C 点击体验待后续真机抽样 | `docs/AI_CONTEXT.md` §4.50；`docs/DECISIONS.md` D019；`docs/T7_TARGET_PREFERENCE_MAP.md` |
| — | T7-4：Point Target spread 连续尺寸适配 | 已完成：`adapt_point_profile` 从「只调 `preferred`」扩展为用**同一个** `size_factor` 也调 `core/medium sigma`（`_lerp` 向「`tiny` 的 σ 锚点」`POINT_MIN_*_SIGMA_*` 插值）+ `tail`（`base_tail * f`，削掉的 `base_tail*(1-f)` 回补 `core_weight`，`medium_weight` 不动）。**未新增第二条尺寸曲线**（仍只有一个 `size_factor`）、`safe_margin` / `max_attempts` / `provisional` / `name` 原样透传。端点精确（`_lerp` 写成 `lo*(1-t)+hi*t`）：`f=1`（`short_side≥96`）→ EffectiveProfile **逐字段等于 base** → `RyouToppa.C_AREA_1`（ss=116）生产落点分布不变（双测试锁）；`f=0`（ss≤24）→ σ=锚点、tail=0、preferred=center。锚点选 `tiny` 的 σ（审计 default/small/tiny/strict 后，语义最贴「Point 最小尺寸」且不退化）。+19 用例，回归 609→628，零生产行为变化 | `docs/AI_CONTEXT.md` §4.32；`docs/DECISIONS.md` D014 |
| — | RealmRaid `_fire_again()` 最后一次 attempt timing bug —— Level C 真机失败 → 定位 → 修复 | 已完成（2026-09-11，`docs/AI_CONTEXT.md` §4.63，非新 ADR）：**真机 Level C defect** —— 退四首战主动退出后「再次挑战」`RES_FIRE_AGAIN → RES_SHOW_AGAIN → RES_FRESH_ENSURE`，游戏已实际进入 `page_battle_prepare`（右下角「准备」按钮正常出现），但 `Fire again: attempt 4, reaction 0.77s → Fire again: button gone during reaction, re-evaluate → WARNING Fire again: bounded retry / timeout without entering battle`，任务异常遗留在 `page_battle_prepare`。**根因**（READ ONLY 已定位，非 `page_battle_prepare` success state 缺失——它与 `_is_active_battle_entry()` 逐字同源）：`_fire_again()` 里「reaction 后按钮消失」分支是裸 `continue`，缺一次像兄弟分支那样的 `_wait_again_entered_battle()` 有界确认；非最后一次 attempt 靠下一轮循环顶部隐式补一次确认掩盖，但最后一次 attempt 没有下一轮，直接耗尽判超时。**修复**（唯一生产改动，一处分支）：`tasks/RealmRaid/script_task.py::_fire_again` 的该分支先调用 `_wait_again_entered_battle()`，`'battle'` 才判成功、否则才 `continue`，复用既有 `RR_AGAIN_POST_CLICK_TIMEOUT`（未新增 timeout 常量）。**严格未改** `_is_active_battle_entry()` / `is_in_prepare()` / `is_in_real_battle()` / `is_in_battle()`（`_fire_again` 不回退成宽 detector，continue 遵守 D001「Battle Lifecycle Detector ≠ New Battle Entry Detector」）；`fire()` 里同构的裸 `continue`（`RR_FIRE_*`）本轮明确不动（当前证据只指向 `_fire_again()`）；未新增 `aborted` 后 recovery / navigator edge / `close_unknown_pages` closer / GeneralBattle 改动。新增 `tests/test_realm_raid_state.py::FireAgainLastAttemptReactionGoneTest`（3：最后一次 attempt 按钮消失 + wait 判 `'battle'` → True 且不落超时warning / wait 一直不给 `'battle'` → 仍 bounded False，调用次数 = `RR_AGAIN_MAX_TRIES` / 源码形态锁定分支必须调用有界确认），`_is_active_battle_entry()` 未改宽已有 `ActiveBattleEntryContractTest` 覆盖、未重复造测试，回归 **1415 → 1418**，0 regression | `docs/AI_CONTEXT.md` §4.63；`docs/DEVELOP_LOG.md` 2026-09-11 同名条 |
| — | KekkaiUtilize Scheduler v1：静默窗口 + 短期 retry cooldown 随机化 | 已完成 Level A/B（2026-09-11，`docs/AI_CONTEXT.md` §4.64 / `docs/DECISIONS.md` **D023**）：正式落地「候选 `next_run` → quiet 归一化 → 写回」单向管线。新增纯函数模块 `tasks/KekkaiUtilize/scheduling.py`（`is_in_quiet_window`/`next_quiet_window_end`/`normalize_for_quiet_window`，`[start,end)` 半开区间、支持跨午夜、不 import `script.py`）+ `ScriptTask` 7 个 task-local helper（`_build_retry_target`/`_normalize_quiet_target`/`_schedule_target`/`_schedule_retry`/`_guard_quiet_window` 等）。**5 处出口改造**：5 次未达标 / 连续 3 次失败 / 跨区+同区无达标候选三处旧 `now+5/10/20min` 硬编码统一改 `_schedule_retry`（随机 5~30 分钟 cooldown）；「已在寄养」OCR 剩余时间出口**逐字保留**，只接入静默窗口归一化（`min_run_interval` 地板顺序不变）；`utilize_enable=False` 的通用 `success_interval=6h` 路径**不动**。`run()` 入口新增 `_guard_quiet_window()`，静默窗口内立即重排 + `TaskEnd`，不进任何页面/OCR/业务动作。**配置**：`UtilizeScheduler` 新增 `quiet_window_enable`/`quiet_start`/`quiet_end`（类型沿用 `AntiBan.sleep_start/sleep_end` 同一套 `Time`）/`quiet_resume_jitter_min/max`/`cooldown_min/max`；跨字段校验用 `field_validator(..., mode='after')` + `info.data`（沿用 `tasks/Dokan/config.py` 既有写法）——**不用** `model_validator`，会撞 `ConfigBase.__init__` 异常恢复的 `IndexError`（本轮实测复现）。**`server` 参数**核对 5 个出口后统一显式 `server=False`（`UtilizeScheduler.float_time` 默认 0，此前隐式 `server=True` 本就是 no-op）。随机源统一 `module.base.utils.random.random_int`，未引入 stdlib `random.Random()`；`Config.task_delay()` 里既有 stdlib `random.randint` 本轮未动、未复制其写法。新增 `tests/test_kekkai_utilize_scheduling.py`（16，纯函数 CASE A~H 全覆盖含跨午夜）+ `tests/test_kekkai_utilize_state.py` 5 个新测试类（19），回归 **1418 → 1453**，0 regression。**未做**（下一轮）：不领取寄养奖励（入口 `check_utilize_harvest`）、自己结界卡满级检查/替换（入口 `check_max_lv`）、`goto_page` 失败 U3 技债、好友扫描/卡种筛选/收益阈值/OCR、`KekkaiActivation` 同类调度。**Level C 待验**：00:00~07:00 实际不启动、正常寄养 next_run 仍符合 OCR 语义、cooldown/jitter 分布体感 | `docs/AI_CONTEXT.md` §4.64；`docs/DECISIONS.md` D023；`docs/DEVELOP_LOG.md` 2026-09-11 同名条 |
| — | KekkaiUtilize 业务开关配置：满级检查/替换新增开关，寄养奖励沿用既有字段，OASX 0 修改 | 已完成（2026-09-11，`docs/AI_CONTEXT.md` §4.65 / `docs/DECISIONS.md` **D024**）：READ ONLY 先核实 OASX（`d:\oas_xy\OASX`）任务配置面板完全 schema-driven——`TaskParameterPanel → Args → ArgsController.loadGroups() → ApiClient().getScriptTask() → ArgumentModel.fromJson() → ArgumentView` 按后端 `model_json_schema()` 的 `type` 字符串通用分发控件（`'boolean' => Checkbox`），全仓无 KekkaiUtilize 专属配置页面，任何新增裸 `bool` 字段无需改 OASX 一行代码（唯一隐藏机制 `dynamic_hide` 未对任何 KekkaiUtilize 字段使用）。**结论：本轮 OASX 零修改**。**`utilize_harvest`**（是否领取寄养奖励）：已存在字段，未被隐藏，本就会出现在前端——不新增重复字段、不改默认值（维持 `True`）。**新增** `UtilizeConfig.auto_replace_max_level: bool = False`（是否自动检查满级式神并替换）：`run()` 唯一调用点从无条件 `self.check_max_lv(con.shikigami_class, con.auto_fill)` 改为 `if con.auto_replace_max_level: self.check_max_lv(...)`；`check_max_lv()` / `unset_shikigami_max_lv()` / `switch_shikigami_class()` / `set_shikigami()` 全部原样保留未删除，默认关闭。新增 `tests/test_kekkai_utilize_state.py::MaxLevelAndHarvestSwitchTest`（8：两个开关各 True/False 通过完整 `run()` 验证调用/不调用 + 参数透传、新字段默认值、config model 可 instantiate/schema dump 且 `type` 均为 `'boolean'`、源码级锁定旧方法未删除），回归 **1453 → 1461**，0 regression | `docs/AI_CONTEXT.md` §4.65；`docs/DECISIONS.md` D024；`docs/DEVELOP_LOG.md` 2026-09-11 同名条 |
| — | GeneralBattle Settlement Micro-Burst v1.2：lifecycle / segment 分层，修复 Reward budget 卡死 | **v1.1 Level C FAIL → v1.2 Level A/B 已完成，Level C RE-TEST PENDING**（2026-09-14，`docs/AI_CONTEXT.md` §4.70 / `docs/DECISIONS.md` **D025 v1.2 修订**）。真机失败链：`budget=2` → Result 1 click → Reward 1 click → 错误地把 budget exhaustion 当 terminal → 下一帧仍 `page_reward` 并卡住。修复后 2/3/4（50%/30%/20%）只表示当前 semantic state 的局部 Click Segment 节奏预算；fresh result/reward 上 segment 耗尽可续段，semantic terminal 才 teardown。采用方案 B：Result → Reward 新建独立 segment，Result 已用 clicks 不侵占 Reward；segment renewal 与 anchor 生命周期解耦，安全时仍可保持同点。保留 Click → Observe → Decide、same-state second-click gate、Unknown 禁止盲补、Reward layout-aware region、session 最多一次主动 anchor switch、FIRE isolation。新增 `settlement_total_clicks` + provisional `SETTLEMENT_MAX_TOTAL_CLICKS=9` 作为固定故障安全帽（Device click/stuck guard 仍为第二层），segment exhaustion 不再记录为 terminal。新增 11 个 deterministic 测试，完整基线见 `AI_CONTEXT` §7。未改 RealmRaid/Activity/FIRE/OASX 等边界 | `docs/AI_CONTEXT.md` §4.70；`docs/DECISIONS.md` D025；`docs/DEVELOP_LOG.md` 2026-09-14 v1.2 条 |
| U3 | KekkaiUtilize：进入好友结界寄养页导航失败必须阻断寄养业务 | 已完成 Level A/B（2026-09-14，`docs/AI_CONTEXT.md` §4.68，可靠性修复，无新 ADR）：`check_utilize_add()` 里 `if not self.goto_page(page_guild_realm_utilize): logger.info(...)` 只记录日志没有 return/continue 的旧技债（§4.64/§4.65 末尾已挂账）——导航失败后仍会落入 `run_utilize(...)`，在错误页面继续 OCR/swipe/click。**根因**：`goto_page()` 真实契约是「成功 `True` / 失败抛 `GamePageUnknownError`」，从不返回 `False`，旧代码的 `if not goto_page(...)` 判断因此从未生效过。**修复**：本地 `try/except (GamePageUnknownError, GameStuckError)` 把「异常」与「假值返回」两种失败信号统一收敛，失败即复用 `_finish_low_value_utilize`/`_record_utilize_failure` 同款终态失败出口（`_schedule_retry` + `utilize_terminal_failure=True` + `return False`），使外层 `run()` 的 `if not self.check_utilize_add(): return` 自然生效，跳过 `check_max_lv`/`check_utilize_harvest`/`check_box_ap_or_exp`/`receive_guild_assets` 全部下游业务；成功路径逐字不变。不新增第二套重试机制，不触碰 `utilize_add_count`/`utilize_failed_count`，未改 Navigator/`goto_page` 契约。新增 `tests/test_kekkai_utilize_state.py::UtilizeNavigationFailureGuardTest`（10：导航成功/失败(含异常)/深层业务 0 穿透/复用 Scheduler v1 retry/quiet window 仍生效/失败仅记一次/本轮立即结束/成功路径不变），回归 **1479 → 1489**，0 regression。**Level C 待验**（真机制造导航失败、确认日志与业务隔离、retry/quiet 顺延正确） | `docs/AI_CONTEXT.md` §4.68；`docs/DEVELOP_LOG.md` 2026-09-14 同名条 |
| — | FIRE 点击前延迟配置任务级可调（RealmRaid / RyouToppa / EvoZone / Orochi / ActivityShikigami / EternitySea / FallenSun / Sougenbi） | 已完成 Level A/B（2026-09-18，`docs/AI_CONTEXT.md` §4.77 / `docs/DECISIONS.md` D001 补记，非新 ADR）：从未合并的 L2 worktree `wt-l2-interaction-reaction` 按「最小范围移植」把 `FireReactionConfig` + `fire_reaction_range()` 接入 master，不带入该 worktree 混杂的 L1 Global Click Pipeline / L2-2 `appear_then_click(policy=)` 普通点击迁移。8 个任务 `config.py` 加 `fire_reaction` 组（默认 400/800ms，`template.json` / `zh-CN.json` 同步）；6 个既有 FIRE FSM（RealmRaid ×2、RyouToppa、EvoZone、Orochi 单人+组队、ActivityShikigami、`GeneralInvite.click_fire`）只换 reaction 来源，FSM 形状不动，Orochi `run_wild` 按要求排除且不修复其资产断链；EternitySea / FallenSun / Sougenbi 从零 reaction 裸连点新接入 primitive `confirm_delay=`，不新建状态机。新增 `tests/test_fire_reaction_task_config.py`（30，含真实驱动到点击分支证明配置生效、10 类变异全部被抓），改 7 个既有测试文件的 `REACTION_FIRE` 断言，回归 **1530 → 1560**。真实隔离 HTTP 验证 8 个任务 `GET/PUT .../args` 正确、`oas1.json`/`oas2.json` 全程未变。**Level C 待验**（真机看 reaction 是否按任务生效、EternitySea/FallenSun/Sougenbi 新 reaction 不影响进图节奏） | `docs/AI_CONTEXT.md` §4.77；`docs/DECISIONS.md` D001 补记；`docs/DEVELOP_LOG.md` 2026-09-18 同名条 |

---

## 当前任务

**L1 / L2 交互层（2026-09-15，worktree `feature/l2-interaction-reaction-layer`，基于 master `a5e2d7e6`，未提交）**：

- **L1 Global Click Pipeline + L1.2**：Level A/B **COMPLETE**（master 基线移植见 AI_CONTEXT §4.71；Login / DailyTrifles 原为显式
  排除项，Stage 3B §4.86 起已迁入 L1 执行器，不再豁免），Level C **PENDING**。
- **L2-1 Interaction Reaction Layer**：Level A/B **IMPLEMENTED**（InteractionPolicy + `appear_then_click(policy=)` + FIRE 任务配置
  400~800ms，§4.72），Level C **PENDING**。
- **L2-2 Interaction Policy Migration**：Level A/B **COMPLETE**（10 个高频公共模块 179 个点击逐点分类，迁移 7，§4.73 /
  `docs/L2_INTERACTION_POLICY_MAP.md`），Level C **PENDING**。NEEDS_C：GeneralInvite `check_then_accept` ×5、准备页
  「不同御魂」弹窗 ×2。
- **L2-3B FIRE Batch A**（master 集成：**不含 Orochi 野队**，见 §4.82）：Level A/B **PASS**（Orochi 野队 / EternitySea / FallenSun / Sougenbi 四条 FIRE 收口为有界状态机，
  正向判据 = 进入准备 / 战斗页，三个任务新增 `fire_reaction` 配置组，§4.74），Level C **PENDING**：真机看 4 次 / 10s / 3s 是否够、
  准备页 / 战斗页 marker 在四个入口是否稳定命中、结果 / 奖励页残留是否误判、reaction 是否符合配置。**Orochi 野队另需先补
  `I_OROCHI_WILD_FIRE` RuleImage（ROI 标定）才可运行**。
- **L2-3 FIRE 后续批次（未开始，按 L2-3A 审计）**：Batch B（有资格但缺 Level C 参数）AreaBoss ×2、Secret、DemonEncounter ×2；
  Batch C（先设计）GeneralBattle「准备」与 `PREPARE_CLICK_DELAY` 的合并；Batch D（已有正向判断，补齐流程）MetaDemon /
  OtherWorldTwilight / WantedQuests 秘闻 / Hunt 麒麟 / MartialArts ×2 / GoryouRealm / HeroTest / SixRealms / Dokan / Bondling；
  Batch E（被 `ui_click` 卡住）WantedQuests `challenge` / DemonRetreat / DyeTrials；Batch F（特殊 / NEEDS_C）FakeGod / RichMan /
  GuguArtStudio / TrueOrochi / AbyssShadows / CostumeBattle。
- **L2 后续（未开始）**：① 其余 50 个 master 模块约 670 处点击按模块批次审计（WeeklyPurchase = EXCLUDED_BY_PROJECT_DECISION，不进 backlog）；② `ui_click*` helper 是否透传 policy（需先设计
  helper 内循环的 reaction / retry 语义）；③ Navigator 逐 edge 的 NAVIGATION 元数据；④ 准备 / 伪神降临 / 大富翁的 Battle
  Entry 按 FIRE contract 收口（Orochi 野队已随 Batch A 收口）；⑤ 其它 `click_fire` / `run_invite` consumer 的 `fire_reaction`
  配置组（FallenSun / EternitySea 已随 Batch A 接入）；
  ⑥ `RR_AGAIN_CONFIRM_DELAY` 是否归入 CONFIRM（需 Level C 证据，不借机调参）。
- L1 / L2 真机验收与本阶段其它 Level C 合并进行；小号轮换业务如需 L2，另从 master 选择性移植到 synevo。

**当前阶段：CODE FREEZE（2026-09-14）——推送准备中，不接新功能。**

工作树是一份跨多轮的大型集成 WIP（77 个 tracked 改动 + 约 108 个 untracked 新文件），已完成
pre-push 深度审查并修掉审查发现的唯一 BLOCKER（ActivityShikigami macro idle ownership 泄漏）。
当前状态：

- **Level A/B 全部收口**，完整回归 **1494 / 1494 OK**，`compileall` 通过。
- **剩余工作只有两类**：① 各功能块的 **Level C 真机验收**（见下方「需要真机验证的任务」与各条
  目的 Level C 待验清单）；② **推送准备**（分支 / commit 组织 / untracked 文件 staging）。
- **本阶段不新增功能任务**。新需求一律等 Level C 收口、代码推送之后再立项。

已完成、**不再是待办**（曾经挂在下面「下一步」里的 P0）：Kekkai Scheduler v1（§4.64 / D023）、
Kekkai 业务开关（§4.65 / D024）、Kekkai U3 导航失败阻断（§4.68）、GeneralBattle Settlement
Micro-Burst v1 → v1.1（§4.66 / §4.67 / D025）、ActivityShikigami 爬塔线适配（§4.62）。
已**取消 / 关闭**：组队入口改造（CANCELLED）、返回庭院优化（CLOSED），见下方各自小节。

仍未开工的 P0 候选（**冻结期间不启动**）：Navigation「突破 → 式神录」READ ONLY 审查、
剩余 FIRE FSM（契灵 PARTIAL / 活动副本 / Orochi wild 断链）。

---

### 历史：2026-09-08 前后的主线

2026-09-08 最近完成的主线为 RealmRaid §4.51~§4.57，以及 Exploration 一连三轮：
**§4.58 / D021**（Boss 直退 / Exit Contract / Chapter FrameWait / solo Fatigue，Level A/B）→
**§4.59 Level C hotfix**（Exit `EXIT_SUCCESS` 多态 positive + Rotation Contract：轮换归用户所有）→
**§4.60 Boss Reward Flow 收敛**：用户重新确认真实业务需求 ——**不是**「Boss 后完全不领地图宝箱
直接退出」，**而是**原项目一直以来的「检查地图宝箱 → 有则正常领取 → 无则继续 → **不领取小纸人
奖励** → 沿用原 Exploration 原生退出 flow」。§4.58 + §4.59 基于错误理解新增的整套 Boss 专用退出结构
（`_run_boss_exit_transaction` / `EXIT_READY..EXIT_SUCCESS` / `_is_boss_exit_success_state` /
`_complete_boss_business_cycle` / `_skip_treasure_once_at_entrance` / `BOSS_EXIT_*` / `ExplorationExitState`）
本轮**撤销**，恢复原生 `collect_reward`（地图宝箱 or 小纸人 policy）链；solo Fatigue Safe Point 重挂到
`_maybe_boss_cycle_fatigue()`（外层稳定页）；**§4.59 Rotation Contract 与 §4.58 Chapter FrameWait
独立成立、保留不动**。`D021` 标 **Superseded** + 加「D021 corrected 补记」。`tests/test_exploration_state.py`
`66 → 56`（−10 全为删掉的废弃 contract 用例），完整回归 `1331 → 1321`，0 regression。

（以上为 2026-09-08 当时的状态快照。**该轮所写的「下一步」已被上方「当前任务」取代**：其中
结界卡精简 + 静默窗口、GeneralBattle settlement micro-timing、活动体力 / 门票 FIRE + Fatigue
均已完成；组队入口与返回庭院已取消 / 关闭。）

---

## P0 —— 已确定的后续业务改造（需真机窗口 / Level C）

> 用户已明确需求、方向已定，**正式实施前必须先按各条列出的「真实调用链核对」项 grep / 读源码确认现状**（不依赖旧报告机械改）。多数改的是同一个主循环，注意不要连续重构两次——相关联的改一起做。

### FIRE / Reaction Timing 覆盖维度（长期检查口径修正，§4.54 / §4.55）

**不能只统计「单人 FIRE consumer」**，且 **reaction 与 post-click state contract 是两件事，分别检查**。任何需要进入战斗的副本，长期按三个维度检查：

1. **单人 challenge / FIRE owner**（`run_alone` 各自点挑战 / FIRE）——RealmRaid `fire()` + `_fire_again()`（退四内部「再次挑战」，§4.56）/ RyouToppa `attack_area` / Orochi `_fire_orochi_alone` / EvoZone `_fire_evozone_alone` 已收口（含 bounded FSM + 三态 / 四态 post-click verify）；契灵 `run_alone`（`I_BALL_FIRE`）PARTIAL。
2. **组队 leader / host challenge owner**（本机在房间里点「挑战 / 开始战斗」）——统一 = `GeneralInvite.click_fire()`：**§4.54 补 `REACTION_FIRE` reaction**；**§4.55 补 bounded 四态 FSM**（BATTLE / ROOM_FAILED / RETRYABLE_ROOM / TRANSITION_UNKNOWN，返回 `str`，`Timer(GI_FIRE_TIMEOUT)` + `for attempt in range(GI_FIRE_MAX_TRIES)`，无 `while 1`，「旧 room 状态消失」单独发生不当 success，`run_invite` / 4 直接 caller 加 `== 'battle'` guard）。11 个副本自动继承两者。
3. **member / passive wait path**（只等队长开战）——`wait_battle` / `check_then_accept` / `run_member`，**明确不加 reaction**。

**判据**：凡本机真实点击稳定「挑战 / 开始战斗」按钮（维度 1 或 2）→ 统一纳入 `REACTION_FIRE=(0.4,0.8)` + fresh reconfirm + no stale click + no same-semantic timing stack。RealmRaid / RyouToppa 保持各自独立实现（不并入 `click_fire`）。

### FIRE FSM 收口进度

- **已完成（Level A/B）**：RyouToppa `attack_area`（§4.52）、RealmRaid `fire()` R-R1 + 三态边界（§4.52）、**RealmRaid `_fire_again()`（退四内部「再次挑战」，§4.56，三态 `battle` / `failure_page` / `timeout` + `RR_AGAIN_MAX_TRIES=4` + `Timer(RR_AGAIN_TIMEOUT=10)` + 每 attempt 独立 `REACTION_FIRE` + 可达 `return False` → `run()` 退四路径改走刷新）**、**Orochi `run_alone` `_fire_orochi_alone` + EvoZone `run_alone` `_fire_evozone_alone`（§4.53）**、**公共组队 challenge owner `GeneralInvite.click_fire()`（§4.54 reaction + §4.55 bounded 四态 FSM）**。标准形状 = `State Ready → REACTION_FIRE(0.4~0.8, 每 attempt 独立) → fresh reconfirm → click → 三态 / 四态 post-click verify（BATTLE / RETRYABLE_* / [ROOM_FAILED / FAILURE_PAGE] / TRANSITION_UNKNOWN）→ bounded retry（max_tries + finite Timer，无 while 1）→ explicit success / failure（str / bool）→ caller guard（不误交接 run_general_battle）`。`click_fire` 是四态版（含 ROOM_FAILED），正向战斗确认用 `_battle_entry_positive()`（优先 `is_in_battle()` + marker 兜底）。
- **PARTIAL / 未迁**：
  - **契灵（BondlingFairyland）`I_BALL_FIRE`**：真实结构 = `run_alone` 连点结契按钮直到消失、否则 3 次 → `raise BondlingNumberMax`（契灵存量满 500）；caller `run_catch` 两条独立语句 `self.run_alone(); self.run_general_battle(...)`；`run_catch` / `run_leader` 共用 `while 1`。**不是单次稳定 Point FIRE → GeneralBattle**，套三态模板需业务级重构（拆 `run_alone` + `run_catch` 循环 + `BondlingNumberMax` 资源信号）。待评估：是否值得为契灵单独做，还是保持现状。
  - **Orochi `run_wild` 的 `I_OROCHI_WILD_FIRE`**：全仓**无 `RuleImage` 定义**（只有图片 `tasks/Orochi/o/o_orochi_wild_fire.png`），`run_wild` FIRE 路径当前 `AttributeError` = 既有断链。修复 = 造 `I_OROCHI_WILD_FIRE` 资产 + ROI / threshold 标定（需真机截图），再照三态模板收口。
- **后续（活动副本）**：活动体力 / 门票挑战的稳定挑战 / FIRE 按钮 —— 见下方「活动体力 / 门票挑战」。要求同一 FIRE Contract：`REACTION_FIRE=(0.4,0.8)` + fresh reconfirm + positive battle + retryable state + transition unknown + bounded retry + finite timeout + explicit failure guard；**不新增第二套 FIRE delay**。

### Fatigue 安全节点铺开

- **已接入**：RyouToppa（每完成一个区域）、**Orochi / EvoZone 的 `run_alone`（§4.53，一场战斗完整结束、回到稳定挑战页之后）**、**RealmRaid（§4.56，`begin_fatigue_task('RealmRaid')` 在主 `while 1` 前 + `_realm_raid_cycle_safe_break()`：普通目标 / 退四「一个完整目标业务循环结束、即将回循环顶重扫九宫格」处，fresh `screenshot` + `_is_realm_raid_retryable_state()` 才 `try_fatigue_break(safe=True, repeat_completed=True, deadline=None)`；无墙钟时限故 `deadline=None`）**、**Exploration solo（§4.58 / D021，Boss win → map ready → direct exit → `page_exp_entrance` positive 后，deadline=`start_time+limit_time`，fatigue 后 fresh revalidate）**。
- **剩余待接**：
  - **活动体力 / 门票挑战** —— 等活动入口 inventory + FIRE FSM 完成后接。
- **长期规则（`docs/DECISIONS.md` D001「Fatigue Safe Point 铺开补记」）**：只在 `Business Cycle Complete + Stable Return State` 触发；fatigue 后必须 `fresh screenshot + revalidate`；组队 / member / wild 等有邀请 / 房间 / 队友同步的路径不接；`GeneralBattle` 永不是 Fatigue owner。

### Exploration 主流程收口 —— Level A/B 已实施，Level C re-test pending（§4.58 / §4.59 / §4.60 / D021）

> **§4.60（2026-09-08）撤销了 §4.58 / §4.59 基于错误需求（「Boss 后完全不领地图宝箱直接退出」）的
> Boss 专用退出结构，恢复原项目原生 reward / exit 链。D021 标 Superseded + 加「D021 corrected 补记」。**

| 拆项 | 当前状态 | 静态落地 / 剩余验收 |
|---|---|---|
| Boss 战后 reward flow | **IMPLEMENTED（原生链，§4.60）** | `page_exp_main → run_on_exp_main → collect_reward()` = `collect_treasure_box()`（有小 / 大宝箱 → `ui_click` 领取；无 → `return False`）`or collect_paper_man_reward()`。**检查地图宝箱、有则正常领取**（可多次 dispatch 处理多宝箱）。真机待验：宝箱识别 / 领取动画收尾 |
| 小纸人奖励 | **不领取（原生，§4.60）** | `collect_paper_man_reward` 里 `fire_monster_type=='boss' and not collect_paper_reward` → `"Not collect paper doll reward"` + 原生 `quit_exp_main()` + `return True` —— **原项目本就有**；`I_BATTLE_REWARD` + `collect_paper_reward` 领取分支仍在（地图 treasure 与小纸人是两个独立开关） |
| 退出 | **原生 flow（§4.60）** | `quit_exp_main()`（`I_UI_BACK_YELLOW` + `confirm_delay=REACTION_NAVIGATION`）→ `page_exp_exit` → `run_on_exp_exit`（`I_E_EXIT_CONFIRM` + `confirm_delay=REACTION_CONFIRM`）→ `exec_exp_page` page-dispatch 自然接管 `page_exp_entrance` / `page_exploration`。**无 Boss 专用 exit transaction / EXIT 状态机**（§4.58 / §4.59 的已删） |
| 自动退出（无宝箱时游戏自动结束探索） | **原 dispatcher 天然接管（§4.60）** | 未造 Auto Exit FSM；`quit_exp_main` / `run_on_exp_exit` 的 `appear_then_click(confirm_delay=)` 在 delay 后重新截图二次确认目标 marker、消失则不点 → 天然避免 stale click。未加新 current-page guard。真机待验：场景 B 不产生 stale `I_UI_BACK_YELLOW` / 错误 exit confirm |
| 轮换模式（Rotation Contract） | **IMPLEMENTED（§4.59，§4.60 保留不动）** | `switch_rotate()` 的 `AutoRotate.no`（默认）分支 = `pass`（observe only，脚本绝不点 `I_E_AUTO_ROTATE_ON` / `I_E_AUTO_ROTATE_OFF` 取消 / 重置用户轮换）；`page_exp_main` 的 `any_of` 本就覆盖轮换开 / 关两布局。`AutoRotate.yes` 仍 `C_CLICK_SETTINGS → fill_shikigami + 开轮换`（用户 opt-in，只做「开」方向） |
| Chapter FrameWait | **PARTIAL（Level A/B 接线完成，参数 Level C pending；§4.60 未动）** | 真实 swipe 后 `baseline → changed+stable → OCR`；ROI 从 OCR asset 推导，`(1065,203,1189,555)` + 6 个 task-local provisional 参数；无 swipe 的 `sleep(1)` 作为 retry throttle 保留 |
| Exploration Fatigue（solo） | **IMPLEMENTED（§4.60 重挂）** | `run()` 里 `begin_fatigue_task('Exploration')`（仅 ALONE）；Safe Point = `_maybe_boss_cycle_fatigue()` 在 `run_on_exp_entrance` / `run_on_exp` **顶部**——这两个 handler 只在 `get_current_page()` 正向命中外层页时才分发（= 已在合法外层稳定页），仅 `user_status==ALONE` 且刚完成 Boss 循环（`fire_monster_type=='boss'`）→ `try_fatigue_break(deadline=start_time+limit_time)` + 消费 Boss 标记 + early return（交下一轮 fresh dispatch = fatigue 后 fresh revalidate）。**不在 `page_exp_main`（宝箱 / 小纸人 policy / 退出未完成）触发**；leader/member 不接 |

**Level C re-test（§4.60 后下一轮真机重点）**：**场景 A（有地图宝箱）** Boss Win → `collect_treasure_box`
识别小 / 大宝箱 → 正常领取 → 无宝箱后 `collect_paper_man_reward` → `Not collect paper doll reward` →
`I_UI_BACK_YELLOW` → `I_E_EXIT_CONFIRM` → outer page；**场景 B（无地图宝箱）** Boss Win → 无 treasure →
游戏自动退出 / 原生退出 → **不产生 stale `I_UI_BACK_YELLOW` / 错误 exit confirm** → page-dispatch 接管
outer page。两场景：轮换模式都保持用户原状态；solo `_maybe_boss_cycle_fatigue()` 只在回到
`page_exp_entrance` / `page_exploration` 后触发。其余 Level C（§4.58 遗留）：`page_exp_main` ready 稳定性、
章节 ROI 与 changed/stable/poll/timeout 分布、OCR settle 后命中率。**不得用 FrameWait stable 替代 OCR
chapter success；不得为「简化」跳过地图宝箱；不得把地图 treasure 与小纸人混成一个开关。**

### RealmRaid 目标选择 / 退四策略改造 —— 已实施（§4.56 / `docs/DECISIONS.md` D020，Level A/B，2026-09-08）

用户已确认的 8 条需求本轮全部落地，唯一生产文件 `tasks/RealmRaid/script_task.py`。三个 AskUserQuestion 收口：退四触发 =「完全替换为『只剩最后 1 个可攻打目标』」；普通失败 =「保留配置，仍按 `when_attack_fail` 走」；Fatigue =「本轮就接入」。

| # | 需求 | 落地 |
|---|---|---|
| 1 | 不再按勋章数 / 勋章排序算攻击顺序 | ✅ `_grid_targets()` 用 `order_medal.find_everyone()` 当过滤器，勋章值只影响呱太判定 |
| 2 | 九宫格固定 1→9（从左到右、从上到下） | ✅ `run()` 取 `index = min(targets)`（1-based order） |
| 3 | 目标级业务 pacing 1.0~2.5s（非 `REACTION_FIRE`，旧「1~3 秒」作废） | ✅ `RR_TARGET_PACING = (1.0, 2.5)` module 常量 + `_enter_target()` 里唯一一次 `random_delay(*RR_TARGET_PACING)` → `sleep` → fresh 二次确认；与 `fire()` 的 `REACTION_FIRE` 是两个 timing owner |
| 4 | 普通目标失败 → 跳过 + 刷新，「再次挑战」不用于普通失败 | ✅ 普通分支落 `run()` 尾部 `when_attack_fail`（默认 REFRESH）；`_fire_again()` 只在退四分支被调 |
| 5 | 退四 = 只剩最后 1 个可攻打目标才进入 + 进入前解锁阵容 | ✅ `only_last = len(targets) == 1` 触发（替换旧 `index == 1`）；退四分支 `ensure_lock(False)` → … → `ensure_lock(lock_default)` 恢复 |
| 6 | 退四内「再次挑战」点击前 `REACTION_FIRE` + fresh 二次确认 | ✅ `fire_again()` → `_fire_again()` 三态 bounded 重写（`RR_AGAIN_MAX_TRIES=4` / `RR_AGAIN_TIMEOUT=10` / `RR_AGAIN_POST_CLICK_TIMEOUT=3`）+ `_wait_again_entered_battle()` |
| 7 | 退四最终失败 → 统一刷新（不停失败页） | ✅ `if aborted or not last_battle:` → `check_refresh()` → `continue` / CD 中 break |
| 8 | RealmRaid Fatigue Safe Point | ✅ `begin_fatigue_task('RealmRaid')` + `_realm_raid_cycle_safe_break()`（`deadline=None`） |

**退四次数**：`RR_EXIT_FOUR_SURRENDERS = 4` —— 旧 `run()` 逐字硬编码的 4 次，源码可证、非新拍（§5「don't guess」不触发）。

**Correctness follow-up 已完成（2026-09-08，同 §4.56 / D020 补充）**——§4.56 落地后复核出的 4 个 correctness 边界本轮全部修完（不扩新功能）：

| # | 边界 | 落地 |
|---|---|---|
| A | `when_attack_fail == CONTINUE` 必须真正跳过刚失败目标（旧涂帧方案删掉后此语义丢了） | ✅ 纯局部 `failed_orders: set`（不涂帧 / 不当 broken）；`available = targets - failed_orders`；`available` 空才 refresh；`check_refresh()` 成功 → `failed_orders.clear()` |
| B | Fatigue Safe Point 必须在 failure recovery / refresh 完成之后（原为 battle → fatigue → refresh） | ✅ 删普通分支 battle 后的 `_realm_raid_cycle_safe_break()`；改在共享尾部每个结果流 recovery 之后触发；helper 前 `wait_until_appear(I_BACK_RED, wait_time=RR_CYCLE_STABLE_TIMEOUT=10)` |
| C | 退四前 fresh screenshot 重新确认仍只有 1 个 attackable target | ✅ 只读 helper `_is_only_remaining_target(order)`；退四入口 `screenshot` + 该 helper（在 `ensure_lock(False)` 前）不满足 → `continue`；`_enter_target(order, require_only_remaining=True)` pacing 前后收紧；`only_last` 仍用 raw `len(targets)` 不用 `available` |
| D | 退四临时 `ensure_lock(False)` 必须 try/finally 恢复 `lock_default`（异常 / continue / break 都恢复） | ✅ `ensure_lock(False)` → `try:` (battles) `finally: ensure_lock(lock_default)`；无 `except`、不吞原异常；`lock_default` 来自 `con.general_battle_config.lock_team_enable` 不写死 |

**Level C hotfix 已完成（2026-09-08，§4.56 Level C hotfix / D001 补记）**——真机日志证明 `_fire_again()` 把退四首战失败结果页（`is_in_battle()` 因 `I_FALSE` 恒 True）误判成「已进新战斗」，没真实点「再次挑战」就 `Fire again: entered battle` → 假退四循环。修：`_fire_again` / `_wait_again_entered_battle` 的 4 处 `is_in_battle(False)` → RealmRaid-local 窄 helper `_is_active_battle_entry()` = `is_in_prepare(False) or is_in_real_battle(False)`（`is_in_battle()` 本体不动）。**OASX 文案同步已完成**（`cn_realm_raid_config.dart` 退四 /「攻打的勋章档位」；`order_attack` 字段保留改名不删）。

**Level C hotfix #2 已完成（2026-09-11，§4.63）**——上一条 re-test 窗口期间真机发现了不同的第二个缺陷：退四「再次挑战」最后一次 attempt（`RR_AGAIN_MAX_TRIES=4`）reaction 后按钮消失分支是裸 `continue`，缺一次 `_wait_again_entered_battle()` 有界确认，把已成功进入的 `page_battle_prepare` 误判成超时，任务遗留在准备页（详见 §4.63 / `docs/DEVELOP_LOG.md` 2026-09-11 同名条）。修：该分支先调 `_wait_again_entered_battle()`，`'battle'` 才判成功；`_is_active_battle_entry()` 未改宽、`fire()` 同构问题本轮不动。**下一轮 Level C re-test（合并两条 hotfix 一起验）**：首战主动退出 → `Battle result: Lose` 之后必须依次出现 `Fire again: attempt N, reaction 0.xxs` → `Click RES_FIRE_AGAIN`（真实点击）→ `Fire again: entered battle after click` → `GENERAL BATTLE START` → 真正进入 `page_battle_prepare` / `page_battle`；不能再出现 `Lose → 无 click → Fire again: entered battle`（hotfix #1 回归）；也不能再出现 `attempt 4（最后一次）→ button gone during reaction → bounded retry / timeout` 而游戏其实已进入 `page_battle_prepare`（hotfix #2 回归）；重点看 `RR_AGAIN_POST_CLICK_TIMEOUT=3` 是否够覆盖真实过渡帧窗口。

**Level C 验收待窗口**：`_grid_targets` 用 `find_everyone` 全格扫描的稳定性（match center → `roi_front` 映射准确率）、`_broken_orders` 对刚失败未刷新格的命中率、`RR_TARGET_PACING (1.0, 2.5)` 体感、退四「只剩最后 1 个」判定的真机可靠性、`ensure_lock(False)` 在退四场景确实解锁、`RR_AGAIN_*` 参数体感、退四最终失败刷新收敛、Fatigue safe point 是否稳定回到个人突破页；**（follow-up 追加）** CONTINUE `failed_orders` 与真机「失败格是否变 broken」的实际重叠（无害但值得观测）、`RR_CYCLE_STABLE_TIMEOUT = 10` 是否够覆盖刷新 / 结算动画、退四入口 `_is_only_remaining_target` 二次确认是否偶发误否决（模板抖动）、`check_refresh()` 返回 True 后 `I_BACK_RED` 出现的实际延迟；**（Level C hotfix 追加）** `_is_active_battle_entry()` = `is_in_prepare or is_in_real_battle` 在真机是否稳定（失败结果页确实 False、真正的新战斗准备 / 进行页确实 True，无 overlay 误命中）、退四 4 次是否都能看到真实 `I_FIRE_AGAIN` click、`fire()` R-R1 的 `is_in_battle()` 是否也需要同样收窄（本轮判定安全-by-context 未动，真机若见 `fire()` 侧类似假进入再收窄）。`RR_TARGET_PACING` / `RR_AGAIN_*` / `RR_CYCLE_STABLE_TIMEOUT` 均 PROVISIONAL，改值连带更新 D020 / D001 补记 + §4.56。

### Exploration 改造

**Level A/B 已实施，拆项状态与 Level C 验收见上方「Exploration 主流程收口」表。** Boss 直退、
treasure bypass、Exit Contract、solo Fatigue 已 IMPLEMENTED；Chapter FrameWait 已接线但参数仍为
PARTIAL / Level C calibration pending。不得再把本节当未开始任务。

### 组队入口改造 —— **CANCELLED / WON'T DO（2026-09-14，用户决定）**

原设想：庭院 → 直接点「组队」→ 进入组队 / 副本选择页 → 选对应副本 → 开始组队（取代「庭院 →
进入探索 → 找对应副本 → 进入组队」）。**取消原因**：直进 `page_team` 之后缺少「选层」能力，
要补齐副本层级选择才能真正可用，实现成本高于收益；用户明确决定取消。**不再作为待办**，
后续若重启需重新立项并重新核对 `navigator` / `page_team` / 各组队 consumer 真实链。

### 返回庭院优化 —— **CLOSED / NO IMPLEMENTATION（2026-09-14，用户决定）**

原设想：① 当前页面若有直接返回庭院按钮则优先点它；② 点后 fresh screenshot 二次确认；
③ 未成功再走旧 navigation fallback。**关闭原因**：现有 `goto_page(page_main)` / `navigator`
回庭院链已经够用，未发现值得新增的通用 direct-home capability。**不实施**，不保留为待办。

### 结界卡流程精简 + 静默窗口 / cooldown

**调度部分（③④⑤）已完成 Level A/B（2026-09-11，`docs/AI_CONTEXT.md` §4.64 / `docs/DECISIONS.md`
D023，Scheduler v1，仅 `KekkaiUtilize`，`KekkaiActivation` 未动）**：③ **00:00~07:00 默认静默**——
`UtilizeScheduler.quiet_window_enable=True`/`quiet_start=00:00`/`quiet_end=07:00`，`[start,end)`
半开区间，算法支持跨午夜（不写死 `start<end`）；`run()` 入口 `_guard_quiet_window()` 静默窗口内立即
`set_next_run` + `TaskEnd`，不进任何页面/OCR/业务动作。④ candidate 落入静默窗口 → 顺延到
**窗口结束 + 随机 5~30 分钟恢复 jitter**（`quiet_resume_jitter_min/max`，`normalize_for_quiet_window`
纯函数）。⑤ 短期失败（5 次未找到合格卡 / 连续 3 次蹭卡失败 / 跨区+同区都无达标候选）next_run 改为
**随机 cooldown 5~30 分钟**（`cooldown_min/max`，取代旧 5/10/20 分钟硬编码），统一走
`_schedule_retry` 单一 owner；**由 scheduler `set_next_run(target=..., server=False)` 实现，未用任务
内部 blocking sleep**。**正常寄养（OCR 剩余时间 + `min_run_interval` 地板）逐字保留，未被 cooldown
取代**，只是接入同一次静默窗口归一化。`cooldown`/`quiet jitter` 不会叠加两次（单向 `candidate →
normalize → set_next_run` 管线，D023）。

**业务精简部分（①②）已改为"配置开关控制"落地完成（2026-09-11，`docs/AI_CONTEXT.md` §4.65 /
`docs/DECISIONS.md` D024，非删除）**：最终定型为"旧能力保留、加开关默认可关/可开"而不是删代码——
① 是否领取寄养奖励：**沿用既有** `UtilizeConfig.utilize_harvest: bool = True`（`run()` 内
`if con.utilize_harvest: self.check_utilize_harvest()` 本就已经是配置驱动，未新增重复字段、未改
默认值）；② 是否自动检查满级并替换：**新增** `UtilizeConfig.auto_replace_max_level: bool = False`，
`run()` 唯一调用点改为 `if con.auto_replace_max_level: self.check_max_lv(con.shikigami_class,
con.auto_fill)`——`check_max_lv()` / `unset_shikigami_max_lv()` / `switch_shikigami_class()` /
`set_shikigami()` 全部原样保留，默认关闭。**OASX 前端零修改**——READ ONLY 确认 OASX 任务配置面板
完全 schema-driven（`ArgumentView` 按后端 `model_json_schema()` 的 `type` 字符串自动渲染控件，
`'boolean'` → `Checkbox`），新增 `bool` 字段无需改一行前端代码，详见 D024。

`KekkaiActivation` 的静默窗口 / cooldown 未纳入本轮范围，若后续需要可复用同一套
`tasks/KekkaiUtilize/scheduling.py` 纯函数思路（不 import `script.py`，独立实现），但需要为
`KekkaiActivation` 单独决定配置落点与出口接入方式。

### Navigation READ ONLY 审查

**只读**审查「突破 → 式神录」的异常点击链（RealmRaid / 各副本切御魂时 `goto_page(page_shikigami_records)` 前后是否有误点 / 多余点击 / 回不去的路径）。产出问题清单，不改代码。

### 活动体力 / 门票挑战 FIRE + Fatigue

**普通爬塔线（`ActivityShikigami.NormalClimbAct`）已于 2026-09-09 §4.62 完成 Level A/B**（`docs/AI_CONTEXT.md` §4.62、`docs/DECISIONS.md` D001「§4.62」补记、`docs/DEVELOP_LOG.md` 同名条）：
① 活动入口改本期 `I_MAIN_GOTO_ACT_2` + 旧图标回退 + `REACTION_NAVIGATION`；**①b（2026-09-10）二层爬塔入口 page graph：`page_act --I_TO_BATTLE_MAIN--> page_climb_main（新中间页，positive = 该页稳定按钮 I_TO_BATTLE_MAIN_2）--I_TO_BATTLE_MAIN_2--> page_climb_ap/pass`；climb 页 recognizer 加 `any_of(I_CHECK_BATTLE_PASS_2 primary, I_CHECK_BATTLE_PASS legacy)` 仍 `all_of` 带 mode；未改 `page_act` recognizer/priority/threshold**；② 稳定挑战 / FIRE `REACTION_FIRE=(0.4,0.8)`；③ positive battle confirmation = 窄 `_is_active_battle_entry()`（不含 result/reward/活动结算弹窗 marker）；④ bounded retry `ACTIVITY_FIRE_MAX_TRIES=4`；⑤ finite timeout `ACTIVITY_FIRE_TIMEOUT=12` / `ACTIVITY_FIRE_POST_CLICK_TIMEOUT=4`；⑥ transition unknown 三态（`battle`/`ready`/`unknown` + `_wait_climb_fire_state` `'timeout'`）；⑦ 资源不足 = FIRE 前 OCR（本期无 disabled 按钮 / 无资源弹窗 / 无购买弹窗，不建假 marker）；⑧ 门票不足同 ⑦；⑨ 次数耗尽 = `ActivityResourceNotEnough` 优雅停 + 日志 `Climb resource exhausted: <type>`；⑩ 活动结算弹窗 = 有界 `_drain_activity_settlement` 复用 GeneralBattle `C_RANDOM_DEFAULT`（未改 Settlement V3）；⑪ Fatigue Safe Point = `_activity_challenge_safe_break`（「稳定挑战页 + Challenge Ready」，唯一 `try_fatigue_break`，`random_sleep` 让位）；⑫ cycle complete = 「结算处理完 + 回稳定挑战页 + Challenge Ready positive」后 revalidate。新增 `tests/test_activity_shikigami_climb.py`（46），回归 `1357 → 1403`。**`ACTIVITY_FIRE_*` / `ACTIVITY_SETTLEMENT_*` PROVISIONAL —— Level C 待验，见下方 Level C 区。**

**未做（不在本轮）**：大富翁（`RichManAct`）/ 伪神降临（`FakeGodAct`）的 FIRE / Fatigue（各自资产未重截、玩法结构不同，不机械套三态模板）；`BaseAct.before_run` 的 `page_battle_result` recognizer 全局 monkeypatch 收敛（P1 技债）；活动结算弹窗是否应升级为 GeneralBattle 通用新 result layout（待 Level C 证据）。

### GeneralBattle repeated / settlement micro-timing

**result advance / reward repeated advance / settlement micro-timing 已完成 Level A/B
（2026-09-12，`docs/AI_CONTEXT.md` §4.66→§4.67 / `docs/DECISIONS.md` D025，Settlement
Micro-Burst v1 → v1.1）**：结算点击从「固定两次点击 / 每次重新随机点位」升级为 Settlement
Click Session——总点击预算随机 2~4 次（上限非必须点满）、单次 micro-burst 硬上限 2 次且同一
anchor、状态合法前向跳级、anchor persistence 服从安全区域交集（全 session 最多 1 次主动
换点）、离开结算立即销毁 session（防止点击穿透到 Challenge/FIRE 页面）。**v1.1（同日修订）**
把「burst 内第二下」从「预抽 burst_size==2 就无条件盲点」收紧为 Observed Micro-Burst——第一下
点击后必须先 `fresh screenshot` + 复用既有 `GameUi.detect_page_in` 做一次语义观察，仍是同一
结算 state 才允许补第二下；观察到已推进到另一个结算页 → 当前 burst 结束、不换旧 state 补点；
观察到已离开结算（Challenge/FIRE 相关页）→ 立即 teardown；观察到 Unknown → 不补点也不
teardown。**Settlement V3 仍保持自己独立的 timing owner**——`SETTLEMENT_BURST_CLICK_INTERVAL_RANGE`
不接 `confirm_delay` / reaction timing / Fatigue，v1.1 只是把这个既有常量的语义从「两次盲点
间隔」收紧为「点击→观察前等待」，没有新增第二个 owner；旧固定两次 `_advance_generic_result`
已移除，不存在旧逻辑与新 session 双 owner 叠加。**剩余专项**：`prepare` timing / repeated
click 仍按原计划留在「暂缓任务」T6-3，本轮未动。Level A/B 已收口，Level C 待真机（见 D025 /
`docs/AI_CONTEXT.md` §4.67 的 8 项清单）。

---

## P1

### T4-1 List Changed / Stable 独立组件

状态：**已完成组件本体 + 单测；当前经 FrameWait 间接服务 3 个生产 consumer，均有各自 Level C 标定项**
优先级：P1
结论：
- 新增 `module/atom/frame_state.py`：`frame_difference(a, b, roi, pixel_threshold) -> float`（ROI 内变化像素占比 `[0,1]`）+ `FrameStateDetector`（`reset` / `update -> FrameStateResult(changed, stable, difference, stable_count)`）。纯 numpy，零设备 / 任务依赖，不截图 / 不 sleep / 不轮询。当前由 `module/base/frame_wait.py` 服务 Kekkai K3、`BaseTask.list_find`、Exploration 章节 swipe 三个生产 consumer。
- `tests/test_frame_state.py`：25 用例。语义固定见 `docs/DECISIONS.md` D010。
- 完整回归 191 → 216。
剩余（Level C，待真机）：真实列表 ROI / `changed_threshold` / `stable_threshold` / `stable_frames` / 截图轮询 interval / timeout 的确定，以及接入任一任务并验证效果——见「需要真机验证的任务」。设备无关的「取帧 + 轮询 + timeout」外壳已由 T4-2（`module/base/frame_wait.py`）提供，剩下的是喂真实 `device.screenshot` + 真实参数。

### T4-4 `harvest_card` State → Action → Verify 局部落地

状态：迁移前静态收口已完成（见「已完成」/ `docs/Kekkai状态机静态收口.md` §5），落地待 Level C
优先级：P1
前置依赖：
- BehaviorTrace v1（已完成，用于改造前后对照）
- 建议先有 T4-1（Verify 阶段可能用到「页面变化 / 卡消失」判定）
- Kekkai 状态机静态收口 + characterization（已完成 2026-09-02）——`docs/Kekkai状态机静态收口.md`
  明确当前 `harvest_card` = 无截图 8 连点、无验证、无 reward 闭环；落地的第一个未知是
  「点击后的期望状态」（弹奖励 / 按钮消失 / 跳页），需 Level C 观察
- 现状已由 `tests/test_kekkai_activation_state.py` 锁定（8 连点顺序 / 无 screenshot / 纯线性），
  作为改造前后对照基线
允许修改：
- `tasks/KekkaiActivation/script_task.py` 的 `harvest_card`（当前是无状态确认的固定 8 连点，见 `docs/机械性审查报告.md` 发现 10）
- 对应测试
禁止修改：
- `KekkaiActivation` 其他环节
- 引入完整 State Enum / FSM Engine（v1 只在该函数内局部结构化，见 `docs/DECISIONS.md` D004）
是否需要真机：
- 是（改变了 `harvest_card` 的实际点击时序 / 循环行为）——Level C
验收标准：
- `harvest_card` 每次 `appear_then_click` 前有 `screenshot`，按「扫描 → 命中 → 点击 → 验证消失 / 页面变化 → 继续」结构
- 单元测试锁住新结构（用 mock 驱动 screenshot / appear 序列）
- 真机首跑确认领取不漏、不卡

### T4-3 Wait / Retry 策略层（设计）

状态：**设计归纳已完成（2026-09-02）——结论：当前不落地任何 Policy 层**。
基于 `list_find` / Kekkai / RealmRaid 三个真实案例横向归纳（`docs/状态验证与重试模式归纳.md`），
四类职责的 owner 与「未来 bounded retry primitive 的硬约束」已固化到 `docs/DECISIONS.md` D015：

- **Verify / semantic wait**：`Task` 组合既有 `appear` / `wait_until_appear` /
  `wait_until_disappear` / `detect_page_in`——**不新增 Verifier / WaitPolicy 类**。
- **视觉结构 wait**：`module/base/frame_wait.py` 已就位，无缺口。
- **bounded retry**：三案例共同的只有约 15 行内核（计 attempts + 独立 timeout + frozen
  result）；当前**零消费者、每个接入都是 Level C、缺真实 adoption 校准形状**——**不抽**，
  等第一个真实 Level C 迁移（大概率 RealmRaid `fire()`）时对着真实消费者抽。API proposal
  见专题文档 §17。
- **recovery**：永远留 Task。

落地（把某个真实 while-retry 循环换成 primitive）仍是 Level C。禁止事项延续 D015：不在
`Control` / `minitouch` / `frame_wait` 加 retry / recovery；不新建 `RetryPolicy` /
`ActionVerifier` / `RecoveryEngine` / `GenericStateMachine`。

---

## P2

### T5-1 BehaviorTrace 覆盖 `swipe_adb` 直连路径

状态：待执行（Kekkai 侧静态收口已确认两处直连位置与参数，见 `docs/Kekkai状态机静态收口.md` §6）
优先级：P2
前置依赖：
- 与 KekkaiUtilize / KekkaiActivation 的状态驱动重构一起做（那时会重新审视这两处滑动）
- 已确认：`KekkaiUtilize.perform_swipe_action`（`swipe_adb(duration=2)`，`p2=p1.y-416`）与
  `KekkaiActivation.check_card_num`（`swipe_adb(duration=2)`，`p2=p1.y-410`，且用 stdlib
  `random.randint`）是仅有的两处直连；均绕过 `Control.swipe` 的 `distance_check` /
  `_invalidate_image_batch_cache` / trace（`control.py:189` 注释已说明 v1 不覆盖）
允许修改：
- 视方案：`module/device/method/adb.py` 的 `swipe_adb` 加一行 `record(...)`，或改调用方走 `Control.swipe`
禁止修改：
- BehaviorTrace 事件模型
是否需要真机：
- 若改调用方走 `Control.swipe`：是（滑动路径变化）
- 若只在 `swipe_adb` 加观测行：否
验收标准：
- `KekkaiUtilize.perform_swipe_action` / `KekkaiActivation.check_card_num` 的滑动能被 BehaviorTrace 记录，或明确决定不记录并从 ROADMAP 移除

### T5-3 点击分布：OASX 前端页面（点击散点 + swipe trajectory 可视化）

状态：待执行（**Backend 已全部完成**——click v1 + Swipe Trajectory Backend，2026-09-07；本项现在**只剩 OASX 前端**）
优先级：P2
前置依赖：
- click 后端 v1 已完成（`GET /stats/{script_name}/behavior/clicks`，见「已完成」）
- **swipe 轨迹后端已完成**（同一端点扩 `swipes[]` + `task`/`interaction_type`/`target` 过滤 + `available_*` metadata；schema 见 `docs/AI_CONTEXT.md` §7「BehaviorTrace Swipe schema（前端待接）」、契约见 D004 补记 + D011 补记）
允许修改：
- **仅 OASX 前端仓库 `d:\oas_xy\OASX`**（本工作区之外）：统计 → 点击分布页 → 点击散点 + swipe trajectory 折线；类型下拉（全部 / 点击 / 滑动）、target 下拉（用 `available_targets`）、task 下拉（用 `available_tasks`）；四个筛选 `date + task + type + target` 传给后端
- 本仓库后端**本轮之后不需要再改**（除非前端联调发现 schema 缺口，再回来 additive 补）
禁止修改：
- BehaviorTrace 事件模型（仍 `ACTION` / `TASK`；swipe 已是「一个 ACTION + 嵌入 trajectory」，不得逐 MOVE）
- 加后台聚合 / 数据库 / SSE（见 `docs/DECISIONS.md` D011）
- 破坏 `points` / `tasks` / `summary` 旧计数的兼容契约
是否需要真机：
- 否（后端逐行读 JSONL 可静态验证；前端需真实运行数据才好调，但不改 OAS 行为）
验收标准：
- OASX 页面能请求后端并按 `date + task + interaction_type + target` 过滤
- 点击散点与 swipe trajectory 分开渲染：click 之间的时序连线 ≠ 一条 swipe 内部的 MOVE path（两种数据结构不混）
- swipe trajectory 用原始 1280×720 坐标 + 现有 click 坐标变换绘制
- 旧数据（无 swipe / swipe 无 trajectory）不报错

### T5-2 `BaseTask.list_find` 翻页后的状态等待

状态：**前半（静态收口）已完成；后半（FrameWait 接入）2026-09-08 已落地 Level A/B，参数标定待真机（Level C）**
优先级：P2
前置依赖：
- T4-1（List Changed / Stable 组件）
- T4-2（`module/base/frame_wait.py`，翻页后的「取帧 + 等稳定 + timeout」外壳已就绪，接入时传 `self.device.screenshot` + 真实参数即可）

**前半（已完成，会话内称 T4-3）**：`list_find` 真实控制流逐行审查、全部 8 处生产调用方 + 2 个包装入口（`list_appear_click`）清单、语义等待 vs 视觉结构等待分类、`tests/test_list_find.py`（22 characterization 用例，锁住截图 / 识别 / swipe / sleep 时序、退出语义、返回值形态、异常透传）。**结论：不抽 seam**——理由见 `docs/DECISIONS.md` D013（未来链路要求动作前 baseline 显式传入，而 `list_find` 现有动作前帧是为识别而截；真实 ROI / 阈值 / timeout 全是 Level C，seam 签名此刻无法定稿）。`list_find` 生产行为零改动。详见 `docs/AI_CONTEXT.md` §4.19。

**后半（FrameWait 接入，2026-09-08 已落地 Level A/B）**：`list_find` 翻页 `sleep(random.uniform(0.8, 1.3))` → `wait_for_changed_and_stable(settle_baseline, self.device.screenshot, roi=_list_roi_back_to_box(target.roi_back), *模块级 `_LIST_FIND_SETTLE_*` provisional 阈值*)`。**FrameWait 返回值丢弃、不参与控制流**——settle 成功或 timeout 都回循环顶重新识别，`max_swipe` 仍是唯一收敛边界，**不新增 BOTTOM / ABORT / 提前返回**（W1 结构性 settle；见 `docs/DECISIONS.md` D013 补记 / `docs/AI_CONTEXT.md` §4.49）。`settle_baseline = self.device.image`（`list_find` 循环里 swipe 前未再截图、干净，无需重截）；ROI 用每个 RuleList 自己的 `roi_back`（无全局 ROI）；`RuleList.swipe_pos` 翻页几何未动。`tests/test_list_find.py` `22 → 27`。
**剩余 Level C（参数标定，非阻断）**：
- 用真实列表翻页的 `changed` / `stable` / `elapsed` 分布标定 `_LIST_FIND_SETTLE_*`（`changed 0.02 / stable 0.01 / stable_frames 2 / timeout 1.5s / poll_interval 0.12` 都是 provisional）
- 在多个 `list_find` 消费任务（Orochi / FallenSun / WeeklyPurchase navbar / navigator / Dokan …）上验证不漏找 / 不误翻、是否比旧固定 sleep 更快
- KekkaiActivation / KekkaiUtilize 的 `perform_swipe_action`（直连 `swipe_adb`）是平行实现，一并迁移单独评估（见「已完成」表全仓 Swipe 迁移的 future candidate）

### T7-1 点击空间模型 v2：Safe ROI + preferred center + 混合分布

状态：**第 1、2 阶段 + Phase A + T7-2 + T7-3 / T7-3.1 + T7-3.2 + T7-4 已完成**。第 1 阶段 = `ClickSampler` + `LEGACY_UNIFORM` 等价接入（§4.24）；第 2 阶段 = `ClickProfile` + `HABIT`/`STRICT`/`UNIFORM` + Safe ROI 能力（§4.25），新策略零生产消费者，回归 381→425。**Phase A / Contract v2（2026-09-02，§4.28）已被 SETTLEMENT CONTRACT V3 取代（2026-09-03，§4.3.1 / §4.39 / D016）**：`GeneralBattle` 通用结算不再用 `C_RANDOM_RD` + HABIT profile。V3 = 三个 Large Safe Region（`C_RANDOM_DEFAULT` / `C_RANDOM_SAVE_RIGHT` / `C_RANDOM_SAVE_BOTTOM`）整 ROI 均匀（改回 `LEGACY_UNIFORM`）+ **Generic Result 强制两次点击序列** `_advance_generic_result`（用户实测业务事实）+ **Reward layout-aware region policy**（`I_GET_BATTLE_REWARD` marker 命中 → DEFAULT，否则 80% RIGHT / 20% BOTTOM）。`is_win` / `_exit_matcher` / missing fallback / FSM / `random_click()` 均未改；仅改 `general_battle.py`，回归 741→759，仍需 Level C MuMu 真机验收。**因此 GeneralBattle 结算已不再是非 LEGACY 生产 opt-in——当前非 LEGACY opt-in 只剩 `RyouToppa.C_AREA_1`（T7-3.2）。****T7-2（2026-09-02，§4.29）= Point Target 连续尺寸适配**：`module/click_profile.py` 的 `point_size_factor` + `adapt_point_profile`（Semantic Base Profile + 运行时 ROI → EffectiveProfile，只调 `preferred`，smoothstep 锚点 24/96），取代离散尺寸分类。**零生产消费者**，回归 559→581。**T7-3 / T7-3.1（2026-09-02，§4.30）= 首批 opt-in 候选审计 + Point Semantic Profile 数据基线收口**：`wide_card` preferred 按真实 `RyouToppa.C_AREA_1` RuleClick ROI 基准从 `(0.68,0.53)` 收口为 `(0.58,0.59)`（旧值坐标基准是手工估的大卡片区）；`normal_button` 缺 runtime ROI、`large_area` 是 Region 混合来源，两者不重估、补 `provisional`；`DEFAULT_PROFILES` 8 条全部 `provisional=True`。sigma/weights/margin 全未改，`provisional` 纯文档标记。+3 用例（65→68），回归 581→584，零生产行为变化。**T7-3.2（2026-09-02，§4.31）= 首个普通 Point Target 生产 opt-in**：`RyouToppa.C_AREA_1` → `ClickSampler.sample_point` → `wide_card` + `adapt_point_profile` + HABIT，逐调用点身份守卫，`C_AREA_2..8` 不迁移。当前 ROI 够大 → 热点不收缩。+25 用例，回归 584→609。**Point Target 生产消费者 0 → 1，仍待 Level C**。**T7-4（2026-09-02，§4.32）= Point Target spread 连续尺寸适配**：`adapt_point_profile` 用同一个 `size_factor` 也把 `core/medium sigma` 向「`tiny` 的 σ 锚点」插值、`tail` 向 0 收（削掉的回补 `core_weight`，`medium_weight` 不动），未新增第二条尺寸曲线、`safe_margin` / `max_attempts` / `provisional` / `name` 仍原样透传。端点精确：`f=1`（`short_side ≥ 96`）逐字段恢复 base → `C_AREA_1`（ss=116）生产落点分布不变；`f=0`（ss≤24）σ=锚点、tail=0。+19 用例，回归 609→628，零生产行为变化。**第 3 阶段**（挑其余 wide_card / normal_button 语义代表目标显式 opt-in）待做，各自前置是**真实 ROI / 采样数据**（`normal_button` 需真机 `runtime_roi_probe --target I_FIRE`），不阻塞于 T7-4。**Phase B / C**（`BondlingFairyland._handle_result`、`SixRealms/{moon_sea,peacock_kingdom}._handle_reward`、Exploration / MetaDemon task-private、两个 page hook 的结算迁移）待做。**GeneralBattle Settlement RD2 未来触发条件已冻结**（见下方「需要真机验证的任务」）。
优先级：P2
前置依赖：
- 点击空间机制审查（已完成，见「已完成」/ `docs/AI_CONTEXT.md` §4.20）
- ManualClickRecorder v1（已完成，采人工习惯样本）
- 人工点击日志 burst-collapse 分析器 `dev_tools/manual_click_analyze.py`（已完成；`docs/AI_CONTEXT.md` §4.22。2026-09-02 补：`landing_stats` 的 ROI-relative 统计已改为只用 ROI 内落点，外点单独报告不污染）
- 只读 `dev_tools/runtime_roi_probe.py`（已完成，`docs/AI_CONTEXT.md` §4.23）——用来采真进攻按钮 `I_FIRE` 的运行时 ROI；**待用户接真机手动跑一次**（进攻弹窗出现后 `toolkit\python.exe dev_tools\runtime_roi_probe.py --config yys1 --target I_FIRE`，建议连采 5~10 次弹窗看 ROI 是否稳定）
- 大尺寸点击 ROI 人工审查目录 `dev_tools/large_click_roi_review.py`（工具已完成，`docs/AI_CONTEXT.md` §4.27）——**用户逐项人工确认仍待做**：按 `log/click_roi_analysis/large_click_roi_review.md` 的编号 001~198（先看「建议优先查看的前 20 个」）确认每块区域的语义、ROI 是否合理、走 Uniform 还是个人热点、是否需要重新截图；确认结果把 `review_status` 从 `pending` 改成 `confirmed_button` / `confirmed_safe_region` / `needs_reframe` / `keep_uniform` / `use_profile` / `unused`。这是第 3 阶段挑代表目标的直接输入。注：`C_RANDOM_RD` / `C_RANDOM_RD2` 已随 SETTLEMENT CONTRACT V3（§4.3.1 / D016，2026-09-03）从生产移除；新的 `C_RANDOM_DEFAULT` / `C_RANDOM_SAVE_RIGHT` / `C_RANDOM_SAVE_BOTTOM` 是 V3 结算消费者（整 ROI 均匀）；`C_GREEN_MARK_AREA`(1280×720) / `C_RANDOM_ALL`(1207×543) / `C_FG_RANDOM_*` 全套仍零消费者，待人工定夺删除或保留
- 点击 ROI 尺寸静态普查 `dev_tools/click_roi_inventory.py`（已完成，`docs/AI_CONTEXT.md` §4.26）——第 3 阶段挑代表目标 / 定 tiny·small profile 尺寸门槛的数据依据。结论：confirmed_clickable 子集 short_side p05≈21 / p25≈40 / median≈55 / p90≈110；**候选边界 Tiny<24 / Small 24–40 / Normal 40–96 / Large≥96（数据推导，未采纳、未落地任何默认行为）**，中间分界更接近 40 而非人为的 48；窄条按钮（`C_QUIT_AREA` / `C_ANSWER_ENSURE_*` / `C_BUY_MORE` 等）需按语义单独指定，不能只按 short_side 归类
- BehaviorTrace 补 `page` 身份 + `ROI` 几何字段（未做——学习 per-target profile 的前提，存量不可回填）
- 真机 + 用户人工采样数据（首个样本 `yys1_2026-09-01` 已分析并经 k-means 三簇核实：`C_AREA_1` 目标卡片——cluster_A 107 个 burst 独立落点，**ROI-relative 数值依赖 `C_AREA_1` ROI 定义版本**：对当前 WIP ROI `(514,141,223,116)` 是 inside 105/107、`mean_uv≈(0.582,0.586)`；对 HEAD ROI `(533,162,177,74)` 是 inside 101/107、`mean_uv≈(0.627,0.622)`（T7-3.1 已据此把 `wide_card` preferred 收口为 `(0.58,0.59)`，§4.30）；真进攻按钮 `cluster_B` 只有屏幕归一化 `(0.53,0.55)`，**没有可靠 runtime `I_FIRE` roi_front，ROI 待 `runtime_roi_probe` 真机采集后用 `--three-cluster --roi x,y,w,h` 重算**——`normal_button` preferred 在此之前保持 `provisional` 不重估；Large Area 热点≈`(0.74,0.67)` 是屏幕空间混合来源、Region 语义、不作 Point profile；42.5% raw click 是连点冗余——**实验结果见 `docs/DEVELOP_LOG.md` 2026-09-01 / 2026-09-02 条目，非长期标准**）
允许修改（分阶段，每阶段行为等价可回退）：
- ~~抽「统一点击采样入口」：默认实现 == 现 `random_point_in_roi`~~ ——**第 1 阶段已完成**（`ClickSampler.sample` + `LEGACY_UNIFORM`，2026-09-02）
- ~~采样入口内加 Safe ROI（按尺寸比例内收 padding）+ `ClickProfile`/`ClickProfileManager` + HABIT/STRICT/UNIFORM core-medium-tail 混合分布~~ ——**第 2 阶段已完成**（2026-09-02，新策略零生产消费者，默认仍 `LEGACY_UNIFORM`）
- ~~Phase A：GeneralBattle 通用 result/reward 从 `random_click()` 换成 `C_RANDOM_RD` timer 驱动重复点击~~ ——**已被 SETTLEMENT CONTRACT V3 取代**（2026-09-03，§4.3.1 / §4.39 / D016）：RD/RD2 + HABIT profile 移除，改为三个 Large Safe Region 整 ROI 均匀 + Generic Result 强制两次点击序列 + Reward layout-aware region policy。仅 `general_battle.py`，回归 741→759，仍需 Level C 真机验收
- ~~T7-2：Point Target 连续尺寸适配（`adapt_point_profile` / `point_size_factor`，取代离散尺寸分类）~~ ——**已完成**（2026-09-02，§4.29；`module/click_profile.py` 纯计算，零生产消费者）
- ~~T7-3 / T7-3.1：首批 opt-in 候选审计 + Point Semantic Profile 数据基线收口（`wide_card` preferred 按真实 ROI 基准收口、`DEFAULT_PROFILES` 全标 `provisional`）~~ ——**已完成**（2026-09-02，§4.30；纯数值 + 元数据，零采样行为变化）
- ~~T7-3.2：首个普通 Point Target 生产 opt-in（`RyouToppa.C_AREA_1` → `ClickSampler.sample_point` → `wide_card` + `adapt_point_profile` + HABIT，逐调用点身份守卫）~~ ——**已完成静态**（2026-09-02，§4.31；Point Target 生产消费者 0→1，`C_AREA_2..8` 与其它一切不变，回归 584→609）。**仍待 Level C 真机验收 `C_AREA_1` 真实落点分布**
- ~~T7-4：`adapt_point_profile` 加 spread / sigma 的尺寸适配（同一个 `size_factor` 也调 `core/medium sigma` → `tiny` σ 锚点、`tail` → 0，`safe_margin` 不变）~~ ——**已完成**（2026-09-02，§4.32；纯 `ClickProfile` 数学，`f=1` 逐字段恢复 base → `C_AREA_1` 生产落点不变，回归 609→628）
- ~~第 3 阶段（其余代表目标逐调用点 opt-in）~~ ——**被 T7-5 取代**（2026-09-04）：不再逐调用点 opt-in，改为全局默认每个 target 经 `sample_target` 拿 preferred（未标定 → `CENTER_FALLBACK`）。见「已完成」T7-5 行 / `docs/AI_CONTEXT.md` §4.44 / D014 T7-5 段
- ~~**T7-5 Stage 2**：① `device.click` 旁路迁移；② GeneralBattle V3 区内采样；③ `normal_button`/`I_FIRE` empirical~~ ——**②（GeneralBattle 区内采样 → `sample_region`）+ ① 里有安全 ROI 的部分（`GeneralInvite._random_point_in_area`）已完成（2026-09-07，见「已完成」T7-5 行 / `docs/AI_CONTEXT.md` §4.45）。剩余 Level C（不算架构迁移，属 calibration / 逐点评估）：**
  - **T7-5 empirical calibration（Level C）**：`normal_button` / `I_FIRE` 用真机 `runtime_roi_probe --target I_FIRE`（3 个不同 asset 各自）+ manual click cluster_B 重算 inside/mean/median/std/percentile，可靠才登记 EMPIRICAL（不硬造）；GeneralBattle 三个 SAVE 区用 BehaviorTrace + OASX 散点标定后可就地把 registry 条目升 EMPIRICAL。
  - ~~T7-5 Stage 2 收尾（`Secret`）~~ ——**已完成（2026-09-07）**：`Secret.find_battle` 关卡卡片 `click_rule.center` ×2 → `for click_index in 1..2: click_rule.coord()` → `sample_target`（两次独立采样）+ `SecretLayerCardMigrationTest` 4 用例。**T7-5 Stage 2 = IMPLEMENTED。**
  - **后续任务 A：Empirical Hotspot Calibration（Level C，非 Stage 2 blocker）**：`I_FIRE` / `normal_button` 用真机 `runtime_roi_probe --target I_FIRE`（3 个不同 asset 各自）+ manual click cluster_B 重算，可靠才登记 EMPIRICAL（不硬造）；GeneralBattle 三个 SAVE 区用 BehaviorTrace + OASX 散点标定后就地把 registry 条目升 EMPIRICAL；其它 target 的 `CENTER_FALLBACK → EMPIRICAL`。
  - **后续任务 B：Exact Coordinate ROI Discovery（Level C，非 Stage 2 blocker）**：`docs/T7_TARGET_PREFERENCE_MAP.md` §3.4 的 23 处里个别「有运行时 bbox 但点角点 / 偏移」的（`GeneralBattle.green_mark_name` OCR 角点 +5/+30、`Dokan:376` bounty bbox 左上角、`QuickLoadout` panel 相对偏移）—— 真机确认安全 ROI 存在且改分布不打断业务后再迁；其余（点检测中心 / 硬编码 / 识别失败兜底猜测点 / slave 设备）无安全 ROI，保留 exact。
- Phase B：`BondlingFairyland._handle_result`、`SixRealms/{moon_sea,peacock_kingdom}._handle_result` / `._handle_reward` 末尾的 `random_click()` 换成 CONTRACT V3 的 `_settlement_click(context, region=...)` / `_advance_generic_result`（保留各自 special 链；这些当前全私有不 `super()`，迁移是有意接入而非穿透）
- Phase C：`Exploration` / `MetaDemon` task-private 结算循环、`default_pages.py` 的 `page_battle_result` / `page_reward` enter-success hook 的 `random_click()` 迁移（需先厘清为何绕开 GeneralBattle）
禁止修改：
- `random_point_in_roi` 的均匀语义（除非本 ADR 做「有意 supersede」说明，见 `docs/DECISIONS.md` D014）
- `Control` / minitouch / `CommandBuilder.convert` 加点击空间抖动（D002 / D014）
- 放大任何现有 ROI（只可内收）
- `C_RANDOM_*` 安全大区（需求是散开，不套 preferred center）、超小目标、登录固定点
是否需要真机：
- 抽采样入口 + Safe ROI + 单测：否（行为等价可静态验证）
- 引入 preferred center / 非均匀分布：是——Level C，且需先有人工采样数据标定
验收标准：
- 默认 / 关闭路径逐字等价于当前 `random_point_in_roi`（characterization 护栏）
- preferred center 只在独立 manager，`Rule` / `Control` / `Task` 不承载学习状态
- 分布形态「多数集中 + 少量中等 + 极少外围」，尾部 σ << ROI 半宽，全部 clamp 在 Safe ROI 内

---

## 暂缓任务

### T6-1 `module/atom/cBezier.py` 孤儿文件是否删除

状态：暂缓
优先级：P3
说明：`RuleSwipe.trace()` 删除后（T3-2），`module/atom/cBezier.py` 全仓无消费者。`module/base/cBezier.py` 是另一个文件、仍被 `windows_impl.py` 使用。删除孤儿模块超出「清死路径」边界，留作独立决定。低收益，不急。

### T6-2 `Script._publish_fatigue_state` 2 秒轮询线程

状态：暂缓（记录为 P2 架构审查项）
说明：`script.py` 的 `_publish_fatigue_state` 是独立 daemon 线程，固定 `sleep(2)` 无退出条件，禁用疲劳时仍持续推送。与「前端复用现有状态刷新链路、不单独增加高频轮询」的意图有出入。需要连同 OASX 前端一起评估，非阻断。

### T6-3 GeneralBattle 后期处理

状态：暂缓
说明：`docs/机械性审查报告.md` 指出 `GeneralBattle` 基类 `PREPARE_CLICK_DELAY_RANGE = (3.0, 3.0)` / `SETTLEMENT_CLICK_INTERVAL_RANGE = (0.8, 0.8)` 是退化区间。是否调整属于「改变所有战斗任务默认时序」，需真机 + 逐任务评估，且受 `docs/DECISIONS.md` D008（不无依据重写 FSM）约束。当前只记录，不动。

---

## 需要真机验证的任务（Level C，真机窗口打开后再做）

- **ActivityShikigami 当期普通爬塔线适配 Level C 验收**（2026-09-09 §4.62 已实施 Level A/B，见「已完成」表 / `docs/AI_CONTEXT.md` §4.62 / `docs/DECISIONS.md` D001「§4.62」补记；`ACTIVITY_FIRE_*`=`(4,12,4)` / `ACTIVITY_SETTLEMENT_*`=`(6,15)` 全 PROVISIONAL，不得标 VERIFIED）。用户手动跑普通爬塔连续 **5~10 轮**，重点观察：① 二层导航链 `庭院 main_goto_act_2 → page_act → I_TO_BATTLE_MAIN → page_climb_main（中间页，positive = I_TO_BATTLE_MAIN_2）→ I_TO_BATTLE_MAIN_2 → 真正爬塔页（I_CHECK_BATTLE_PASS_2 + mode）` 跑通、无 unknown recovery 退回庭院、climb 页 yellow-back 走 `climb → climb_main → activity` 正确、boss/ap100 是否也有中间页；② Challenge Ready 识别 + Fatigue **只在**挑战 ready 时触发、休息后 fresh revalidate（页 / 挑战键 / 资源 OCR）；③ FIRE reaction 0.4~0.8、不重复 FIRE、窄 `_is_active_battle_entry()` 不把活动结算弹窗 / 上一场结果误判为新战斗；④ 活动结算弹窗 `_drain_activity_settlement` 用 `C_RANDOM_DEFAULT` 点击安全、`ACTIVITY_SETTLEMENT_MAX_CLICKS=6` 是否够、有界回到挑战页；⑤ 资源 OCR 新 ROI（`O_REMAIN_AP` `(551,14,119,33)` / `O_REMAIN_PASS` `(752,16,97,31)` / `O_REMAIN_PENTA_PASS`）读数正确、resource 0 后不再 FIRE 优雅停（日志 `Climb resource exhausted: <type>`）；⑥ 无无限循环（fire 键一直不出现 / transition 卡住 / ~1s 提示文字一闪 都有界退出）；⑦ `ACTIVITY_FIRE_TIMEOUT=12` / `POST_CLICK_TIMEOUT=4` 是否够覆盖本期战斗加载。**未接大富翁 / 伪神降临**（各自资产未重截）。据真机日志调 `ACTIVITY_FIRE_*` / `ACTIVITY_SETTLEMENT_*`（不在静态轮次凭感觉改）；若确认活动结算弹窗其实是 GeneralBattle 通用新 result layout，再单独评估是否收敛进 GeneralBattle。
- **ActivityShikigami 活动入口导航停滞真机复现**（2026-09-22 发现 + 已最小修复，`docs/AI_CONTEXT.md` §4.89）：真机日志证实入口图标 `goto_activity_entry` 曾一次性零命中、落入旧回退 `find_activity_entry`，5 次栏目切换点击后进程停滞约 35 分钟直至外部重启，期间无任何日志。已加固：点击后新帧立即复用判断（不重复截图）+ 墙钟上限 `ACTIVITY_ENTRY_FALLBACK_TIMEOUT=20s` 与原次数上限 `8` 并列。**用户需连线真机复现观察**：① 这次入口图标零命中的具体原因（活动图标是否变化 / 庭院栏目停留位置）；② 加固后再次触发回退时能否在有界时间内恢复或明确报错退出，而不是再次静默挂起（若仍挂起，说明卡点在某次同步设备调用内部，需要另立专项排查设备层，不在本轮范围内）；③ `ACTIVITY_ENTRY_FALLBACK_TIMEOUT=20s` 是否合适。
- **ActivityShikigami 战后结算兜底乱点 + 第二次静默停滞真机复现**（2026-09-22 发现 + 已最小修复，`docs/AI_CONTEXT.md` §4.90）：真机日志证实 `_drain_activity_settlement` 把「已在真实挑战页 `page_climb_pass`、挑战键还没渲染完」误判成「还有残留弹窗」连点 6 次 `random_default`；点击预算耗尽的警告之后，`_run_climb_type` 的 `current_page is None` 分支（原来只有 `sleep(0.5); continue`、无上限）导致进程静默空转约 **66 分钟**直至外部重启，期间无任何日志——与上一条是**同一症状（静默停滞直至外部重启）、不同触发点**。已加固：`_drain_activity_settlement` 命中 `destination` 但挑战键未就绪时复用既有 `_wait_climb_fire_state` 等待而非盲点（事故复现用例验证盲点次数从 6 次降为 0 次）；`_run_climb_type` 新增 `ACTIVITY_CLIMB_UNKNOWN_PAGE_TIMEOUT=20s` 墙钟，持续无法识别页面超时后 `raise GamePageUnknownError`。**用户需连线真机复现观察**：① 加固后是否还会出现这次 66 分钟静默停滞（若仍挂起，说明卡点在某次同步调用内部无法被任务层 Timer 中断，需要另立专项排查，不在本轮范围内）；② `_wait_climb_fire_state` 等待分支在真机上的耗时是否合适（沿用既有 `ACTIVITY_FIRE_POST_CLICK_TIMEOUT=4s`，未单独标定）；③ `ACTIVITY_CLIMB_UNKNOWN_PAGE_TIMEOUT=20s` 是否合适；④ `fake_god.py` 里与两次事故同族的其它潜在无界循环本轮未逐一排查，是否需要单独立项。
- **ActivityShikigami 普通爬塔专用结算单击**（2026-09-23 已实施，`docs/AI_CONTEXT.md` §4.91 / `docs/DECISIONS.md` D025 补记）：取消本线对 GeneralBattle Micro-Burst 的参与，换成每次只点一次、70% `C_RANDOM_ACTIVITY_1` / 30% `C_RANDOM_ACTIVITY_2`、每次都重新 reaction + fresh confirm 的专属结算。reaction 由本线自己的 `ACTIVITY_SETTLEMENT_REACTION = (0.45, 0.85)` 持有（同日收口，不再借用 GeneralBattle 的 burst observe 间隔；后者已在同日后续一轮独立调整为 `(0.30, 0.60)`，两套 timing 各调各的，互不牵连）。**用户需连线真机复现观察**：① 70/30 权重与 reaction 区间 `(0.45, 0.85)` 是否合适（偏短会显得机械、偏长会拖慢刷取节奏）；② 两处新区域点击是否安全，不会误触活动结算页面上的其它控件；③ 观察是否还会出现 §4.89 / §4.90 那类「弹窗消失后打到底层页面」的现象（若仍出现，说明问题不只在 anchor 复用，需要进一步排查）。
- T4-1 / T4-2 后半：`frame_state` + `frame_wait` 的真实参数与接入——真实列表 ROI、`changed_threshold`、`stable_threshold`、`stable_frames`、截图轮询 interval、timeout；以及先开 BehaviorTrace 采集旧行为 baseline、采集真实列表帧后再定参数、把 `wait_for_changed_and_stable` 接上 `self.device.screenshot` 并在某个任务上验证效果
- T4-4 `harvest_card` 状态驱动改造
- **T5-2 后半 `BaseTask.list_find` 翻页 settle —— FrameWait 已接入（2026-09-08 Level A/B，见「已完成」表同名条 / §4.49 / D013 补记），剩 Level C 参数标定**：用真实列表翻页的 `changed`/`stable`/`elapsed` 分布标定模块级 `_LIST_FIND_SETTLE_*`（`changed 0.02 / stable 0.01 / stable_frames 2 / timeout 1.5s / poll_interval 0.12` 全 provisional）；在多个 `list_find` 消费任务上验证不漏找 / 不误翻、是否比旧固定 `sleep(0.8~1.3)` 更快。**FrameWait 结果不参与控制流**（settle / timeout 都回循环顶重扫，`max_swipe` 仍是唯一收敛边界），所以参数取错只影响多等 / 少等、不影响正确性——Level C 是「调优」不是「验对错」
- **KekkaiUtilize 单向分区搜索 Level C 待验收**（改造已实施 → §4.41 / `docs/DECISIONS.md` D017；未真机，不得标 VERIFIED）：① `I_U_EMPTY_CARD` 是否 100% 表示「好友结界卡列表已到底」（当前是唯一 BOTTOM marker）；② ~~切换同区 / 跨区后列表是否稳定回到 TOP~~ —— **2026-09-06 Level C 已确认「只要 SAME/CROSS 发生实际切换，新进入的分组列表必定从顶部显示」**。据此标准路径每个 PASS 前从 `_reset_utilize_friend_list`（`S_U_END` 滚到底 + 切区双切）简化为只 `switch_friend_list(该分组)`（见 §4.41「PASS 前初始化简化」/ D017 补记，`test_kekkai_utilize_state` 53→62，回归 944→953）；`_reset_utilize_friend_list` 仅怠惰路径保留。**新增待复核**：标准路径进列表后不再「先把整列表拽到底」、当前分组正确时 0 tab click、每 PASS 切一次即从顶部扫描、命中率不变；③ 当前正向 ADB swipe（`SWIPE_DISTANCE=416`、`duration=2`）是否仍覆盖所有候选、不漏不重；④ card-column 结界卡 ROI 是否需要扩大（旧审查 ~400-450px vs swipe 416px，冗余小）——**本轮未捆绑，独立 Level C 项**；⑤ **标准 PASS 搜索的下划已迁 minitouch trajectory（K1，2026-09-04；见「已完成」表 + §4.41）** —— minitouch 配置下 `_perform_search_swipe` 走 `TouchSwipeModel` + `Control.swipe_trajectory`，416px 几何 / 怠惰模式 / D017 契约不动；**K1 真机验收（实际滚动量 / 惯性 / 帧间重叠 / 是否识别为滑动 / 无 fling / `find_everyone`·OCR·bottom 不受影响）= 下午 Level C**；~~小步滑动（K2）~~ ——**静态完成（2026-09-07，见「已完成」表 K2 行 / §4.41「K2」）**：标准 minitouch 路径 `SWIPE_DISTANCE` 416 → 随机 `SWIPE_DISTANCE_RANGE`（首档 `(212,265)` 首次 Level C 实测滚 >4 格 → **已回调为 `(140,180)`，需二次 Level C**）+ 轻微整体斜度 `end_x = clamp(start_x + random_int(*SWIPE_LATERAL_OFFSET_RANGE), *SWIPE_START_X_RANGE)`（`(-12,12)` provisional，不给 MOVE 加噪声、不扩大 start_x 范围，二次 Level C 一并观察 `end_x` 左右斜是否不越安全区）；~~FrameWait（K3）~~ ——**已完成（2026-09-07，见「已完成」表 K3 行 / §4.41「K3」）**：标准 minitouch 路径 swipe settle 从固定 `time.sleep(2)` 改成 `wait_for_changed_and_stable`（首个 `frame_wait` 生产消费者），失败 → `PassResult.ABORT`（`no change ≠ BOTTOM`）；**K3 真机验收**：`SWIPE_WAIT_ROI` 是否恰框滚动主体 / 4 个 provisional 阈值（`changed 0.10`·`stable 0.02`·`stable_frames 3`·`timeout 3.0s`）是否合适（看 `changed·stable·elapsed·diff` 日志分布）/ `timeout=3.0` 是否够覆盖惯性拖尾 / 正常滚动稳定判 `changed&stable`、真卡死稳定进 ABORT / 是否比旧固定 2 秒更快；~~scroll dy·新旧区域过滤（K4）~~ ——**K4-1 + K4-2 Level A/B 已实施（2026-09-07，见「已完成」表 K4 行 / §4.47 / D017 K4 补记）**：K4-1 `I_IS_SELECTED` 锚点测 `actual_scroll_dy`（≠ commanded）+ K4-2 上一屏↔当前屏投影去重、只处理 new candidate、measurement unavailable → 完整扫描、K3 失败仍 ABORT、BOTTOM 仍只认 `I_U_EMPTY_CARD`；**K4 overall Level C pending**（selected glow 连续稳定 / 0.8 无假阳性 / dy 正确 / 新卡不误去重 / 重叠 1~2 格不重复点旧卡 / center-based 出屏判定只重复不漏卡）；`new-region-only optimization`（按 dy 只裁新进入视觉区域跑识别）⏸ optional 未实现；⑥ 最后一轮扫到底后「最后一次点击的候选」是否稳定保持选中（`FINAL_USE_LAST` 依赖它）；⑦ `SEARCH_MAX_SWIPES=20` 是否够在正常好友列表滑到 `I_U_EMPTY_CARD`；⑧ 太鼓 / 斗鱼真实收益分布 + 用户可接受下限（是否调 `taiko/fish_reward_threshold` 默认）；⑨ 极低概率「列表卡种与详情卡种不一致」——本轮按当前详情 OCR 判断、未加复杂 recovery，Level C 日志证明有影响再单独加有限兜底
- **KekkaiUtilize Scheduler v1 Level C 待验收**（改造已实施 Level A/B → §4.64 / `docs/DECISIONS.md` D023；未真机，不得标 VERIFIED）：① 00:00~07:00 静默窗口内确实不会真正启动业务（观察实际任务开始时间戳，`run()` 入口 `_guard_quiet_window` 是否总能在 scheduler 意外唤醒时立即拦截）；② 正常寄养（OCR 剩余时间）调度后的 `next_run` 是否仍符合寄养实际到期时间语义（未被误接入 cooldown）；③ 短期 retry（5 次未达标 / 连续 3 次失败 / 跨区+同区无达标候选）的 5~30 分钟随机 cooldown 体感是否合适；④ `quiet_resume_jitter_min/max`（默认 5/30）、`cooldown_min/max`（默认 5/30）默认值是否需要调整；⑤ candidate 落窗顺延到窗口结束 + jitter 在真机上是否符合预期（尤其跨天场景，例如 23:xx 完成后顺延到次日 07:xx）。
- **GeneralBattle Settlement Micro-Burst v1.2 Level C 复测**（v1.1 已由真机明确判 FAIL；v1.2 已实施 Level A/B → §4.70 / D025 v1.2 修订，未真机，不得标 PASS/VERIFIED）：① 必测 `budget=2`：Result 1 click → Reward 至少 2 clicks，确认不会因旧总预算提前耗尽卡在“点击屏幕继续”；② Reward 同 state 的 segment 耗尽后能继续生成 bounded segment，日志出现 `segment exhausted/renewed` 而不是 `terminal budget exhausted`；③ Generic Result → Reward 时 Reward 获得独立 2~4 segment，Result 点击不侵占 Reward；④ semantic terminal 后立即 teardown，后续 Challenge/FIRE 无残留 settlement 点击；⑤ Unknown 不续 segment、不盲补；⑥ 永久 `page_reward` 不无限 renew，确认 `SETTLEMENT_MAX_TOTAL_CLICKS=9` 与 Device guard 的实际收口行为，并判断 9 是否过紧/过松；⑦ Reward layout marker / 80-20 safe region、anchor keep/resample、Loss 路径不回归；⑧ RealmRaid / GeneralInvite / RyouToppa / Activity FIRE 完全不受影响；⑨ mid-burst 观察间隔（2026-09-23 已由 `0.10~0.30s` 放宽为 **`0.30~0.60s`**，D025 补记）与 2/3/4 权重仍按真机体感标定——重点看放宽后是否还会出现「在旧帧上判定 same state 而补了第二下」，以及连点观感与整体结算耗时是否可接受。
- RealmRaid 核心流程（`fire` / 退四固定 4 次 `fire_again` / 页面流程）——静态收口已完成（§4.34 / `docs/RealmRaid状态机静态收口.md`）。**① `fire()` R-R1 + FIRE post-click 三态边界已实施 Level A/B（2026-09-08，见「已完成」表 / §4.52 / D001 补记 FIRE 分节 + D015 补记）**：`fire()` 改为正向战斗确认（`is_in_battle(False)`）+ 有限 `RR_FIRE_MAX_TRIES=4` + `Timer(RR_FIRE_TIMEOUT=10)` + 每 attempt 独立 `REACTION_FIRE` reaction + fresh reconfirm + 可达 `return False`。**FIRE post-click 三态**：`_wait_fire_entered_battle()` 返回 `'battle'` / `'retryable'`（`_is_realm_raid_retryable_state` = `I_RR_PERSON`/`I_FIRE`/`I_BACK_RED` 任一）/ `'timeout'`（transition/unknown —— **既不当 success 也不当 immediate failure**，timer 内持续等）；`C_PARTITION_n` 点击**必须**有明确 retryable-state 守卫，`not appear(I_FIRE)` 不再是充分条件。`run()` 的 `if not self.fire(index): continue` 现在真正生效。**剩 Level C**：`REACTION_FIRE` 0.4~0.8 体感、FIRE 一次成功率 / retry 触发率、`RR_FIRE_TIMEOUT` / `RR_FIRE_POST_CLICK_TIMEOUT` 是否够、transition blank 帧实际持续时间、是否频繁命中 `_is_realm_raid_retryable_state`（`I_BACK_RED` 在但非战斗）、partition guard 是否影响正常重开详情、false retry 是否明显减少；`R-R2`（`fire_again` 死分支）仍未做。② 给其余无界 `while`（`fire_again` / `ensure_lock` / `check_refresh`×2 / `reward_detect_click` / 呱太弹窗）与其余 `wait_until_appear`（`fire()` 已带 `wait_time`；其余仍未传）定合理超时，需先观察正常一轮各阶段耗时；③ 确认 `find_one` 对 `device.image` 的原地涂黑是否仍必要、能否改为不污染共享帧；④ 确认九宫格静态 `C_PARTITION_*` ROI 与实际勋章位置的偏差（本轮 R-R1 未动九宫格空间行为）
- **`confirm_delay`（Point Action reaction timing）—— 第一批已落地（2026-09-08，Batch A #1–#23，27 调用点 / 5 任务，见「已完成」表 / §4.51 / D001 补记）；剩 Level C 参数标定 + `*_FIRE` 系列**：① **参数标定**——`module/reaction_profile.py` 的 6 组 `REACTION_*` 区间是 provisional engineering baseline，用 `dev_tools/manual_click_recorder.py` 人工样本 + BehaviorTrace + 真机观察验证：二次 confirm 成功率 / reaction 实际分布 / 任务耗时是否明显增加 / delay 期间 target 消失导致漏点 / 高频稳定 Action 是否需 NORMAL → NORMAL_HIGH（当前 NORMAL_HIGH 0 consumer）；在拿到 Level C 证据前不放大区间。② **`*_FIRE` 系列**：**RealmRaid `I_FIRE` + RyouToppa `I_FIRE` 已于 2026-09-08 统一 `REACTION_FIRE=(0.4,0.8)` 并完成 RealmRaid `fire()` R-R1（见上 / §4.52）**——RealmRaid FIRE 现有正向战斗确认（`is_in_battle`）+ bounded retry + reaction；RyouToppa FIRE 保留成熟手写链、只统一 delay profile。**Orochi `run_alone`（`I_OROCHI_FIRE`）+ EvoZone `run_alone`（`I_EVOZONE_FIRE`）已于 2026-09-08 收口为同一三态模板（§4.53，第二批）**——`_fire_{orochi,evozone}_alone` + `_wait_*_fire_state` + `_is_*_challenge_retryable` + `{OROCHI,EVOZONE}_FIRE_MAX_TRIES=4` / `*_FIRE_TIMEOUT=10` / `*_FIRE_POST_CLICK_TIMEOUT=3`；剩 Level C = `REACTION_FIRE` 体感 / 一次成功率 / retry 触发率 / transition blank 帧时长 / `_is_*_challenge_retryable`（`I_*_FIRE` threshold 0.6）是否过宽 / Fatigue 安全节点是否真安全。**公共组队 challenge owner `GeneralInvite.click_fire()` 已于 2026-09-08 补 `REACTION_FIRE`（§4.54）+ 收口为 bounded 四态 Battle Entry FSM（§4.55：`_classify_room_entry_state` BATTLE / ROOM_FAILED / RETRYABLE_ROOM / TRANSITION_UNKNOWN，返回 `str`，`Timer(GI_FIRE_TIMEOUT=15)` + `range(GI_FIRE_MAX_TRIES=4)`、无 `while 1`，`run_invite` / 4 直接 caller 加 `== 'battle'` guard）**——11 个副本（BondlingFairyland / EternitySea / EvoZone / Exploration / FallenSun / Orochi / OtherWorldTwilight 经 `run_invite`；ExperienceYoukai / GoldYoukai / Hunt / Tako 直接调）自动继承；剩 Level C = 组队挑战按钮 0.4~0.8 体感 / leader challenge 是否漏点 / reaction 后按钮消失频率 / **challenge click → battle positive marker 实际时间** / **room marker 消失到 battle marker 出现的 blank duration** / **retryable room 实际命中率** / **room destroyed / leader gone 真实 marker 是否够** / `GI_FIRE_TIMEOUT=15` / `GI_FIRE_POST_CLICK_TIMEOUT=4` / `GI_FIRE_MAX_TRIES=4` 是否合理（全 PROVISIONAL；**`GI_FIRE_TIMEOUT` 静态复核已定性为 soft new-attempt admission budget，显式返回上界 ≈ `15 + 4.8 ≈ 19.8s`，保留 soft 不改 strict**）/ member 完全不受影响 / 多副本共用一致 / **`run_invite` 返回 `False` 更频繁是否导致任务过早退出**。**`_battle_entry_positive()` fallback 收窄（移除 `I_EXIT`）不新增 Level C 需求**——11 consumer 全走 `is_in_battle()`、fallback 是 dead code。**仍 pending**：契灵 `I_BALL_FIRE`（PARTIAL，结构特殊，见 P0）、Orochi `run_wild` 的 `I_OROCHI_WILD_FIRE`（全仓无资产定义、既有断链，见 P0）、退四「再次挑战」reaction（见 P0 RealmRaid 第 6 条）、活动体力 / 门票挑战 FIRE（见 P0）。GeneralBattle Settlement / `I_PREPARE_HIGHLIGHT` / poll 循环 / Static Region / 瞬态按钮**永不加**
- **TouchSwipeModel Level C 验收**（基础设施 + 第二阶段运动学优化均已实现 → §4.42 / `docs/DECISIONS.md` D018 + 第二阶段追加；未真机，不得标 VERIFIED）。**第一阶段图形 / MuMu 设置页已确认空间结构正确**；**第二阶段（平滑 dt + 尾段慢拖）代码已就绪，真机待执行**（2026-09-04；尾段力度已「收一档」：`tail_density_gain 1.55→1.35` / `tail_slow_gain 0.55→0.35`）。工具 `dev_tools/test_touch_swipe_mumu.py` —— 一次性、非循环，`Device(config=...)` 标准初始化 → `model.generate` 只调一次 → 同一条轨迹存 `log/touch_swipe_test/touch_swipe_<id>.{png,json}`（PNG 1400×900：**A1 Screen View 1280×720 实屏比例** + **A2 ZOOMED VIEW** + B/C dt·速度图 + D 统计；JSON 的 `asdict(model.params)` 自动带第二阶段 4 个新字段）→ 人工 Enter → `device.swipe_trajectory(..., control_name='TOUCH_SWIPE_LEVEL_C')` 执行一次 → 退出。可选 `--curve left|straight|right`（经模型已有的 `rng=` 注入点，不改生产代码）、`--start`/`--end`（默认 `520,560`→`520,300`，仅测试坐标）。`tests/test_touch_swipe_mumu_tool.py` 37 用例，零生产改动。**PNG C 面板与 stats 的 segment speed 已按 executor 时间对齐**（2026-09-04：段 `p_i→p_{i+1}` 用 `dt_i`，排除 `p0→p1` 与 `dt_last`；见「已完成」表「segment speed 与 dt 对齐修正」行）。验收环境**优先非游戏 Android 可滚动长列表**（系统设置 / 文件管理器 / 浏览器离线长页 / 专用测试 APK 或 WebView），过关后才接阴阳师。命令：`toolkit\python.exe dev_tools\test_touch_swipe_mumu.py --config oas1`（另跑 `--curve left` / `--curve right` / `--curve straight` / `--start 520,520 --end 520,400`（~120px）/ `--start 520,620 --end 520,180`（~440px））。步骤：① 设备端确实按点序 MOVE、每段 `wait(dt)` 被如实执行（不是一次跳到终点）；② minitouch 是否把它识别为「滑动」而非「点击」（位移 ≥5px 门槛）、页面确实滚动、滚动量与直线 `insert_swipe` 版差异是否可接受；③ `--curve left/straight/right` 分别确认左弯 / 近直线 / 右弯，且落点精确到 `end`；④ **第二阶段专项**：PNG 的 B（dt / WAIT 尾部仍平滑增加但斜率比收一档前温和，无锯齿）/ C（速度中段无尖峰、尾部平滑下降但**最后不再长时间贴近 0**）；人工看是否还有「黏住终点」感 / UP 是否更干净 / 是否恢复过多 fling / 尾段是否更平稳 / 页面滚动距离有无明显改变；⑤ 长轨迹与短轨迹总时长有明显差异且都在合理范围。据真机数据再定 `dt_smooth_alpha` / `tail_slow_gain` / `tail_density_gain` 或是否整体放慢、`tail_correction_*` 是否保留 / 默认开（**另开任务，不由工具自动改参**）。⑥ 之后才逐调用点显式接入（首选 KekkaiUtilize `perform_swipe_action` 或 `list_find` 翻页），每个接入点单独 Level C
- ~~**普通滑动端点空间分布 v2（`SwipeEndpointSampler` / D022 / §4.61）的真机标定**~~ —— **已 Level C PASS（2026-09-08），当前 `SwipeEndpointParams` 默认值冻结，从本区移除**。用户多轮 MuMu 真机 + OASX「点击位置与滑动轨迹」统计（最新约 42 swipe）连续观察：起点 / 终点已从单点变为「明显但仍集中」的独立小范围分布、多条同类 trajectory 不再完全重合（轻微高度 / 角度差）、主方向稳定、无反向 / 无极端短长 / 无大角度斜滑 / 无 endpoint 飞散，连续使用无误触 / 滑不动 / 滑过头 / 业务状态错误 / Kekkai·Exploration swipe 回退。达成目标「解除固定 endpoint + 保持业务稳定」，**不继续扩大端点分布**。**重新打开 calibration 的条件见 `docs/DECISIONS.md` D022「Level C 补记」**（页面 swipe 误触 / 距离不足 / 明显过滑 / 落入危险控件 / direction guard 失败 / BehaviorTrace 与实际执行不一致 / 新统计图重现极端聚集 / 特殊 consumer 被错接）。`TouchSwipeModel` 中间轨迹算法的 Level C 另见下方「TouchSwipeModel Level C 验收」条，与 D022 无关。
- **全仓 Swipe Consumer 迁移的真机验收**（2026-09-07 已完成静态，见「已完成」表「全仓 Swipe Consumer 审查 + TouchSwipeModel 全局迁移」/ §4.48 / D018 全仓迁移补记；除 KekkaiUtilize 外全部 Level C PENDING，不得标 VERIFIED）——`BaseTask.swipe(RuleSwipe)` 经新 helper `BaseTask.swipe_trajectory` 在 minitouch 下走 `TouchSwipeModel` 轨迹，42 个 `self.swipe(S_*)` consumer + GeneralBuff `exp_50/100` 透明迁移。按风险从低到高逐模块验收（纯纵向 list → 页面滚动 → 左右翻页 → 多次循环 scroll）：① minitouch 下曲线轨迹是否被识别为「滑动」而非「点击」、页面确实滚动；② 滚动量与旧直线 `insert_swipe` 版差异是否可接受（尤其 AreaBoss / SwitchSoul / MemoryScrolls / WeeklyPurchase 的列表）；③ 连续多次 scroll 是否因单次轨迹耗时变化触发上层 `Timer` / `interval` timeout；④ 横向 ≤16px 弓形（`curve_cap = min(16, dist·0.12)`）是否经过危险可点击区域（各页 swipe area 检查）；⑤ 左右翻页（AreaBoss `S_AB_LEVEL_RIGHT` / SwitchAccount `S_SA_SVR_SWIPE_LEFT`）曲线是否影响翻页判定；⑥ Exploration `arrive_end` / 换式神 swipe 计数（依赖 `click_record.count(swipe.name)`）不受影响；⑦ GeneralBattle `random_click_swipt` 战斗中随机滑动仍正常、不误触。非 minitouch 后端回退 `Control.swipe` 端点滑动，逐字不变、无需真机
- 任何改变 minitouch 真实时序、MOVE interval、滑动 / 点击节奏的改动
- FatigueManager 各概率 / 时长参数的真实模拟器长跑采样校准
- **Fatigue 安全节点铺开 Level C（2026-09-08，§4.53 / §4.56 / §4.58 + correctness follow-up）**：Orochi / EvoZone 的 `run_alone`（§4.53）+ **RealmRaid 主循环（§4.56 / follow-up，`_realm_raid_cycle_safe_break()`，`deadline=None`；follow-up 后只在 failure recovery（刷新 / CONTINUE skip 记录）完成之后触发，helper 前 `wait_until_appear(I_BACK_RED, wait_time=RR_CYCLE_STABLE_TIMEOUT=10)`）** + **Exploration `ALONE`（§4.58 / D021，Boss win → map ready → direct exit → `page_exp_entrance` positive，`deadline=start_time+limit_time`，fatigue 后 fresh revalidate）** 已接 `begin_fatigue_task` + `try_fatigue_break`。待验：① 休息 / 发呆结束后页面是否仍保持在各任务稳定入口页；② 是否漏掉一场战斗 / 次数统计是否正确；③ 组队 / member / wild 路径确未被误插 idle；④ deadline 截断是否正常（Orochi/EvoZone/Exploration 有任务时限、RealmRaid `deadline=None` 靠 `number_attack`）；⑤ RealmRaid safe point 是否只在 recovery 完成后的稳定九宫格触发；⑥ Exploration safe point 是否只在 exit positive success 后触发且休息后 `page_exp_entrance` fresh revalidate 稳定；⑦ `RR_CYCLE_STABLE_TIMEOUT = 10` 与 Exploration exit/frame 参数的真机时序是否足够。活动副本的 Fatigue 仍待各自业务链收口后再接（见 P0）。
- **RealmRaid 九宫格固定 1→9 / 退四主流程改造 Level C（2026-09-08，§4.56 / D020）**：静态已实施 Level A/B，真机待验——① `_grid_targets` 用 `order_medal.find_everyone()` 全格扫描的稳定性（每个 match 中心落 `C_PARTITION_n.roi_front` 的映射准确率、有无相邻格串位）；② `_broken_orders` 对「刚失败、还没刷新」的格的实际命中率（漏判 → 重复挑战已破格）；③ `RR_TARGET_PACING = (1.0, 2.5)` 体感（过短 / 过长）；④ 退四触发「只剩最后 1 个可攻打目标」判定在真机是否可靠（8 格全破的识别是否稳定）；⑤ `ensure_lock(False)` 在退四场景是否确实解锁阵容、退四后 `ensure_lock(lock_default)` 恢复；⑥ `_fire_again()` 三态 + `RR_AGAIN_MAX_TRIES=4` / `RR_AGAIN_TIMEOUT=10` / `RR_AGAIN_POST_CLICK_TIMEOUT=3` 体感、`I_SHOW_AGAIN`「不再提示」是否稳定点掉；⑦ 退四最终失败 → `check_refresh()` 收敛是否正常（不停在失败页）；⑧ 普通失败按 `when_attack_fail`（默认 REFRESH）是否与旧行为一致；⑨ **（correctness follow-up）** CONTINUE 下 `failed_orders` 局部 set 跳过刚失败目标在真机是否与预期一致（失败格是否也变 broken、两套状态重叠是否有副作用）；⑩ Fatigue Safe Point 后移后确实在 refresh / skip 完成之后触发（`wait_until_appear(I_BACK_RED, wait_time=RR_CYCLE_STABLE_TIMEOUT=10)` 是否够覆盖刷新 / 结算动画）；⑪ 退四入口 `_is_only_remaining_target` 二次确认是否偶发误否决（模板抖动导致本该退四却回主循环重扫）；⑫ 退四异常路径 `try/finally` 恢复锁在真机可观测。`RR_TARGET_PACING` / `RR_AGAIN_*` / `RR_CYCLE_STABLE_TIMEOUT` 均 PROVISIONAL，据真机数据调整时连带更新 D020 / D001 补记 + §4.56
- **RealmRaid 再次挑战确认弹窗 reaction Level C（2026-09-08，§4.57）**：`I_FRESH_ENSURE` 已由资产与调用链确认是刷新确认 / 再战确认共用的确认按钮；只在 `_fire_again()` 的再战流使用 `RR_AGAIN_CONFIRM_DELAY=(0.3,0.6)`。观察顺序应为 `Fire again: attempt N, reaction 0.xx` → `I_FIRE_AGAIN` 点击 → 确认弹窗 → 约 0.3~0.6 秒后 `I_FRESH_ENSURE` 点击 → prepare / battle。核对确认弹窗在 delay 后仍存在才点击、消失时不发生 stale click；`I_SHOW_AGAIN` 不新增延迟；`REACTION_FIRE=(0.4,0.8)` 仍只属于 `I_FIRE_AGAIN`，不与确认 reaction 叠加。该区间为 PROVISIONAL，真机数据调整时同步 D001 补记、§4.57 与对应测试。
- BehaviorTrace enable=True 在真机上的实际开销与日志质量
- **SETTLEMENT CONTRACT V3 Level C 验收**（GeneralBattle 新通用结算策略，§4.3.1 / §4.39 / D016）——前置：待跑 config 开 `script.optimization.behavior_trace_enable=true`；用户手动把游戏带到能跑 GeneralBattle 的状态（优先 RyouToppa 普通 config）。核心验收项：① 通用结果页首帧确实执行**两次** `random_default` 点击、中间隔约 0.7~1.0s、两次坐标不同；② 之后不立即出现第三击；③ reward 页命中 `I_OVER_GHOST` / `I_GB_SKIN_CONFIRM` 后本帧不再 region click；④ reward marker 命中 → `random_default`，未命中 → `random_save_right` / `random_save_bottom` 约 80/20；⑤ 三个安全区落点全在各自 ROI 内、每次重采；⑥ `_exit_matcher` 正常退出、退出后不再点；⑦ `C_RANDOM_DEFAULT` 新 ROI 不误触其它按钮。分析器 `dev_tools/settlement_trace_check.py` 已按 3 区域名 + 整 ROI 做最小更新（count / x·y / ROI-relative / ROI 内外 / `distinct_xy` / 间隔 / 高速重复），**但不校验双击时序结构与 80/20 分流**——需要时新契约的专用 Level C 分析器另行设计。另需确认 `I_BATTLE_STATE_INFO`-only 结果帧是否需要也纳入强制双击（本轮保守排除）、marker miss 的 RIGHT/BOTTOM 是否覆盖绝大多数剩余布局、特殊任务真实战后 UI 有无遗漏。`I_OVER_GHOST` / `I_GB_SKIN_CONFIRM` 用户当前无法稳定复现 → 未自然出现标 PASS_WITH_PENDING，不为此改特殊逻辑。
- **T7-3.2 `RyouToppa.C_AREA_1` Level C 验收**（首个普通 Point Target 生产 opt-in，§4.31）——前置：待跑 config 开 `script.optimization.behavior_trace_enable=true`，用户跑 `RyouToppa` 到点击目标区域。核对 `log/behavior/*.jsonl` 里 `target="area_1"` 的落点：集中在 `C_AREA_1` ROI 相对 `(0.58,0.59)` 附近（屏幕 ≈ `(643,209)`）、全部落在 wide_card Safe ROI `(525,147,201,104)` 内、每次点击不同（非固定点）；对照 `target="area_2".."area_8"` 应仍是各自 ROI 全区域均匀。若 `C_AREA_1` 落点明显偏移 / 出框 / 命中率下降 → 回滚点见 §4.31，或据真机数据调 `wide_card` preferred（走 T7-3.1 式的 ROI-relative 重算，不拍脑袋）。
- T7-1 点击空间模型 v2 的 preferred center / 混合分布标定（先用 ManualClickRecorder 在真机采人工点击习惯样本）
- ManualClickRecorder v1 的端到端人工验收：真机上按 config 定位 MuMu、窗口移动 / resize 后逻辑坐标仍正确、ROI 模式 u/v 与 preferred-center 摘要（见 `docs/AI_CONTEXT.md` §4.21 与 DEVELOP_LOG 的验收清单）

---

## 可以纯静态验证的任务（Level A / B，当前可做）

- T4-1 List Changed / Stable 组件本体 + 单测（已完成）
- T4-2 FrameState Wait Layer 本体 + 单测（已完成，`frame_provider` / `clock` 注入使其完全可静态验证）
- T4-3 Wait / Retry 策略接口设计（**已完成归纳，结论「当前不落地」**，见「已完成」/ D015）
- Level A cleanup **批次 1 已完成**（§4.36，只删零引用死代码，behavior-neutral，710→708）。剩余队列（`docs/状态验证与重试模式归纳.md` §16 + §4.36 DEFERRED）里的 B 类要么正确语义不唯一（`ocr_time` crash / `check_utilize_add` 返回语义）、要么按 D007 保留（`check_card_num` stdlib random）、要么 NOT_REPRODUCED（lazy_roll），C 类（RealmRaid `fire()` 不可达 return + 死分支）与 Level C 状态迁移绑定——**没有「下一批 cleanup」是纯安全可做的**
- Exploration 状态机迁移前静态收口（第四案例，已完成 §4.37）——`tests/test_exploration_state.py`（33 用例）+ `docs/Exploration状态机静态收口.md`，`tasks/Exploration/` 零 diff。**四案例静态收口全部完成**；下一步是等 Level C 做第一个真实迁移，或再收口 `Dokan` / `MetaDemon`
- T5-2 前半 `list_find` 迁移前静态收口 + characterization（已完成）
- 点击空间机制全仓审查（已完成，§4.20）
- ManualClickRecorder v1 的纯函数部分（坐标换算 / u·v / 统计 / JSONL / config 安全，已完成 31 用例）
- 人工点击日志 burst-collapse 离线分析器 `dev_tools/manual_click_analyze.py`（已完成 22 用例，纯离线只读、不改原始日志、不改生产代码）
- T7-1 第 1、2 阶段：ClickSampler + LEGACY_UNIFORM + ClickProfile + HABIT/STRICT/UNIFORM + Safe ROI（均已完成，新策略零生产消费者、默认仍 LEGACY_UNIFORM，见「已完成」）
- 现有循环 / 等待逻辑的 characterization 测试（迁移前锁现状，不改生产行为）——`list_find`（已完成 §4.19）、
  `Kekkai`（已完成 §4.33 / `docs/Kekkai状态机静态收口.md`）、`RealmRaid`（已完成 §4.34 /
  `docs/RealmRaid状态机静态收口.md`）、`Exploration`（已完成 §4.37 /
  `docs/Exploration状态机静态收口.md`）之后，下一批候选：`Dokan` / `MetaDemon`
  的 State/Action 循环。**注意**：四组已收口对象的公共层并不相同——`list_find` / Kekkai 的
  共同点是「swipe 后等画面停稳」（视觉结构等待），RealmRaid 没有这类等待、其共同点是「点击 →
  旧标识消失即成功」+「`while`+`interval` 无超时反复点」，而 Exploration `fire()` 已是「正向
  战斗页确认 + 双重上界 + 可返回 False」的目标形态（D015 未来 retry primitive 的现成参照）。
  抽公共层前需按模式分组，且超时值全部是 Level C 未知，本阶段继续只收口不抽象
- 任何新增的纯图像 / 纯状态 detector，或设备无关、可注入依赖的等待 / 轮询外壳
- ~~**`TouchSwipeModel` 接入（Plan B）① 模型 ② executor ③ `Control.swipe_trajectory` ④ 首个生产 opt-in**~~ ——**① ~ ④ 已完成**：①②③ 基础设施（2026-09-04，D018，回归 783→836）；**④ 首个逐调用点 opt-in = KekkaiUtilize 标准 PASS 搜索的列表下划（K1，2026-09-04）** —— `_run_search_pass` 的下划入口换成 `_perform_search_swipe`：minitouch → `TouchSwipeModel().generate((rand,rand),(rand,rand-416))` → `Control.swipe_trajectory(..., control_name='KEKKAI_UTILIZE_SWIPE')`；非 minitouch → 回退旧 `perform_swipe_action`（`swipe_adb`）；怠惰模式 / 416px 几何 / D017 契约 / settle 全不动。`test_kekkai_utilize_state` 46→53，回归 897→904。见「已完成」表「KekkaiUtilize TouchSwipe 接入 K1」。**剩余（Level C + 后续）**：⑤ K1 真机验收（minitouch 连续扫描是否正常滚动 / 识别为滑动 / 无 fling / 业务不受影响）；**K2 静态完成（2026-09-07）** —— 标准 minitouch 路径 416px → 随机 `SWIPE_DISTANCE_RANGE` + 轻微整体斜度 `SWIPE_LATERAL_OFFSET_RANGE`（`end_x` 有界，`TouchSwipeModel` 未改）；**首次 Level C 实测「一次滚 >4 格」→ 回调 `(212,265)` → `(140,180)`，需二次 Level C**（见「已完成」表 K2 行）；**K3 已完成（2026-09-07）** —— 标准 minitouch 路径 swipe settle 固定 `time.sleep(2)` → `wait_for_changed_and_stable`（`frame_wait` 首个生产消费者），失败 → `PassResult.ABORT`（`no change ≠ BOTTOM`）；K3 真机验收（`SWIPE_WAIT_ROI` / 4 个 provisional 阈值 / `timeout=3.0` / 是否比旧 2 秒更快）见「已完成」表 K3 行；**K4-1 + K4-2 Level A/B 已实施（2026-09-07）** —— K4-1 `I_IS_SELECTED` 锚点测 `actual_scroll_dy` + K4-2 一帧对一帧投影去重（见「已完成」表 K4 行 / §4.47 / D017 K4 补记）；K4 overall Level C pending；`new-region-only optimization`（按 dy 只裁新进入视觉区域跑识别）⏸ optional，未实现；**⑥ 全仓 Swipe Consumer 迁移已完成静态（2026-09-07，见「已完成」表「全仓 Swipe Consumer 审查 + TouchSwipeModel 全局迁移」/ §4.48 / D018 全仓迁移补记）** —— 新增薄公共 helper `BaseTask.swipe_trajectory`，`BaseTask.swipe(RuleSwipe)` 委托它，42 个 `self.swipe(S_*)` ordinary-scroll consumer + GeneralBuff `exp_50/100` 透明迁到 trajectory（minitouch）/ 端点回退（非 minitouch）；回归 1067→1083。**迁移 consumer 真机验收（除 KekkaiUtilize 外全部 Level C PENDING）** 见下方 Level C 区
- **全仓 Swipe 迁移的 3 个 future candidate（Level A/B 可做，但各有前置）**：
  - `BaseTask.list_find` 翻页 swipe → trajectory —— **settle 部分已于 2026-09-08 迁到 FrameWait（T5-2 后半，见上）**；swipe 输入后端仍 `self.device.swipe`（`RuleList` 翻页几何 D013 明令不动），是否也换成 `swipe_trajectory` 单独评估（低优先，翻页 swipe 短、直线足够）
  - KekkaiActivation `check_card_num` 好友卡 `swipe_adb` → trajectory —— 距离敏感、KekkaiUtilize 姊妹路径，应像 KekkaiUtilize 那样单独走 K1 式分阶段 + 自己的 Level C（`test_kekkai_activation_state.py` 已有 `swipe_adb` 参数基线）
  - RyouToppa `flush_area_cache` swipe → trajectory —— 显式依赖 `duration=`（D003 / T3-1 契约、`test_swipe_duration_cleanup.py` 锁定）+ 逐 attempt 手调参数 + retry/verify，需连 duration 契约一起评估；且是活跃 WIP
- Config 字段新增（默认值不改变现有行为）
- 死代码删除（先全仓 grep 确认零调用方）
- API 参数契约测试（锁「谁调用、带什么参数」）
- 文档体系维护

## L1 + L2 集成进 master（2026-09-21，`docs/AI_CONTEXT.md` §4.82）

- **状态**：Level A/B **完成**（1668 → 1809）；Level C **待验**。全仓 1092 个点击调用点已登记（`docs/L2_CALLSITE_REGISTER.md`）：human 179 + rule 913；NEEDS_C 24；没有新增迁移（无真机证据不加延迟）。**（这是 2026-09-21 集成时点的历史数据，最新分类与统计见下一条 L2 当前状态。）**
- **L2 当前状态（2026-09-21，§4.87 C1-A1 + §4.88 C0 收口）：已完成当前批准的开发范围，后续迁移暂停，Level C 待验。** 当前**没有获批的 L2 功能开发任务**；不是「全部 L2 点位已迁移」。C0 复核的 24 点最终状态：**已完成 4**（Dokan `priority_enter_dokan` NORMAL / Pets `_feed` NAVIGATION / SixRealms `refresh_store` CONFIRM / `choose_and_enter_island` NORMAL）；**DEFERRED（C0 建议 MIGRATE、用户暂缓）6**（Duel 练习入口 ×2、SixRealms 商店 ×4）；**KEEP_IMMEDIATE 3**（DemonRetreat / Quiz / SixRealms `open_shop`）与 **KEEP_SPECIAL 4**（Buy `buy_more` ×2 / `switch_moon_sea_shikigami` / `_mark_peacock_boss`）——不安排迁移；**NEEDS_C + DEFERRED 7**（GeneralInvite 接受邀请 ×5 / GeneralBattle 准备弹窗 ×2：技术上需 Level C 依据，且用户暂缓）。登记册 1092 点：ALREADY_L2 37 / DEFERRED 6 / NEEDS_C 7 / KEEP_IMMEDIATE 630 / KEEP_SPECIAL 232 / MIGRATE 7 / PRIMITIVE 35 / EXCLUDED 138。**DEFERRED 是用户排期，不是技术完成，也不是永久禁止**；重启前须重新评估（GeneralInvite 应先设计事务级 reaction 与有界状态确认）。全量 1980 → 1996。
- **仍待办（有依据）**：① Batch B（AreaBoss / Secret / DemonEncounter）逐项审查（暂无排期）；② NEEDS_C 7 处（GeneralInvite 接受邀请 ×5 / GeneralBattle 准备弹窗 ×2）技术上需 Level C 依据，且用户已决定暂缓；
  ③ `ui_click*` 透传 policy 的设计；④ Navigator 逐 edge NAVIGATION 元数据；⑤ `RR_AGAIN_CONFIRM_DELAY` 归属；⑥ Orochi 野队与 WeeklyPurchase 是项目排除项，不进 backlog。
- **Level C 最小场景（MuMu 实测，每项只看现象，不调参）**：
  1. **普通 reaction + fresh confirm**：GeneralInvite 房间「+」（NORMAL）与预设面板（NORMAL / CONFIRM）——日志里 reaction 落在区间内、reaction 期间按钮被换位 / 消失时零点击；
  2. **FIRE**：RealmRaid / Orochi / EvoZone / ActivityShikigami 各一次，以及 EternitySea / FallenSun / Sougenbi（Batch A）——reaction 符合各任务 `fire_reaction`、无双重等待、准备页 / 战斗页 marker 稳定命中、结果 / 奖励页残留不被误判；
  3. **Settlement**：一场普通副本结算——节奏 / 点击数与集成前一致（segment / 9 次安全帽），无额外 reaction；
  4. **导航**：ActivityShikigami 入口与 Exploration 退出（NAVIGATION / CONFIRM）——不过度变慢；
  5. **L1 consumer 落点**：Dokan / SixRealms 购买区 / WantedQuests / WeeklyTrifles / QuickLoadout / SwitchSoul / GeneralRoom / Chess 发现卡的点击落在目标内，BehaviorTrace 坐标 = 实际点击；
  6. **后端**：minitouch dwell / pressure 在 MuMu 上正常；百鬼夜行 window_message fast 仍高频可用；
  7. **KekkaiUtilize 一轮完整寄养**：分类切换 ≤ 4 次点击、失败短期重试、成功 OCR 调度（含 `success_jitter`）——确认 L1 / L2 集成没有让寄养内部循环变慢。

## L1 Stage 1：BaseTask primitive 执行入口统一（2026-09-21，`docs/AI_CONTEXT.md` §4.83）

- **状态**：Level A/B **完成**（1809 → 1836）；Level C 无新增待验项（落点分布与采样均未改）。BaseTask 直接 `device.click` 8 → 0；显式执行器调用点 25 → 33；不是 100% 全仓 L1。
- **第二阶段（已完成，2026-09-21，§4.84）**：剩余 9 个生产直接 `device.click` 已迁入执行器，生产直接单击归零（1836 → 1854）。
- **第三阶段 A（已完成，2026-09-21，§4.85）**：长按独立执行入口 `execute_long_click` 落地，BaseTask 的 6 处 `device.long_click` 全部迁入（1854 → 1894；采样 / 时长 / 后端 / 等待均未改）。
- **第三阶段 B（已完成，2026-09-21，§4.86）**：Login / DailyTrifles / WeeklyPurchase 的最后 3 处直接单击迁入执行器（项目业务排除不再等于点击架构豁免；WeeklyPurchase 仍不做 L2 Reaction Policy），并建立全局静态守卫 `dev_tools/click_entry_guard.py`（默认禁止、精确白名单、新文件自动扫描、过期白名单失败）。至此当前 master 范围内生产单击 / 长按全部经各自的 L1 执行器（1894 → 1948）。
- **L1 之后的候选（未开始，待用户确认）**：① BehaviorTrace 补 ROI / 来源 / duration / provenance 字段（可选）；② ROI 质量审计（Login 固定坐标 (106, 535) 是否在真实可点击区域、小目标、退化 ROI、大随机区域 Point 与 Region 语义、`random_click` 改共享资产名字）需 Level C 或离线标定；③ `nemu_ipc` 长按未注册（既有行为）与 `Control.multi_click` 死代码的处置。
